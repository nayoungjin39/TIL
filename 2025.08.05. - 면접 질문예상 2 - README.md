# 클래스랑 스트럭스 의 차이  

##  사용 관례 (Convention)
struct는 일반적으로 **데이터를 담는 단순한 구조체(POD)**에 사용됩니다.
예: 좌표, 설정 값 등.

class는 **멤버 함수와 데이터, 캡슐화, 상속, 다형성 등 객체지향 프로그래밍(OOP)**을 구현할 때 사용됩니다.

하지만 문법적으로는 struct도 클래스처럼 생성자, 소멸자, 상속, 멤버 함수 등을 모두 사용할 수 있습니다.

- “C++에서 class와 struct는 문법적으로 거의 동일하지만, 가장 큰 차이는 기본 접근 제어자입니다. struct의 멤버는 기본적으로 public이고, class는 기본적으로 private입니다.

관례적으로 struct는 단순한 데이터 구조, 즉 값만 담는 구조체에 사용되고, class는 캡슐화, 상속, 다형성 등 객체지향 설계에 사용됩니다.

예를 들어, 좌표나 RGB 값처럼 데이터를 담는 용도는 struct로 만들고, 기능과 데이터를 함께 다루는 객체는 class로 구현하는 것이 일반적입니다.

실제로 struct도 생성자, 멤버 함수, 상속 등이 모두 가능하기 때문에 문법적으로 큰 차이는 없지만, 의도를 구분해서 사용하는 것이 좋다고 생각합니다.”

💡 팁
**핵심은 "기본 접근 제어자"와 "관례적 사용 목적"**이라는 걸 명확히 말하세요.

면접관이 추가 질문할 수 있으니, struct도 상속이나 함수 정의 가능하다는 것도 짚고 넘어가는 게 좋습니다.

가능하면 간단한 예시 하나 정도 말하는 것도 좋습니다.

예:

“예를 들어, struct Point { int x, y; };처럼 좌표만 담는 구조는 struct를 쓰고,
class Circle { double radius; ... };처럼 동작이 있는 경우는 class를 사용합니다.”


## ✅ C++에서 `class` 와 `struct` 차이 정리표

| 항목                | `struct`                           | `class`                              |
|---------------------|-------------------------------------|-------------------------------------|
| **기본 접근 제어자** | `public`                            | `private`                          |
| **캡슐화**           | 비권장 (가능은 하지만 잘 안 씀)        | 적극 사용                          |
| **상속 및 다형성**   | 가능 (class와 동일)                | 가능 (주 용도)                        |
| **사용 목적/관례**   | 단순 데이터 묶음 (POD 구조체 등)     | 객체지향 설계 (멤버 함수 + 데이터)     |
| **생성자/소멸자**    | 사용 가능                          | 사용 가능                             |
| **멤버 함수**        | 정의 가능                          | 정의 가능                             |
| **기본 인식**        | 값(value) 중심 구조체               | 기능 중심 객체                       |

## 🎤 면접에서 이렇게 말해보세요

> “C++에서 `class`와 `struct`는 문법적으로 거의 동일하지만, 가장 큰 차이는 **기본 접근 제어자**입니다.  
> `struct`는 기본적으로 `public`, `class`는 `private`이며,  
> 관례적으로 `struct`는 데이터 구조체, `class`는 객체지향적인 기능 설계에 사용합니다.”

예를 들어 이런 식으로 정리하면 좋습니다:

```cpp
// struct 예제: 단순 데이터
struct Point {
    int x;
    int y;
};

// class 예제: 기능 중심
class Circle {
private:
    double radius;

public:
    Circle(double r) : radius(r) {}
    double area() { return 3.14 * radius * radius; }
};
```

---

## 💡 요약 정리

- `struct`는 기본 `public`, `class`는 기본 `private`
- 문법적으로는 거의 동일 (상속, 함수, 생성자 모두 가능)
- 사용 목적에 따라 의미를 나누는 것이 좋은 코드 스타일
- 면접에서는 **차이점 + 관례적 사용 목적**을 함께 설명하는 게 핵심!
