📌 1. Static 함수의 정의
Static 함수란 객체가 아닌 클래스 자체에 속하는 함수 또는
파일 내부에서만 사용 가능한 전역 함수를 의미한다.
C++에서 static 키워드로 선언하며, 호출 방식과 접근 범위를 제한할 수 있다.

📌 2. Static 함수의 조건
클래스 멤버 static 함수
객체 생성 없이 호출 가능

클래스이름::함수명() 형식

non-static 멤버에 접근 불가

객체에 종속되지 않기 때문에, this 포인터 사용 불가

static 멤버 변수에는 접근 가능

정적 바인딩 (virtual 불가)

virtual static 조합은 허용되지 않음

전역 static 함수 (파일 범위 제한)
해당 cpp 파일 안에서만 사용 가능

외부 링크 제한(Local linkage)

이름 충돌 방지용으로 주로 사용

static 함수는 언제 쓰나요?	전역 함수 대체, 공용 유틸, 객체 독립 로직 설계 의도 파악
static 함수에서 non-static 멤버를 쓸 수 없는 이유는?	this 포인터 부재 개념 이해 확인
static 함수와 전역 함수의 차이는?	OOP 설계와 캡슐화 이해도 평가
virtual과 static을 함께 쓸 수 없는 이유는?	정적 바인딩 vs 동적 바인딩 차이 이해
static 함수의 메모리 위치는 어디에 있나요?	코드 메모리(텍스트 영역)와 데이터 영역 차이 파악
static 멤버 변수와 static 함수의 관계는?	공유 데이터 관리 원리 이해

💡
제가 원하면 이걸 노션용 표 + 예시 코드 형태로 깔끔하게 만들어 드릴 수 있습니다.
그렇게 하면 면접 대비에 바로 써먹기 좋아요.



ChatGPT의 말:
좋아요 👍 그럼 Static 함수의 장점과 단점도 면접 대비용으로 정리해드릴게요.

## 📌 Static 함수 — 장점 & 단점

| 구분 | 장점 | 단점 |
|------|------|------|
| **클래스 멤버 static 함수** | - 객체 생성 없이 호출 가능 → 성능 및 메모리 절약<br>- 객체와 무관한 공용 로직 구현에 적합<br>- static 멤버 변수와 쉽게 연동 가능<br>- 전역 함수보다 캡슐화 가능 | - non-static 멤버 접근 불가 (this 없음)<br>- 가상 함수 불가 → 다형성 제한<br>- 테스트/확장성 낮음 (객체 기반 로직과 분리됨) |
| **전역 static 함수 (파일 내부)** | - 파일 내부 전용 → 네임 충돌 방지<br>- 외부 노출 최소화 → 캡슐화 강화<br>- 링크 타임 최적화 가능성↑ | - 재사용성 낮음 (다른 파일에서 호출 불가)<br>- 오버헤드 없는 대신 확장성 제한 |


"Static 함수와 Singleton 패턴의 차이는 뭔가요?"
→ Static은 객체 없이 호출, Singleton은 객체 단일화

"Static 함수는 메모리에 언제 로드되나요?"
→ 프로그램 시작 시(정적 영역에 로드)

"Static 함수를 쓰면 객체 지향적 설계에 위배되나요?"
→ 무분별 사용 시 절차지향적으로 흐를 수 있음

장점 (Pros) 👍
메모리 효율성: 스테틱 함수는 프로그램 실행 시 한 번만 메모리에 할당됩니다. 따라서 여러 번 호출되어도 메모리를 추가로 점유하지 않아 효율적입니다. 인스턴스를 생성할 때마다 새로운 메모리 공간을 할당받는 인스턴스 멤버 함수와 대조적입니다.

속도 향상: 객체 생성 과정 없이 클래스 이름을 통해 직접 접근하고 호출할 수 있으므로, 인스턴스 함수에 비해 호출 속도가 빠릅니다.

쉬운 접근 및 공유: 클래스명.함수명() 형태로 어디서든 쉽게 호출할 수 있습니다. 이는 전역적으로 사용되어야 하는 유틸리티 함수(예: 수학 계산, 형식 변환)나 여러 객체가 공유해야 하는 데이터를 다룰 때 매우 유용합니다.

명확한 역할 구분: 스테틱 함수는 인스턴스의 상태(멤버 변수)에 의존하지 않습니다. 따라서 함수의 역할이 클래스의 특정 인스턴스와 무관한, 즉 클래스 자체에 소속된 기능이라는 것을 명확히 나타내 줍니다.

단점 (Cons) 👎
유연성 부족: 스테틱 함수는 인스턴스 멤버(객체의 상태를 나타내는 변수나 다른 함수)에 직접 접근할 수 없습니다. 이는 스테틱 함수가 메모리에 할당되는 시점에는 인스턴스가 존재하지 않을 수 있기 때문입니다. 이로 인해 객체의 상태에 따라 동작이 달라져야 하는 기능을 구현하기 어렵습니다.

전역 변수와 유사한 문제: 어디서든 쉽게 접근할 수 있다는 장점은 반대로 코드의 결합도를 높이는 단점이 될 수 있습니다. 스테틱 함수에 대한 의존성이 높아지면 코드를 수정하거나 테스트하기 어려워지고, 객체지향 프로그래밍의 캡슐화 원칙을 해칠 수 있습니다.

상속 및 다형성 적용 불가: 스테틱 함수는 클래스에 귀속되므로 자식 클래스에서 오버라이딩(재정의)할 수 없습니다. 따라서 상속을 통한 다형성을 구현하는 데 사용할 수 없어 객체지향 설계의 유연성을 떨어뜨립니다.

지속적인 메모리 점유: 프로그램 시작부터 종료까지 메모리에 상주하기 때문에, 사용 빈도가 매우 낮은 함수를 스테틱으로 선언하면 메모리 공간을 불필요하게 낭비하는 결과를 초래할 수 있습니다.
