# 언리얼 기초 개념

## Unreal Object Class

  - Native C++의 문제점
  게임을 플레이하는 사람들을 위해서는 최대한 최적화되어서 누구나 플레이 가능해야함.
  그래서 메모리를 직접 관리 가능한 C++가 적합함.
  다만 잘못 접근하면 메모리 누수 혹은 크래시로 인해 유지보수가 까다로움.
  게임을 만드는 사람들을 위해서는 규모가 커져도 실수 없이 편리하게 관리할 수 있어야함.
  이를 해결하기 위해 후발 언어(C#, …)들은 실수를 줄이기 위한 기능(가비지 컬렉션)과
  생산성을 높혀주는 기능(리플렉션, …)들이 언어에 내장되어 있음.

## Unreal C++
  특유의 매크로 문법을 활용해서 Native C++에다가 후발 언어의 기능들을 추가 구현함.
  이러한 기능들이 지원되는 클래스를 Unreal Object 클래스라고 하고,
  이러한 문법들을 Unreal C++이라고 함.

- 들어가기 전에.우리가 작성한 C++ 클래스가 보이게끔 설정이 필요함.
  Settings 클릭 > Show C++ Classes 체크.

## 새 C++ 클래스 생성해보기.
    
  Unreal Object 클래스가 없으면 Content Browser에서도 C++ Classes라는 폴더가 안보임.
  프로젝트 생성 후 처음 Unreal Object 클래스를 만들때는 아래 그림을 따라서 진행.
  앞으로 저자는 아래 과정을 “새 C++ 클래스”라고 부를 예정.
  
  Tools > New C++ Class… 클릭.

  CHOOSE PARENT CLASS 화면. 말그대로, 부모 클래스를 지정하란 뜻.
우리는 None 선택 후 Next 버튼 클릭. None은 Native C++ 클래스를 만들때 사용함.

Name에는 “SXUnrealObject”라고 작성. SX-는 ShooterX의 약자. 앞으로도 이니셜을 달 예정.
Path에는 딱히 수정하지 않음.
Create Class 버튼 클릭.

비주얼 스튜디오에서 클래스 생성 시에 나오는 경고창. Reload All 클릭.
앞으로도 동일하게 대응하면 됨.

Yes 클릭. 그럼 언리얼 에디터가 꺼짐.

-빌드 및 실행 해보기.
Visual Studio 클릭 후 F5를 누르면 빌드 및 실행이 됨.
빌드 및 실행 해보아도 Content Browser에는 우리가 만든 클래스가 안보임.
언리얼 오브젝트 클래스가 아니라서 언리얼 에디터가 해당 C++ 클래스 인식 불가.

UObject 클래스로 만들면 Content Browser에 C++ Classes 폴더가 생기고, 그 안에 해당 클래스가 보임

## 언리얼 오브젝트 클래스로 수정해보기.
```c++ SXUnrealObject.h
#pragma once

#include "CoreMinimal.h"
#include "UObject/NoExportTypes.h"
#include "SXUnrealObject.generated.h"

/**
 * 
 */
UCLASS()
class SHOOTERX_API USXUnrealObject : public UObject
{
	GENERATED_BODY()

};
```

``` // SXUnrealObject.cpp
#include "SXUnrealObject.h"
```

Content Browser > C++ Classes > ShooterX 폴더에 우리가 만든 C++ 클래스가 인식되기 시작함.
