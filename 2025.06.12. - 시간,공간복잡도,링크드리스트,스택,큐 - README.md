# 시간 복잡도 

- 시간 복잡도는 문제를 해결하는데 걸리는 시간과 입력의 함수 관계를 가리킨다.
  컴퓨터과학에서 알고리즘의 시간복잡도는 입력을 나타내는 문자열 길이의 함수로서
  작동하는 알고리즘을 취해 시간을 정량화하는 것이다.
- 정의가 다소 난해하게 느껴질 수 있을 것 같아요! 일단, 정의를 외우려고 하지마시고 사례를 통해서 체감을 하시는게 좋아요!

_사례_

```c++
#include <iostream>
#include <vector>
using namespace std;

int find_max_num(const vector<int>& arr) {
    // 배열 내 최댓값을 찾는 로직 (이중 루프)
    for (int i = 0; i < (int)arr.size(); i++) {
        bool isMax = true;
        for (int j = 0; j < (int)arr.size(); j++) {
            if (arr[i] < arr[j]) {
                isMax = false;
                break;
            }
        }
        if (isMax) {
            return arr[i];
        }
    }
    // 모든 원소를 비교했을 때 최댓값을 찾지 못했다면(이 경우는 불가능하지만)
    // 예외처리를 위해 -1 반환
    return -1;
}

int main() {
    cout << "정답 = 6 / 현재 풀이 값 = " << find_max_num({3, 5, 6, 1, 2, 4}) << "\n";
    cout << "정답 = 6 / 현재 풀이 값 = " << find_max_num({6, 6, 6}) << "\n";
    cout << "정답 = 1888 / 현재 풀이 값 = " << find_max_num({6, 9, 2, 7, 1888}) << "\n";
}
```

_시간 복잡도를 계산해 볼까요?_

```c++
for (int i = 0; i < (int)arr.size(); i++) { // arr의 길이만큼 아래 연산이 실행
    bool isMax = true;
    for (int j = 0; j < (int)arr.size(); j++) { // arr의 길이만큼 아래 연산이 실행
        if (arr[i] < arr[j]) { // 비교 연산 1번 실행
            isMax = false;
            break;
        }
    }
    if (isMax) { // 비교 연산 1번 실행하지만 미미하므로 편의상 실행시간엔 포함시키지 않을게요!
        return arr[i];
    }
}
```

}
​
- 위에서 언급한 말을 다시 살펴볼게요.
  - 문제를 해결하는데 걸리는 시간과 입력의 함수 관계
  
- 입력
  - array의 원소 갯수 = N
  
- 실행 시간
    - array의 원소 갯수 X array의 원소 갯수만큼 비교 연산 = N X N
    - 그러면 우리는 이제 이 함수는 $N^2$ 만큼의 **시간이** 걸렸겠구나! 라고 말할 수 있습니다.
      
- 시간 복잡도는 **Big-O(빅오) 표기법**으로 표시하며 이 경우엔 O($N^2$)의 시간 복잡도를 가진다고 합니다.

_사례2_

```c++
#include <iostream>
#include <vector>
using namespace std;

int find_max_num_linear(const vector<int>& arr) {
    int max_num = arr[0];
    for (int num : arr) {
        if (num > max_num) {
            max_num = num;
        }
    }
    return max_num;
}

int main() {
    cout << "정답 = 6 / 현재 풀이 값 = " << find_max_num_linear({3, 5, 6, 1, 2, 4}) << "\n";
    cout << "정답 = 6 / 현재 풀이 값 = " << find_max_num_linear({6, 6, 6}) << "\n";
    cout << "정답 = 1888 / 현재 풀이 값 = " << find_max_num_linear({6, 9, 2, 7, 1888}) << "\n";
}
```

- 이번에는 이 함수의 시간 복잡도를 계산해볼까요?

```c++
int max_num = arr[0]; // 연산 1번 실행
for (int num : arr) { // array 의 길이만큼 아래 연산이 실행
    if (num > max_num) { // 비교 연산 1번 실행
        max_num = num; // 대입 연산 1번 실행
    }
}
```

- 입력
    - array의 원소 갯수 = N
- 실행 시간
    - max_num 대입 연산 = 1번
    - max_num 대입 연산 1번 + array의 길이 X (비교 연산 1번 + 대입 연산 1번) = 1 + 2N
    - 마찬가지로 이 함수는 2N + 1의 **시간이** 걸렸겠구나! 라고 말할 수 있습니다.
    - 하지만, 빅오 표기법에서는 **N의 지수부분만이 유효하게 쓰이고 나머지는 절삭**합니다.
        - 다시 말해서 2N + 1 → O(N)입니다.

# 공간 복잡도

- 시간 복잡도와는 다르게 공간 복잡도는 문제를 해결하는데에 대한 공간과의 상관관계를 말합니다!
- N개의 입력이 주어지면 공간을 얼마나 쓰는지 나타내는 것이에요! 아래의 코드를 보면서 설명드릴게요!

```c++
#include <iostream>
#include <vector>
#include <algorithm> // max_element 사용
using namespace std;

int largest_product(const vector<int>& arr) {
    vector<int> products; // 배열은 벡터를 쓸겁니다!
    for (int a : arr) {
        for (int b : arr) {
            // products 배열에 원소가 들어가는 이 연산은 (a * b)번이 실행되겠죠? 2중 루프!
            products.push_back(a * b);
        }
    }
    // products의 최대값을 찾는 함수
    int maxVal = *max_element(products.begin(), products.end());
    // 따라서, products의 공간 복잡도는 O(N^2)
    return maxVal;
}

int main() {
    vector<int> myList = {1, 3, 5, 7, 9};
    cout << largest_product(myList) << "\n";
}
```

- 공간 복잡도는 시간 복잡도에 비해서는 중요한 지표는 아니지만 그래도 위에서 얘기한 것 처럼 지나치게 공간을
  헤프게 쓰는 것에 대해 주의만 해주시면 됩니다!
- 현업에서, **알고리즘의 성능 향상을 하기 위해선 공간을 필수적으로 더 사용해야 한다면 주저하지마세요!**

# 링크드리스

**유동적으로 연결고리를 떼었다가 붙였다가 할 수 있는 자료구조**를 **링크드리스트**라고 합니다!

- 링크드리스트 용어 설명
    - 노드
        - 위의 예제에서 각 화물 칸들을 링크드리스트에서는 노드라고 해요!
        - 맨 앞의 노드를 Head, 맨 뒤의 노드(포인터가 NULL)를 Tail이라고 합니다!
    - 포인터
        - 현재 노드가 가리키는 다음 화물 칸을 포인터라고 해요!

```
["자갈"] -> ["흑연"] -> ["밀가루"] -> ["우편"]
```

- 여기서 자갈의 포인터는 흑연, 흑연의 포인터는 밀가루, 밀가루의 포인터는 우편, 우편의 포인터는 NULL이에요!
- 자, 그러면 위에서 자동으로 배열이 조정되었으면 좋겠는데… 이런건 어떻게 쉽게할까요?! 라는 질문을 제가 했었죠?
  이걸 쉽게 하려면 링크드리스트를 사용하면 됩니다!
- 다시 말씀드리면, 배열이 빠르게 값을 갖고 오는 것이 장점이라면 **링크드리스트는 원소의 삽입/삭제에 강점**이 있는 자료구조입니다!
- 하지만, 링크드리스트는 삽입/삭제에 강점이 있는 대신에 조회에는 다소 약한 모습을 보일 수 밖에 없어요ㅠㅠ
    - 특정 노드(화물 칸)가 어디있는지 알아내길 위해서는 최초 노드부터 연결고리를 타고 타고 또 타고~
    - 계속 노드들을 일일이 방문하면서 내가 조회하고자 하는 노드가 맞는지를 볼 수 밖에 없어요.
    - KTX 타실때 직원분들이 표 검사하러 첫칸부터 끝칸까지 순차적으로 방문하면서 표 검사하는 느낌!
    - 그래서 시간 복잡도로 나타내면 링크드리스트 조희 성능은 O(N)이에요!

## 링크드리스트 실제 구현해보기

1. 노드를 클래스로 정의해보기(data(노드 값), next(포인터) 정의 필수)
```c++
// 노드를 클래스로 정의 (data(노드 값), next(포인터) 정의 필수)
class Node {
private:
    string data;
    Node* next; // 일단은 원시 포인터로 러프하게!

public:
    Node(string data) {  // data = "흑연"
        this->data = data;
        this->next = nullptr;  // 초기에는 다음 노드가 없으므로 nullptr로 초기화!
    }
    
    // LinkedList 클래스에서 Node의 private 멤버에 접근 가능하도록!
    // friend class에 대해서도 나중에 배우실겁니다! 일단은 그냥 이런가보다 하시면 됩니다!
    friend class LinkedList;
};
```

2. 링크드리스트 자료구조를 클래스로 정의해보기(head 정의 필수)
```c++
class LinkedList {
private:
    Node* head;
    int nodeCount;  // 노드 개수를 추적하기 위한 변수

public:
    LinkedList(string value) {
        this->head = new Node(value);  // head에 시작하는 Node를 연결합니다.
        this->nodeCount = 1;
    }
    // 소멸자 생략!
    // ...
};
```

3. 링크드리스트 끝에 새로운 노드를 추가하는 append 멤버 함수 작성해보기

```c++
// LinkedList 가장 끝에 있는 노드에 새로운 노드를 연결합니다.
void append(string value) {
		Node* curr = this->head;
		while (curr->next != nullptr) {  // curr의 다음이 끝에 갈 때까지 이동합니다.
				curr = curr->next;
		}
		// 이곳으로 왔다는 것은
		// while 루프를 탈출했다는 의미
		// 탈출을 하려면 curr->next != nullptr 이 조건이 지켜지지 않아야 함
		// curr->next == nullptr
		cout << "curr->next: " << curr->next << endl;
		curr->next = new Node(value);
		this->nodeCount++;
}
```

4. 원하는 위치의 노드를 찾아내는 getNode 함수 작성해보기
```c++
// 10개가 있다고 했을 때, index번째 인덱스에 있는 노드를 갖고 오고 싶어요
Node* getNode(int index) {
		// nodeCount를 근거로 index가 유효한지를 판단해야 됩니다!
		if (index < 0 || index >= nodeCount) {
        throw std::out_of_range("유효하지 않은 인덱스!");
    }

		Node* node = this->head;  // 링크드리스트의 Head를 처음 노드로 지정합니다!
		for (int i = 0; i < index; i++) {
				node = node->next;  // 원하는 위치에 당도할 때까지 다음 노드로 이동!
		}
		return node;
}
```

5. 원하는 위치에 새로운 노드를 추가하는 addNode 함수 작성해보기 (getNode 함수 사용)
```c++
void addNode(int index, string value) {
		// ["자갈"] -> ["흑연"] -> ["밀가루"] -> ["우편"]
		// 이때까지는, this->head = ["자갈"]
		// ["짱짱"]이라는 친구를 0번째 index로 추가
		// ["짱짱"] -> ["자갈"]
		// this->head = ["짱짱"]
		// ["짱짱"] -> ["자갈"] -> ["흑연"] -> ["밀가루"] -> ["우편"]
		Node* newNode = new Node(value);  // 일단 새로운 값을 기준으로 새 노드를 만들어요!
        
		if (index == 0) {  // 0번째에 추가를 하고 싶다면!
				newNode->next = this->head;  // 원래 Head였던 노드를 새 노드의 next로 지정해요!
				this->head = newNode;  // 그리고, Head를 새 노드로 바꾸어줍니다!
				this->nodeCount++;
				return;
		}

		// [3] - [4] - [5]에서 [3] - [4] - [6] - [5]로 6을 중간에 넣는다고 할게요!
		// 추가하고 싶은 index의 이전 노드 정보를 갖고옵니다! 여기선 [4] 입니다.
		Node* node = this->getNode(index - 1);
		// 1. 이전 노드([4])의 포인터([5])를 next_node로 임시 저장해요!
		Node* nextNode = node->next;
		// [4] -> [6]
		// 2. 이전 노드([4])의 포인터를 [6]으로 지정합니다!
		node->next = newNode;
		// [6] -> [5]
		// 3. 새로 삽입한 노드([6])의 포인터를 next_node인 [5]로 지정합니다!
		newNode->next = nextNode;
		this->nodeCount++;
}
```

### 지금까지 잠깐 요약하기!

---

- 지금 여기서 말씀드리는 것은 **인덱스를 알고 있다는 전제**입니다!
- 배열
    - 데이터를 빈번하게 접근해야 될 때 배열을 사용해주세요!
- 링크드리스트
    - 데이터를 빈번하게 삽입/삭제해야 될 때 링크드리스트를 써주세요!

# 스택 

- 편의점 사장님이 음료수를 채울때는 끝에서부터 차곡차곡 채웁니다. 손님은 앞에서부터 음료수를 꺼내요.
- 이렇게 **LIFO(Last In First Out)**의 성격을 가진 자료구조를 스택이라고 합니다!
- 이게 넘치면 그 유명한 StackOverflow가 되는 것이죠! 실제로 반복문에서 종료 조건을 지정하지 않거나 잘못된
  종료조건을 지정하여 무한 루프가 발생하면 메모리의 스택영역이 쌓이고 쌓이다 터집니다!
- 그럼 이게 언제 쓰여요?
    - 모바일 앱을 사용하면 뒤로 가기 버튼을 꽤 많이 누르게 되는데 이때 활용하는 자료구조가 스택이에요!
        - 뒤로 가기 버튼을 누르면 직전에 호출했던 스크린을 호출해야되는 특성때문에 그래요!
        - 실제로 React에서도 네비게이션을 할 때 스택 네비게이션 기법을 사용하기도 하죠!
    - 뿐만 아니라, Undo/Redo와 같은 기능도 스택을 이용합니다. **역순의 성질**을 사용해야 될 때 유용해요!
- 스택에서 사용할 수 있는 기능은 대표적인 기능은 다음과 같아요!
    - 탑
        - 스택의 Top(맨 위) 데이터를 보는 것이에요!
    - 푸시
        - 스택에 원소를 삽입하는 행위에요! 원소는 Top에 들어갑니다!
    - 팝
        - 스택의 Top에서 원소를 가져오는 행위에요! 방금 위에서 넣었던 푸시된 원소가 나오게 되겠죠?
- 실제로, 직접 스택을 구현하려면 벡터나 배열을 이용해서 구현할 수 있으니 한 번 도전해보셔도 좋습니다.
  참고할 수 있도록 코드도 아래에 보여드릴게요!

_스택 코드 (벡터 이용)_
```c++
#include <iostream>
#include <vector>
#include <stdexcept>

class Stack {
private:
    std::vector<int> stack; // 벡터를 이용해 관리
public:
    Stack() = default; // 기본 생성자 자동 생성

    // 스택의 맨 위 데이터를 반환
    int top() {
        if (stack.empty()) {
            throw std::out_of_range("스택이 비어있어요!");
        }
        return stack.back();
    }

    // 스택에 데이터를 추가
    void push(int value) {
        stack.push_back(value);
    }

    // 스택의 맨 위 데이터를 제거
    void pop() {
        if (stack.empty()) {
            throw std::underflow_error("스택이 비어서 꺼낼게 없어요!");
        }
        stack.pop_back();
    }
};

int main() {
    Stack s;

    try {
        s.push(10);
        s.push(20);
        s.push(30);

        std::cout << "현재 톱 원소: " << s.top() << std::endl; // 30

        s.pop();
        std::cout << "현재 톱 원소: " << s.top() << std::endl; // 30 -> 20으로 변경됨

        s.pop();
        s.pop();
        s.pop(); // 예외 발생: 스택이 비어 있음
    } catch (const std::exception& e) {
        std::cerr << e.what() << std::endl;
    }
}
```

# 큐
- 큐는 **FIFO(First In First Out)**의 성격을 가진 자료구조에요!
- 처음으로 들어간 친구가 처음으로 빠져나온다. 어떻게 보면 당연하고 합리적인 자료구조입니다.

![스크린샷 2025-06-12 183255](https://github.com/user-attachments/assets/7e14c056-769f-424a-a983-ac7166174733)

- 서버에 접속하기 위해 대기하고 있는 유저들을 모아놓은 큐가 서버 접속 대기열 큐에요!
- 서버에서 어느정도 여유가 생기면 큐에 쌓인 순서대로 유저들을 갖고와서 접속시켜줍니다.
- 주의하실 점은 그림에도 나왔지만 새로고침하면 순번이 뒤로 밀려요!
- 다시 말해, 강제로 큐에서 이탈되면 다시 큐로 들어가야되는데 이렇게 되면 맨 마지막으로 들어가서 순번이 뒤로 밀리게 되는거에요. 그러니, 예매할 때는 인내심을 갖고 기다리셔야 되는 것이죠.

![스크린샷 2025-06-12 183313](https://github.com/user-attachments/assets/228a9892-2711-4778-98c4-619673c4f4c5)

- 공용 프린터를 쓸 때 인쇄 대기열 큐에 걸려서 생각보다 프린트가 빨리 안되었었던 경험 해보셨죠?
- 이 공용 프린터도 자체 인쇄 대기열 큐에 들어온 작업 순서대로 프린트를 합니다. 세상 공평해요!
- 자, 이렇게 합리적이고 솔직한(?) 자료구조인 큐는 어떤 기능이 있을까요?
    - 프론트
        - 큐의 Front(맨 앞) 데이터를 갖고오는 것이에요!
    - 백
        - 큐의 End(맨 뒤) 데이터를 갖고오죠!
    - 푸시
        - 큐에 원소를 삽입하는 행위에요! 원소는 End에 들어갑니다!
    - 팝
        - 큐에서 원소를 제거하는 행위에요! FIFO 자료구조니 Front에 위치한 원소를 제거하겠죠?
- 큐도 마찬가지에요. 스택처럼 벡터나 배열을 이용해서 구현할 수 있으니 한 번 도전해보세요.
  참고할 수 있도록 코드도 아래에 보여드릴게요!

_큐 코드 (벡터 이용)_
```c++
#include <iostream>
#include <vector>
#include <stdexcept>

class Queue {
private:
    std::vector<int> queue;

public:
    Queue() = default;

    // 큐의 첫 번째 원소 반환
    int front() const {
        if (queue.empty()) {
            throw std::out_of_range("큐가 비어있어요!");
        }
        return queue.front();
    }

    // 큐의 마지막 원소 반환
    int back() const {
        if (queue.empty()) {
            throw std::out_of_range("큐가 비어있어요!");
        }
        return queue.back();
    }

    // 큐에 원소 추가
    void push(int value) {
        queue.push_back(value);
    }

    // 큐의 첫 번째 원소 제거
    void pop() {
        if (queue.empty()) {
            throw std::underflow_error("큐가 비어서 꺼낼게 없어요!");
        }
	        queue.erase(queue.begin());
    }
};

int main() {
    Queue q;

    q.push(10);
    q.push(20);
    q.push(30);

    std::cout << "현재 프론트 원소: " << q.front() << std::endl; // 10
    std::cout << "현재 백 원소: " << q.back() << std::endl; // 30

    q.pop();
    std::cout << "현재 프론트 원소: " << q.front() << std::endl; // 10 -> 20으로 변경

    q.pop();
    q.pop();
}
```
