 GameMode, GameState, Interface 역할 및 흐름 정리
이 문서는 언리얼 엔진 프로젝트의 핵심 게임플레이 클래스인 GameMode, GameState, Interface의 역할과 상호작용 방식을 설명합니다.

1. AGM_GameModeBase: 게임의 총괄 감독 🎬
**GameMode**는 게임의 규칙과 흐름을 정의하는 핵심 클래스입니다. 오직 서버에만 존재하며, 클라이언트에는 생성되지 않습니다. 게임의 시작, 웨이브 관리, AI 스폰, 게임 오버 판정 등 모든 핵심 로직이 이곳에 담겨있습니다.

주요 역할 및 코드
게임 규칙 정의: 헤더 파일(.h)에서 UPROPERTY(EditDefaultsOnly)를 사용하여 웨이브 시간, 몬스터 수와 같은 핵심 규칙을 정의합니다. 이렇게 하면 C++ 코드를 수정하지 않고도 언리얼 에디터의 블루프린트에서 쉽게 값을 조정할 수 있습니다.

```
// GM_GameModeBase.h
UCLASS()
class GIVEMETHEMONEY_API AGM_GameModeBase : public AGameModeBase
{
    GENERATED_BODY()

private:
    // 스폰할 적 클래스
    UPROPERTY(EditDefaultsOnly, Category = "Game Rules")
    TSubclassOf<class ACharacter> EnemyClass;

    // 웨이브 지속 시간
    UPROPERTY(EditDefaultsOnly, Category = "Game Rules")
    float WaveDuration;

    // 최대 웨이브 수
    UPROPERTY(EditDefaultsOnly, Category = "Game Rules")
    int32 MaxWaveIndex;

    // 웨이브 성공을 위한 최소 킬 수
    UPROPERTY(EditDefaultsOnly, Category = "Game Rules")
    int32 MinKillCount;
};
```
웨이브 관리: StartWave() 함수는 새 웨이브를 시작하고, 타이머를 설정하여 WaveDuration 시간이 지나면 OnWaveTimeUp()을 호출합니다.

```
// GM_GameModeBase.cpp
void AGM_GameModeBase::StartWave()
{
    AGM_GameStateBase* MyGS = GetGameState<AGM_GameStateBase>();
    if (!MyGS) return;

    MyGS->CurrentWaveIndex++;
    MyGS->WaveDuration = WaveDuration;
    // ... 웨이브 관련 변수 초기화 ...

    SpawnEnemies();

    // 웨이브 시간 타이머 시작
    GetWorldTimerManager().SetTimer(WaveTimerHandle, this, &AGM_GameModeBase::OnWaveTimeUp, MyGS->WaveDuration, false);
}

void AGM_GameModeBase::OnWaveTimeUp()
{
    EndWave(false); // 시간이 다 되면 웨이브 실패 처리
}
```
핵심: GameMode는 게임의 전체적인 흐름을 지휘하는 감독입니다. 모든 규칙과 이벤트 처리는 이곳에서 이루어집니다.

2. AGM_GameStateBase: 모두가 공유하는 게임 정보 게시판 📋
**GameState**는 게임의 '상태'를 저장하고 모든 플레이어에게 **복제(Replicate)**하는 역할을 합니다. GameMode와 달리 서버와 모든 클라이언트에 존재하며, 게임의 현재 상황을 모두가 동일하게 인지하도록 만듭니다.

주요 역할 및 코드
상태 변수 정의 및 복제: 헤더 파일(.h)에서 UPROPERTY(Replicated) 키워드를 사용하여 모든 클라이언트와 공유할 변수들을 선언합니다.

```
// GM_GameStateBase.h
UCLASS()
class GIVEMETHEMONEY_API AGM_GameStateBase : public AGameStateBase
{
    GENERATED_BODY()

public:
    UPROPERTY(Replicated)
    int32 TotalCoin;

    UPROPERTY(Replicated)
    int32 CurrentWaveIndex;

    UPROPERTY(Replicated)
    float WaveDuration;

    UPROPERTY(Replicated)
    int32 CurrentKillCount;
};
```
복제 설정: 소스 파일(.cpp)의 GetLifetimeReplicatedProps 함수 안에 복제할 변수들을 DOREPLIFETIME 매크로로 등록해야 실제로 네트워크를 통해 복제가 이루어집니다.

```
// GM_GameStateBase.cpp
void AGM_GameStateBase::GetLifetimeReplicatedProps(TArray<FLifetimeProperty>& OutLifetimeProps) const
{
    Super::GetLifetimeReplicatedProps(OutLifetimeProps);

    DOREPLIFETIME(AGM_GameStateBase, TotalCoin);
    DOREPLIFETIME(AGM_GameStateBase, CurrentWaveIndex);
    DOREPLIFETIME(AGM_GameStateBase, WaveDuration);
    DOREPLIFETIME(AGM_GameStateBase, CurrentKillCount);
}
```
핵심: GameState는 경기장의 스코어보드와 같습니다. 서버라는 심판(GameMode)이 이 보드의 내용을 갱신하면, 모든 선수(Client)들이 즉시 확인할 수 있습니다.

3. IGM_Interface: 유연한 소통을 위한 약속 🤝
**Interface**는 클래스 간의 '소통 규격'을 정의합니다. 특정 클래스를 직접 참조하지 않고도 정해진 함수를 호출할 수 있게 만들어, 클래스 간의 의존성을 낮추는 **느슨한 연결(Loose Coupling)**을 가능하게 합니다.

주요 역할 및 코드
통신 함수 선언: 헤더 파일(.h)에 UI 업데이트나 게임 오버 신호 같이 다른 객체에게 전달해야 할 함수들을 UFUNCTION으로 선언합니다. BlueprintNativeEvent는 C++와 블루프린트 양쪽에서 구현할 수 있도록 해주는 지정자입니다.

```
// GM_Interface.h
UINTERFACE(MinimalAPI)
class UGM_Interface : public UInterface
{
    GENERATED_BODY()
};

class GIVEMETHEMONEY_API IGM_Interface
{
    GENERATED_BODY()

public:
    UFUNCTION(BlueprintNativeEvent, BlueprintCallable, Category = "GM Interface")
    void UpdateCoin(int32 TotalCoin);

    UFUNCTION(BlueprintNativeEvent, BlueprintCallable, Category = "GM Interface")
    void UpdateWave(int32 CurrentWave, int32 MaxWave);

    UFUNCTION(BlueprintNativeEvent, BlueprintCallable, Category = "GM Interface")
    void OnGameOver(bool bIsVictory);
};
```
인터페이스를 통한 함수 호출: GameMode는 GameState의 최신 정보를 이용해 주기적으로 UI를 업데이트해야 합니다. 이때 GameState 클래스를 직접 참조하는 대신, IGM_Interface를 통해 함수를 호출합니다.

```
// GM_GameModeBase.cpp
void AGM_GameModeBase::UpdateHUD()
{
    AGM_GameStateBase* MyGS = GetGameState<AGM_GameStateBase>();
    if (MyGS && MyGS->GetClass()->ImplementsInterface(UGM_Interface::StaticClass()))
    {
        // GameState의 최신 정보로 인터페이스 함수 호출
        IGM_Interface::Execute_UpdateCoin(MyGS, MyGS->TotalCoin);
        IGM_Interface::Execute_UpdateWave(MyGS, MyGS->CurrentWaveIndex, MaxWaveIndex); // MaxWaveIndex는 GameMode가 알고 있음
        // ... 다른 UI 업데이트 함수 호출 ...
    }
}
```
핵심: Interface는 서로 다른 역할을 가진 객체들이 소통할 수 있도록 정해놓은 표준 통신 규약입니다. 이 규약 덕분에 GameMode는 UI의 세부 구현을 몰라도 "코인 정보를 업데이트해!" 라는 메시지를 안전하게 전달할 수 있습니다.

최종 정리: 상호작용 흐름 🌊
**[서버] GameMode**는 게임 로직에 따라 GameState의 변수들(CurrentWaveIndex, CurrentKillCount 등)을 변경하며 게임을 운영합니다.

**[복제] GameState**는 변경된 변수들을 Replicated 설정을 통해 모든 클라이언트에게 자동으로 전파합니다.

**[서버] GameMode**는 주기적으로 Interface 함수를 호출하여 "UI를 업데이트하라"는 신호를 보냅니다.

[클라이언트] 각 클라이언트는 자신의 GameState에 복제된 최신 데이터를 바탕으로 Interface 신호를 받아 UI를 그립니다.

