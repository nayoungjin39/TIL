# 포인터와 레퍼런스

## 포인터와 배열

### 포인터

-포인터의 모든 연산은 주소값과 관련되어 있습니다.  
아래와 같은 2가지 연산이 가능합니다. 이 2가지 연산은 오직 포인터 변수만 가능합니다.

1. 변수의 주소값을 담을 수 있습니다.

2. 담고 있는 주소값에 해당되는 메모리에 있는 값을 읽거나 수정할 수 있습니다.

![alt text](<스크린샷 2025-05-28 185905.png>)
![alt text](<스크린샷 2025-05-28 190056.png>)
![alt text](<스크린샷 2025-05-28 190406.png>)
![alt text](<스크린샷 2025-05-28 190424-1.png>)
![alt text](<스크린샷 2025-05-28 190509.png>)
![alt text](<스크린샷 2025-05-28 190547.png>)
![alt text](<스크린샷 2025-05-28 190605.png>)

### 포인터 변수의 역참조

- 포인터를 활용하려면 해당 주소에 있는 실제 값을 읽고 수정할 수 있어야 합니다.
포인터는 주소를 다루는 특성 때문에 산술 연산 역시 주소를 제어하는 방식으로 동작합니다.

이를 위해 역참조 연산자(`*`)을 사용합니다.
A라는 포인터 변수가 있다면,
A를 출력하면 주소값이 나오고, `*A`를 출력하면 해당 메모리에 있는 값이 나옵니다.

![alt text](<스크린샷 2025-05-28 191053.png>)
![alt text](<스크린샷 2025-05-28 191110.png>)
![alt text](<스크린샷 2025-05-28 191121.png>)
![alt text](<스크린샷 2025-05-28 191137.png>)
![alt text](<스크린샷 2025-05-28 191214.png>)

## 배열 

- 일단 배열도 변수입니다. 따라서 변수의 기본적인 특성들을 가지고 있습니다.

-다른 점이 있다면, 일반 변수는 값을 하나 담는데, 배열은 값을 여러 개 담을 수 있습니다.
따라서 배열만이 갖는 추가적인 특성이 있습니다.

1. 배열이름은 배열의 시작 주소를 가지고 있습니다.

2. 값을 저장할 수 있습니다.

### 배열과 포인터 차이

- 배열의 이름은 사용될 때 대부분 포인터로 암시적 형 변환되어 동작합니다.
예를 들어, int arr[4];가 있을 때 arr은 배열 전체를 의미합니다.
하지만 식이나 인자로 사용되면 int* 형으로 변환되어 배열의 첫 번째 원소 주소로 해석됩니다.
조금 다른 점이 있다면 이 주소값은 변경할 수 없습니다.

![alt text](<스크린샷 2025-05-28 191520.png>)
![alt text](<스크린샷 2025-05-28 191538.png>)
![alt text](<스크린샷 2025-05-28 191551.png>)

### 포인터 배열과 배열 포인터

1. 포인터 배열은 포인터를 원소로 갖는 배열입니다.
예를 들어, int* ptrArr[4];는 크기가 4이고, 각 원소가 int*인 배열입니다.

2. 배열 포인터는 배열 전체를 가리키는 포인터입니다.
즉 단일 변수가 아닌 배열 통째를 가리키는 변수입니다. 
보통 다차원 배열을 제어할 때 많이 사용합니다.

![alt text](<스크린샷 2025-05-28 191657-1.png>)
![alt text](<스크린샷 2025-05-28 191730.png>)
![alt text](<스크린샷 2025-05-28 191749-1.png>)
![alt text](<스크린샷 2025-05-28 191816.png>)

### 포인트 역산

- 포인터는 주소값을 담습니다. 따라서 산술 연산 시, 
일반적인 수치 연산이 아닌 메모리 주소의 이동으로 해석됩니다.

`ptr + 1`을 하게 되면, `ptr`이 담고 있는 주소값에 대한 연산이 수행됩니다. 
포인터의 타입에 따라, 해당 타입 변수의 크기만큼 담고 있는 주소를 증가시킵니다.

![alt text](<스크린샷 2025-05-28 191931.png>)
![alt text](<스크린샷 2025-05-28 191937.png>)
![alt text](<스크린샷 2025-05-28 191942.png>)

## 레퍼런스

-포인터를 사용하면 주소값을 직접 다루어야 하므로 복잡해질 수 있습니다.
이 문제를 완화하기 위해 C++에서는 변수에 또 다른 이름을 부여하는 ‘레퍼런스’ 문법을 도입했습니다.
레퍼런스는 일반 변수와 거의 동일하게 사용할 수 있습니다. 
그러나 내부적으로는 해당 변수를 직접 가리켜 주는 역할을 합니다.

레퍼런스는 특정 변수에 대한 별명을 부여하는 것입니다.
한 번 특정 변수의 레퍼런스를 연결하면, 이후로는 마치 그 변수가 두 개의 이름을 갖는것과 같습니다.
선언 방법은 데이터형 뒤에 &를 붙이는 겁니다. 
예를 들어, int& ref = var;처럼 사용할 수 있습니다. 이렇게 하면 ref의 값 변경시 var의 값도 변경 됩니다.

![alt text](<스크린샷 2025-05-28 192031.png>)
![alt text](<스크린샷 2025-05-28 192042.png>)

### 포인터와 레퍼런스의 차이점

![alt text](<스크린샷 2025-05-28 192136.png>)

![alt text](<스크린샷 2025-05-28 192152.png>)
![alt text](<스크린샷 2025-05-28 192200.png>)
![alt text](<스크린샷 2025-05-28 192214.png>)
![alt text](<스크린샷 2025-05-28 192222.png>)

### 상수 레퍼런스 (const int& cref = x;)

![alt text](<스크린샷 2025-05-28 192328.png>)
![alt text](<스크린샷 2025-05-28 192333.png>)