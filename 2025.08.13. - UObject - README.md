유오브젝트(UObject)는 **언리얼 엔진(Unreal Engine)의 모든 클래스가 상속받는 기본 클래스(Base Class)**입니다. 쉽게 말해, 언리얼 엔진 세계관의 '만물의 근원'과 같은 핵심적인 존재라고 할 수 있습니다.

언리얼 엔진에서 생성되는 거의 모든 것(캐릭터, 아이템, UI 요소 등)은 결국 UObject로부터 파생됩니다. 어떤 클래스가 UObject를 상속받으면, 언리얼 엔진이 제공하는 강력하고 필수적인 기능들을 자동으로 갖추게 됩니다.

UObject의 핵심 기능
UObject를 상속받는 클래스는 다음과 같은 언리얼 엔진의 핵심 시스템을 활용할 수 있게 됩니다.

가비지 컬렉션 (Garbage Collection) 🗑️: UObject는 더 이상 사용되지 않을 때, 엔진이 자동으로 메모리에서 제거해 줍니다. 이를 통해 개발자가 수동으로 메모리를 관리해야 하는 부담을 크게 줄여주고, 메모리 누수(memory leak)와 같은 심각한 오류를 방지합니다.

리플렉션 (Reflection): 리플렉션은 코드를 실행하는 도중에 클래스의 이름, 속성(변수), 함수 등의 정보를 파악하고 제어할 수 있는 기능입니다. UObject는 이 기능을 내장하고 있어, 언리얼 에디터가 블루프린트에서 C++ 코드를 시각적으로 보여주거나, 값을 편집하고, 네트워크를 통해 데이터를 동기화하는 등의 복잡한 작업을 가능하게 합니다.

직렬화 (Serialization): 객체의 상태(멤버 변수들의 값)를 파일에 저장하거나 네트워크로 전송 가능한 형태로 변환하는 것을 직렬화라고 합니다. UObject는 이 기능을 기본적으로 지원하여, 게임의 상태를 저장하고 불러오거나(Save/Load), 레벨에 배치된 오브젝트의 정보를 저장하는 등의 작업을 처리합니다.

언리얼 에디터와의 통합: UObject를 상속받은 클래스의 속성(UPROPERTY()로 지정된 변수)들은 언리얼 에디터의 '디테일 패널'에 자동으로 노출됩니다. 이를 통해 개발자는 코드를 직접 수정하지 않고도 에디터 상에서 편리하게 객체의 속성을 변경하고 테스트할 수 있습니다.

캐스팅 (Casting): Cast<T>와 같은 언리얼 엔진의 동적 캐스팅 시스템을 사용하여, 객체의 타입을 안전하게 확인하고 변환할 수 있습니다.

UObject와 다른 클래스와의 관계
언리얼 엔진의 주요 클래스들은 모두 UObject를 기반으로 확장됩니다.

액터 (AActor): 레벨에 배치될 수 있는 모든 오브젝트의 기본 클래스입니다. 예를 들어 캐릭터, 무기, 문, 조명 등이 모두 액터입니다. AActor 역시 UObject를 상속받으므로, UObject의 모든 기능을 가지면서 위치, 회전과 같은 3D 월드에서의 존재에 필요한 기능들이 추가된 형태입니다.

액터 컴포넌트 (UActorComponent): 액터에 붙여서 특정 기능을 추가하는 부품과 같은 역할을 합니다. 예를 들어, 움직임을 처리하는 MovementComponent나 3D 모델을 표시하는 StaticMeshComponent 등이 있습니다. 이름에서 알 수 있듯이 이들 역시 UObject의 일종입니다.

결론적으로, UObject는 언리얼 엔진의 객체 시스템을 떠받치는 가장 근본적인 기둥이며, 가비지 컬렉션, 리플렉션 등 엔진의 핵심적인 자동화 및 편의 기능을 제공하는 필수적인 요소입니다.

스테틱 함수에 대한 꼬리 질문
"스테틱 함수 대신 사용할 수 있는 더 좋은 방법은 없나요?"

답변 방향: 싱글턴 패턴(Singleton Pattern)이나 의존성 주입(Dependency Injection)과 같은 디자인 패턴을 소개할 수 있습니다. 전역적인 접근이 필요하면서도 스테틱 함수의 단점을 피하고 싶을 때 사용하는 더 객체지향적인 대안들입니다.

"스테틱 함수가 테스트하기 어렵다는 것은 구체적으로 어떤 의미인가요?"

답변 방향: 스테틱 함수는 특정 클래스에 강하게 결합(Tightly Coupled)되어 있어, 테스트 시에 해당 함수만 따로 떼어내거나 가짜 객체(Mock Object)로 대체하기 어렵습니다. 이로 인해 단위 테스트(Unit Test)가 복잡해지고 의존성이 얽히게 되는 문제점을 예시와 함께 설명할 수 있습니다.

"언제 스테틱 함수를 사용하는 것이 가장 효과적인가요?"

답변 방향: Math.random()이나 Integer.parseInt()처럼, 입력값이 같으면 항상 동일한 결과를 반환하고 객체의 상태와 전혀 무관한 순수 유틸리티 함수들을 만들 때 가장 이상적이라고 설명할 수 있습니다. 즉, 클래스의 '도우미' 또는 '계산기' 같은 역할을 할 때 적합합니다.

UObject (언리얼 엔진)에 대한 꼬리 질문
"가비지 컬렉션이 구체적으로 어떻게 동작하나요? UPROPERTY() 매크로와는 어떤 관계가 있나요?"

답변 방향: 언리얼 엔진의 가비지 컬렉터는 어떤 UObject가 다른 UObject를 참조하고 있는지 추적합니다. UPROPERTY() 매크로를 변수 앞에 붙여주면, 가비지 컬렉터에게 "이 변수는 다른 UObject를 가리키고 있으니, 함부로 지우지 마세요!"라고 알려주는 역할을 합니다. 이 연결이 끊어지면 가비지 컬렉터가 더 이상 필요 없는 객체로 판단하고 메모리에서 제거하는 원리를 설명할 수 있습니다.

"UObject, AActor, UActorComponent의 차이점은 알겠는데, 언제 어떤 것을 써야 할지 예시를 들어주세요."

답변 방향:

UObject: 게임 월드에 직접 나타나진 않지만, 데이터 묶음이 필요할 때 (예: 아이템 정보, 퀘스트 데이터).

AActor: 레벨에 배치되어야 하는 모든 것 (예: 플레이어 캐릭터, NPC, 총, 문).

UActorComponent: 액터에 기능을 추가하는 부품 (예: 캐릭터의 '인벤토리' 기능, 총의 '발사' 기능, 문의 '열고 닫는' 기능).

"모든 클래스를 UObject로 만들면 편할 것 같은데, 왜 그렇게 하지 않나요? 성능 저하가 있나요?"

답변 방향: UObject는 가비지 컬렉션, 리플렉션 등 강력한 기능을 제공하는 대신 일반 C++ 클래스에 비해 약간의 메모리 및 처리 비용(Overhead)이 발생합니다. 아주 가볍고 단순한 데이터 구조가 필요하거나, 언리얼 엔진의 관리 시스템에 포함될 필요가 없는 경우에는 굳이 UObject로 만들지 않고 순수 C++ 클래스나 구조체(struct, 특히 USTRUCT)를 사용하는 것이 성능상 더 효율적이라고 설명할 수 있습니다.
