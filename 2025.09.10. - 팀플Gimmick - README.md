# Bind(바인드)와 Speedup(이속)

## Bind(바인드)
```c++ TBind.h
#pragma once

#include "CoreMinimal.h"
#include "GameFramework/Actor.h"
#include "TBind.generated.h"

class ACharacter;
class USphereComponent;
class UStaticMeshComponent;

UCLASS()
class TEAM02_API ATBind : public AActor
{
	GENERATED_BODY()
	
public:	
	ATBind();

protected:
	virtual void BeginPlay() override;

	void RemoveBind();

private:
	UPROPERTY(VisibleAnywhere, BlueprintReadOnly, meta = (AllowPrivateAccess = "true"))
	TObjectPtr<USphereComponent> SphereComponent;

	UPROPERTY(VisibleAnywhere, BlueprintReadOnly, meta = (AllowPrivateAccess = "true"))
	TObjectPtr<UStaticMeshComponent> MeshComponent;

	UPROPERTY(EditAnywhere, Category = "Gimmick")
	float BindDuration = 2.0f;

	FTimerHandle BindTimerHandle;

	UPROPERTY()
	TArray<TWeakObjectPtr<ACharacter>> AffectedCharacters;
};
```

1. 기본 구조와 상속
전방 선언(Forward Declaration): class ACharacter;, class USphereComponent;와 같은 코드들은 "이런 이름의 클래스들이 존재한다"고 컴파일러에게 미리 알려주는 역할을 합니다. #include로 전체 파일을 불러오는 대신 이렇게 하면 컴파일 속도가 훨씬 빨라져요.

UCLASS(): 이 매크로는 언리얼 엔진의 리플렉션 시스템이 이 클래스를 인식하도록 해줍니다. 덕분에 에디터에서 ATBind를 배치하거나, 블루프린트에서 기능을 확장할 수 있습니다.

class TEAM02_API ATBind : public AActor: ATBind라는 클래스를 정의하며, 게임 월드에 배치될 수 있는 모든 객체의 기본 클래스인 **AActor**를 상속받습니다.

2. 함수 선언
public: ATBind();: 생성자입니다. 이 클래스의 객체가 만들어질 때 가장 먼저 호출되며, 컴포넌트들을 초기화하는 데 사용됩니다.

protected: virtual void BeginPlay() override;: AActor에 이미 존재하는 BeginPlay 함수를 **재정의(Override)**합니다. 이 함수는 액터가 게임에 나타날 때 딱 한 번 실행되는 핵심 로직을 담당합니다.

protected: void RemoveBind();: 속박 효과를 해제하는 역할을 하는 커스텀 함수입니다.

3. 멤버 변수 (속성)
이 클래스가 데이터를 저장하는 공간입니다. UPROPERTY() 매크로는 이 변수들을 언리얼 엔진의 에디터와 블루프린트에서 사용할 수 있게 만들어줍니다.

TObjectPtr<USphereComponent> SphereComponent;: 속박 효과의 범위를 감지하는 USphereComponent에 대한 포인터입니다. **VisibleAnywhere**와 BlueprintReadOnly 덕분에 에디터에서 이 컴포넌트의 위치를 볼 수 있고, 블루프린트에서도 값을 읽을 수 있지만 변경은 불가능합니다.

TObjectPtr<UStaticMeshComponent> MeshComponent;: 액터의 시각적인 형태를 담당하는 UStaticMeshComponent에 대한 포인터입니다.

float BindDuration = 2.0f;: 속박 효과가 지속될 시간을 결정하는 변수입니다. **EditAnywhere**를 통해 디자이너가 코드 수정 없이 에디터에서 쉽게 값을 조절할 수 있습니다.

FTimerHandle BindTimerHandle;: 속박 시간을 관리하는 타이머의 ID입니다. 코드가 이 핸들을 사용해 타이머를 제어하고, 시간이 다 되면 RemoveBind 함수를 호출합니다.

TArray<TWeakObjectPtr<ACharacter>> AffectedCharacters;: 속박된 캐릭터들을 저장하는 배열입니다. **TWeakObjectPtr**를 사용해 캐릭터가 사라지더라도 프로그램이 멈추는(크래시) 현상을 막는 안전한 포인터 역할을 합니다.

요약하면, 이 헤더 파일은 ATBind가 범위 감지, 시각적 표현, 속박 시간, 그리고 영향을 받은 대상을 관리하는 데이터들을 포함할 것이라는 전반적인 설계도를 보여줍니다.

```c++ TBind.cpp
#include "Gimmick/TBind.h"
#include "Components/SphereComponent.h"
#include "Components/StaticMeshComponent.h"
#include "GameFramework/Character.h"
#include "GameFramework/CharacterMovementComponent.h"
#include "TimerManager.h"

// Sets default values
ATBind::ATBind()
{
	PrimaryActorTick.bCanEverTick = false;
	bReplicates = true;
	InitialLifeSpan = 4.0f; // Failsafe

	SphereComponent = CreateDefaultSubobject<USphereComponent>(TEXT("SphereComponent"));
	SetRootComponent(SphereComponent);
	SphereComponent->SetSphereRadius(1000.0f); // Default 10m radius, editable in Blueprint
	SphereComponent->SetCollisionProfileName(FName("OverlapAllDynamic"));

	MeshComponent = CreateDefaultSubobject<UStaticMeshComponent>(TEXT("MeshComponent"));
	MeshComponent->SetupAttachment(GetRootComponent());
	MeshComponent->SetCollisionEnabled(ECollisionEnabled::NoCollision);
}

// Called when the game starts or when spawned
void ATBind::BeginPlay()
{
	Super::BeginPlay();

	if (HasAuthority())
	{
		TArray<AActor*> OverlappedActors;
		SphereComponent->GetOverlappingActors(OverlappedActors, ACharacter::StaticClass());

		for (AActor* OverlappedActor : OverlappedActors)
		{
			ACharacter* Character = Cast<ACharacter>(OverlappedActor);
			if (Character && Character->ActorHasTag(FName("Tagger")))
			{
				UCharacterMovementComponent* MoveComp = Character->GetCharacterMovement();
				if (MoveComp)
				{
					MoveComp->DisableMovement();
					AffectedCharacters.Add(Character);
				}
			}
		}

		if (AffectedCharacters.Num() > 0)
		{
			GetWorld()->GetTimerManager().SetTimer(BindTimerHandle, this, &ATBind::RemoveBind, BindDuration, false);
		}
		else
		{
			Destroy();
		}
	}
}

void ATBind::RemoveBind()
{
	for (TWeakObjectPtr<ACharacter> CharacterPtr : AffectedCharacters)
	{
		if (CharacterPtr.IsValid())
		{
			UCharacterMovementComponent* MoveComp = CharacterPtr->GetCharacterMovement();
			if (MoveComp)
			{
				MoveComp->SetMovementMode(EMovementMode::MOVE_Walking);
			}
		}
	}

	Destroy();
}
```

1. 기본 구조 및 설정
ATBind 클래스는 게임 월드에 배치될 수 있는 **액터(Actor)**입니다. 특정 위치에 생성되어 잠시 존재하다가 사라지는 '일회용' 능력입니다. 이 액터는 다음과 같은 기본 설정을 가집니다.

성능 최적화: 매 프레임마다 복잡한 계산을 할 필요가 없어 불필요한 연산을 막고 성능을 아낍니다.

멀티플레이어 지원: 서버와 클라이언트 모두에게 똑같이 존재하고 작동하도록 설정되어 멀티플레이어 환경에서 문제없이 사용할 수 있습니다.

안전장치: 만약 어떤 이유로 액터가 제때 파괴되지 않더라도, 생성 후 4초가 지나면 자동으로 사라져 메모리를 정리합니다.

이 액터는 구체(Sphere) 형태의 컴포넌트로 능력의 범위를 감지하고, 스태틱 메시(Static Mesh) 컴포넌트로 시각적인 형태를 가집니다. 구체 컴포넌트가 액터의 중심이 되며, 다른 액터와 물리적 충돌은 하지 않지만, 겹쳐지는 상황을 감지할 수 있도록 설정됩니다.

2. 능력 발동 로직: BeginPlay()
액터가 게임 월드에 생성되면, 주요 로직이 실행됩니다. 이 과정은 서버에서만 실행되어, 클라이언트의 조작으로 인한 오류나 해킹을 방지합니다.

범위 내 캐릭터 감지: 액터는 생성되는 즉시 주변의 모든 캐릭터를 찾습니다. 이때 "Tagger"와 같은 특정 태그를 가진 캐릭터만 대상으로 합니다.

움직임 속박: 조건에 맞는 캐릭터를 발견하면, 해당 캐릭터의 이동 컴포넌트를 가져와 움직임을 완전히 멈추게 만듭니다.

타이머 설정: 속박된 캐릭터가 한 명이라도 있다면, 지정된 시간(예: 3초) 후에 속박을 해제하는 함수가 자동으로 호출되도록 타이머를 설정합니다.

즉시 파괴: 만약 범위 안에 속박할 대상이 아무도 없다면, 이 액터는 할 일이 없으므로 즉시 스스로를 파괴합니다.

3. 속박 해제: RemoveBind()
설정된 시간이 지나면 타이머가 이 함수를 호출합니다. 이 함수는 멈췄던 캐릭터들을 다시 움직이게 하는 역할을 합니다.

캐릭터 목록 순회: 속박되었던 캐릭터들의 목록을 하나씩 확인합니다.

이동 복구: 캐릭터가 아직 존재한다면, 그 캐릭터의 이동 모드를 원래대로(걷기) 복구하여 다시 자유롭게 움직일 수 있게 만듭니다.

액터 파괴: 모든 캐릭터의 속박을 해제한 후, ATBind 액터는 제 역할을 다했으므로 스스로를 파괴합니다.

이 코드는 게임 개발에서 지역 기반의 디버프 스킬, 함정, 또는 환경적 요소를 구현할 때 유용하게 활용될 수 있습니다. 예를 들어, 끈끈한 늪지대를 구현하려면 움직임을 완전히 멈추는 대신 이동 속도를 늦추도록 수정할 수도 있습니다.


## Speedup(이속)

```c++ TSpeedup.h
#pragma once

#include "CoreMinimal.h"
#include "GameFramework/Actor.h"
#include "TSpeedup.generated.h"

class ACharacter;
class USphereComponent;

UCLASS()
class TEAM02_API ATSpeedup : public AActor
{
	GENERATED_BODY()
	
public:	
	ATSpeedup();

protected:
	virtual void BeginPlay() override;

	void RemoveSpeedBuff();

private:
	UPROPERTY(VisibleAnywhere, BlueprintReadOnly, meta = (AllowPrivateAccess = "true"))
	TObjectPtr<USphereComponent> SphereComponent;

	UPROPERTY(EditAnywhere, Category = "Gimmick")
	float SpeedMultiplier = 1.5f;

	UPROPERTY(EditAnywhere, Category = "Gimmick")
	float BuffDuration = 3.0f;

	FTimerHandle SpeedBuffTimerHandle;

	UPROPERTY()
	TMap<TWeakObjectPtr<ACharacter>, float> AffectedCharacters;
};
```

1. 기본 구조와 상속
class ACharacter; class USphereComponent;

**전방 선언(Forward Declaration)**입니다. #include로 전체 파일을 가져오는 대신, "이런 이름의 클래스가 존재한다"고 컴파일러에게 미리 알려주는 방식입니다. 포인터 변수를 선언할 때 주로 사용하며, 컴파일 속도를 향상시키는 좋은 습관입니다.

UCLASS()

이 클래스를 언리얼 엔진의 **리플렉션 시스템(Reflection System)**이 인식하도록 만드는 매크로입니다. 덕분에 에디터에서 이 클래스를 보거나 블루프린트로 확장하는 등의 작업이 가능해집니다.

class TEAM02_API ATSpeedup : public AActor

ATSpeedup이라는 이름의 클래스를 정의하며, 언리얼 엔진의 가장 기본적인 월드 배치 가능 객체인 AActor로부터 상속받는다는 의미입니다.

2. 함수 선언
public: ATSpeedup();

**생성자(Constructor)**입니다. 이 클래스의 객체가 생성될 때 호출될 함수를 선언합니다.

protected: virtual void BeginPlay() override;

AActor에 이미 존재하는 BeginPlay 함수를 **재정의(Override)**하겠다고 선언합니다. 게임이 시작되거나 액터가 스폰될 때의 로직이 여기에 들어갑니다.

protected: void RemoveSpeedBuff();

버프 효과를 제거하는 커스텀 함수의 선언입니다.

3. 멤버 변수 (Properties)
여기가 이 액터의 모든 데이터를 저장하는 곳입니다. UPROPERTY 매크로는 변수들을 언리얼 엔진 시스템에 노출시키는 중요한 역할을 합니다.

TObjectPtr<USphereComponent> SphereComponent;

버프 범위를 감지할 **USphereComponent**에 대한 포인터입니다.

VisibleAnywhere: 에디터의 디테일 패널에서 볼 수 있지만, 수정은 할 수 없습니다.

BlueprintReadOnly: 블루프린트에서 이 변수를 읽을 수는 있지만, 쓸 수는 없습니다.

float SpeedMultiplier = 1.5f;

속도를 얼마나 증가시킬지 결정하는 배율 값입니다. 기본값으로 1.5 (50% 증가)를 설정했습니다.

EditAnywhere: 에디터에서 이 값을 쉽게 수정할 수 있습니다. 기획자가 코드를 몰라도 버프 수치를 마음대로 조절할 수 있게 되죠.

Category = "Gimmick": 에디터의 디테일 패널에서 "Gimmick"이라는 카테고리 아래에 이 변수를 정리해 줍니다.

float BuffDuration = 3.0f;

버프가 지속될 **시간(초)**입니다. SpeedMultiplier와 마찬가지로 에디터에서 쉽게 수정할 수 있습니다.

FTimerHandle SpeedBuffTimerHandle;

버프 지속시간을 관리할 타이머의 ID(핸들) 역할을 하는 변수입니다. UPROPERTY가 없으므로 에디터에는 노출되지 않고, 코드 내부에서만 사용됩니다.

TMap<TWeakObjectPtr<ACharacter>, float> AffectedCharacters;

버프의 영향을 받은 캐릭터들과 그들의 원래 속도를 저장하는 TMap(딕셔너리) 변수입니다.

Key: TWeakObjectPtr<ACharacter> - 버프가 적용된 캐릭터입니다. 캐릭터가 죽거나 사라졌을 때 발생할 수 있는 크래시를 방지하기 위해 **안전한 약한 포인터(TWeakObjectPtr)**를 사용합니다.

Value: float - 해당 캐릭터의 버프 적용 전 원래 이동 속도입니다.

요약
이 헤더 파일은 ATSpeedup 액터가 SphereComponent로 범위를 감지하고, 디자이너가 쉽게 수정할 수 있는 SpeedMultiplier와 BuffDuration 값을 가지며, AffectedCharacters라는 목록을 통해 버프 걸린 대상을 기억하고, TimerHandle을 통해 버프 시간을 관리할 것이라는 전체적인 구조를 명확하게 보여주는 설계도 역할을 합니다.

```c++ TSpeedup.cpp
#include "Gimmick/TSpeedup.h"
#include "Components/SphereComponent.h"
#include "GameFramework/Character.h"
#include "GameFramework/CharacterMovementComponent.h"
#include "TimerManager.h"

// Sets default values
ATSpeedup::ATSpeedup()
{
	PrimaryActorTick.bCanEverTick = false;
	bReplicates = true;
	InitialLifeSpan = 5.0f; // Failsafe

	SphereComponent = CreateDefaultSubobject<USphereComponent>(TEXT("SphereComponent"));
	SetRootComponent(SphereComponent);
	SphereComponent->SetSphereRadius(1000.0f); // Default 10m radius
	SphereComponent->SetCollisionProfileName(FName("OverlapAllDynamic"));
}

// Called when the game starts or when spawned
void ATSpeedup::BeginPlay()
{
	Super::BeginPlay();

	if (HasAuthority())
	{
		TArray<AActor*> OverlappedActors;
		SphereComponent->GetOverlappingActors(OverlappedActors, ACharacter::StaticClass());

		for (AActor* OverlappedActor : OverlappedActors)
		{
			ACharacter* Character = Cast<ACharacter>(OverlappedActor);
			if (Character && Character->ActorHasTag(FName("Hider")))
			{
				UCharacterMovementComponent* MoveComp = Character->GetCharacterMovement();
				if (MoveComp)
				{
					AffectedCharacters.Add(Character, MoveComp->MaxWalkSpeed);
					MoveComp->MaxWalkSpeed *= SpeedMultiplier;
				}
			}
		}

		if (AffectedCharacters.Num() > 0)
		{
			GetWorld()->GetTimerManager().SetTimer(SpeedBuffTimerHandle, this, &ATSpeedup::RemoveSpeedBuff, BuffDuration, false);
		}
		else
		{
			Destroy();
		}
	}
}

void ATSpeedup::RemoveSpeedBuff()
{
	for (auto const& Pair : AffectedCharacters)
	{
		TWeakObjectPtr<ACharacter> CharacterPtr = Pair.Key;
		if (CharacterPtr.IsValid())
		{
			UCharacterMovementComponent* MoveComp = CharacterPtr->GetCharacterMovement();
			if (MoveComp)
			{
				MoveComp->MaxWalkSpeed = Pair.Value; // Restore original speed
			}
		}
	}

	Destroy();
}
```

1. 생성자 (ATSpeedup::ATSpeedup) - 기본 설정
액터가 처음 생성될 때 호출되는 부분입니다. 여기서 기본적인 설정을 합니다.

PrimaryActorTick.bCanEverTick = false;

이 액터는 매 프레임마다 무언가를 할 필요가 없으므로 Tick 기능을 비활성화하여 불필요한 성능 저하를 막습니다.

bReplicates = true;

(중요) 멀티플레이어 게임에서 이 액터가 서버에 생성되면 클라이언트에도 복제되도록 설정합니다.

InitialLifeSpan = 5.0f;

혹시 모를 상황에 대비해 5초 뒤에 자동으로 파괴되도록 안전장치를 설정합니다.

SphereComponent = ...

액터의 눈에 보이지 않는 범위(Trigger Volume) 역할을 할 구(Sphere) 컴포넌트를 생성합니다.

SetSphereRadius(1000.0f): 버프 효과가 적용될 범위를 반지름 1000cm (10m)로 설정합니다.

SetCollisionProfileName(FName("OverlapAllDynamic")): 다른 동적 객체들과 겹쳤을 때(Overlap) 감지할 수 있도록 콜리전 설정을 합니다.

2. BeginPlay - 버프 적용 로직
액터가 월드에 스폰(Spawn)되어 게임이 시작될 때 단 한 번 호출됩니다. 여기가 핵심 로직입니다.

if (HasAuthority())

(매우 중요) 이 코드가 서버에서만 실행되도록 보장합니다. 멀티플레이어 게임에서 모든 주요 로직은 서버(Authority)가 가져야 치팅을 막고 동기화를 맞출 수 있습니다.

SphereComponent->GetOverlappingActors(...)

스폰되는 순간, SphereComponent의 범위 안에 이미 들어와 있는 모든 ACharacter 타입의 액터들을 찾아 배열에 담습니다.

for (AActor* OverlappedActor : OverlappedActors)

찾아낸 액터들을 하나씩 순회합니다.

if (Character && Character->ActorHasTag(FName("Hider")))

단순히 캐릭터라고 해서 전부 버프를 주는 게 아니라, "Hider"라는 태그(Tag)를 가진 캐릭터만 골라냅니다. 이렇게 하면 '술래'는 빼고 '숨는 사람'에게만 버프를 주는 등 유연한 게임 디자인이 가능해집니다.

AffectedCharacters.Add(Character, MoveComp->MaxWalkSpeed);

버프를 주기 직전, 캐릭터의 원래 MaxWalkSpeed를 TMap 변수인 AffectedCharacters에 저장합니다. 나중에 버프가 끝났을 때 원래 속도로 되돌리기 위함입니다.

MoveComp->MaxWalkSpeed *= SpeedMultiplier;

캐릭터의 최대 걷기 속도를 SpeedMultiplier 만큼 증가시킵니다. (예: SpeedMultiplier가 1.5라면 속도가 50% 증가)

GetWorld()->GetTimerManager().SetTimer(...)

만약 버프를 받은 캐릭터가 한 명이라도 있다면, 타이머를 설정합니다.

BuffDuration초가 지나면 RemoveSpeedBuff 함수를 호출하라는 예약입니다.

else { Destroy(); }

범위 안에 "Hider" 태그를 가진 캐릭터가 아무도 없으면, 이 액터는 아무 할 일이 없으므로 즉시 스스로를 파괴하여 리소스를 정리합니다.

3. RemoveSpeedBuff - 버프 효과 제거
BeginPlay에서 설정한 타이머가 끝나면 호출되는 함수입니다.

for (auto const& Pair : AffectedCharacters)

속도 버프를 적용했던 캐릭터들의 목록(AffectedCharacters)을 순회합니다.

TWeakObjectPtr<ACharacter> CharacterPtr = Pair.Key;

버프 지속시간 동안 캐릭터가 게임을 나가거나 죽어서 사라졌을 수도 있습니다. TWeakObjectPtr는 이런 경우에도 게임이 꺼지지 않도록 안전하게 캐릭터를 가리키는 스마트 포인터입니다.

if (CharacterPtr.IsValid())

캐릭터가 여전히 월드에 유효한지 확인합니다.

MoveComp->MaxWalkSpeed = Pair.Value;

BeginPlay에서 저장해두었던 원래 속도(Pair.Value)를 캐릭터에게 되돌려줍니다.

Destroy();

모든 캐릭터의 속도를 원상 복구시킨 후, 이 액터는 임무를 다했으므로 스스로 파괴되어 사라집니다.

정리
이 ATSpeedup 액터는 스폰되자마자 주변의 특정("Hider") 캐릭터를 찾아 원래 속도를 저장한 뒤 속도를 빠르게 만들고, 정해진 시간이 지나면 원래 속도로 되돌린 후 스스로 사라지는, 일회성 범위형 버프 아이템의 완벽한 예시입니다. 멀티플레이어 환경을 고려한 서버 측 로직 구현과 안전한 포인터 사용 등, 참고할 만한 좋은 패턴이 많이 담겨 있습니다.

## 다음에 해야할것들

- 코드상 구현은 했지만 실제로 작동하는지 볼려면 경찰과 도둑이 필요.
  캐릭터 2개만들고 구현해보기.
