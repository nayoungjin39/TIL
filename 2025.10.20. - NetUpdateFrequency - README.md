# **NetUpdateFrequency**

- 액터 레플리케이션 빈도의 최대치.
- 1초당 몇 번 레플리케이션을 시도할지 지정한 값.
- 기본값은 100입니다.
- 즉 이론적으로 서버는 `1/100`초 간격으로 레플리케이션을 시도합니다.

## NetUpdateFrequency는 최대치일 뿐 이를 보장하진 않습니다.
서버의 Tick Rate에 따라 레플리케이션이 발생하지만, 서버의 성능에 따라 달라집니다.
서버의 성능이 NetUpdateFrequency보다 낮은 경우, 서버의 성능에 맞춰서 복제됩니다.
그래서 그래픽 기능이 없는 데디케이티드 서버가 더 좋은 성능을 발휘 가능합니다.

##주요 액터의 NetUpdateFrequency 속성 기본값

<img width="240" height="176" alt="스크린샷 2025-10-20 184457" src="https://github.com/user-attachments/assets/dbfc3f3f-4950-4b5f-85f3-63f0402ced90" />

## **네트워크 부하 줄이는 방법**

어떤 액터의 서버의 위치와 클라이언트의 위치가 서로 동기화되어야 한다고 가정해 봅니다.
동기화하는 방법은 두 가지가 있습니다.
아래 두 가지 중 우리는 두 번재 방법을 실습해보고자 합니다.

1️⃣**매 틱마다 액터의 서버 위치를 클라이언트 측으로 레플리케이션해서 동기화.**

2️⃣**액터의 처음 서버 위치와 속도 값을 안다면, 이를 이용해서 예측을 통해 동기화.**

### NetUpdateFrequency 값을 줄이고, 클라이언트에서 보간하기

```
// DXBox.h

...
class DEDICATEDX_API ADXBox : public AActor
{
	...
	
public:	
	...

protected:
	...

	float NetUpdatePeriod;

	float AccDeltaSecondSinceReplicated;

};
```

```
// DXBox.cpp


...

ADXBox::ADXBox()
	...
	, AccDeltaSecondSinceReplicated(0.0f)
{
	...
	
	const static float BoxActorNetUpdateFrequency = 1.f;
	SetNetUpdateFrequency(BoxActorNetUpdateFrequency);
		// 1초에 1번씩 액터 레플리케이션 시도. 즉, 서버 성능이 아무리 좋아도 1초에 1번씩만 레플리케이션함.
	NetUpdatePeriod = 1 / GetNetUpdateFrequency();
		// 주기 = 1 / 주파수
}

void ADXBox::Tick(float DeltaSeconds)
{
	...

	if (HasAuthority() == true)
	{
		...
	}
	else
	{
		if (NetUpdatePeriod < KINDA_SMALL_NUMBER)
		{
			return;
		}

		AccDeltaSecondSinceReplicated += DeltaSeconds;
		const float LerpRatio = FMath::Clamp(AccDeltaSecondSinceReplicated / NetUpdatePeriod, 0.f, 1.f);

		const float NextServerRotationYaw = ServerRotationYaw + RotationSpeed * NetUpdatePeriod;
		
		const float EstimatedClientRotationYaw = FMath::Lerp(ServerRotationYaw, NextServerRotationYaw, LerpRatio);
		SetActorRotation(FRotator(0.f, EstimatedClientRotationYaw, 0.f));
	}
}

void ADXBox::OnRep_ServerRotationYaw()
{
	...

	AccDeltaSecondSinceReplicated = 0.f;
}
```
