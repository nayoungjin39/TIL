- Animation
    - 6.1 AnimInstance 클래스
        - 언리얼 애니메이션 시스템
        - **애님 인스턴스(AnimInstance) 클래스**
          스켈레탈 메시를 소유한 폰의 정보를 받아서
          애님 그래프가 참조할 데이터를 제공함. 블루프린트와 C++로 구현 가능.
          애님 인스턴스의 블루프린트 애셋에는 애님 그래프 기능이 제공됨.
        - **애님 그래프(Anim Graph)**
          업데이트된 변수 값에 따라 지정된 애니메이션을 재생하는 영역.
          상황에 따라 적합한 애니메이션을 체계적으로  재생할 수 있도록 스테이트 머신 기능 제공.
          블루프린트로만 구현 가능.
        
        - [실습] 애님 인스턴스 클래스 생성
            
            새 C++ 클래스 > AnimInstance 부모 클래스 > “SXAnimInstance”
            Path: Animation
            
            ```cpp
            // SXAnimInstance.h
            
            #pragma once
            
            #include "CoreMinimal.h"
            #include "Animation/AnimInstance.h"
            #include "SXAnimInstance.generated.h"
            
            /**
             * 
             */
            UCLASS()
            class SHOOTERX_API USXAnimInstance : public UAnimInstance
            {
            	GENERATED_BODY()
            	
            };
            
            ```
            
            ```cpp
            // SXAnimInstance.cpp
            
            #include "Animation/SXAnimInstance.h"
            
            ```
            
        
        - [실습] 애님 인스턴스 클래스의 속성 초기화
            
            애님 인스턴스 클래스의 속성을 초기화 할 때는 NativeInitializeAnimation() 함수를 활용.
            
            ```cpp
            // SXAnimInstance.h
            
            ...
            
            class ASXCharacterBase;
            class UCharacterMovementComponent;
            
            /**
             *
             */
            UCLASS()
            class SHOOTERX_API USXAnimInstance : public UAnimInstance
            {
            	...
            
            public:
            	virtual void NativeInitializeAnimation() override;
            	
            protected:
            	UPROPERTY(VisibleAnywhere, BlueprintReadOnly)
            	TObjectPtr<ASXCharacterBase> OwnerCharacter;
            
            	UPROPERTY(VisibleAnywhere, BlueprintReadOnly)
            	TObjectPtr<UCharacterMovementComponent> OwnerCharacterMovement;
            
            };
            
            ```
            
            ```cpp
            // SXAnimInstance.cpp
            
            ...
            #include "Character/SXCharacterBase.h"
            
            void USXAnimInstance::NativeInitializeAnimation()
            {
            	APawn* OwnerPawn = TryGetPawnOwner();
            	if (IsValid(OwnerPawn) == true)
            	{
            		OwnerCharacter = Cast<ASXCharacterBase>(OwnerPawn);
            		OwnerCharacterMovement = OwnerCharacter->GetCharacterMovement();
            	}
            		// 왜 TryGetPawnOwner() 함수에 Try-가 붙을까? 
            		// TryGetPawnOwner() 함수와 반대로, 폰에서 애님인스턴스를 가져올 때는 GetAnimInstance() 함수.
            }
            
            ```
            
        
        - [실습] 걷기 구현을 위한 애님 인스턴스 클래스의 속성 업데이트
            
            폰의 속도에 따라 다른 애니메이션을 재생하기 위해서는
            프레임마다 폰의 속력과 애님 인스턴스의 CurrentSpeed 속성을 연동해야함.
              1. 폰의 Tick() 함수에서 애님 인스턴스의 CurrentSpeed 속성 업데이트.
              2. 애님 인스턴스의 Tick() 함수에서 폰의 정보를 가져와서 CurrentSpeed 속성 업데이트.
            개체 지향 프로그래밍에서는 2번의 형태로 개발할 때가 많음. 
            화분에 물을 주는게 아니라 화분이 스스로에게 물을 주는 느낌.
            1번의 경우로 개발하다보면 폰에 너무 많은 코드가 작성될 여지가 있음.
            애님 인스턴스 클래스의 Tick() 함수는 NativeUpdateAnimation() 함수임.
            
            ```cpp
            // SXAnimInstance.h
            
            ...
            class SHOOTERX_API USXAnimInstance : public UAnimInstance
            {
            	...
            
            public:
            	...
            
            	virtual void NativeUpdateAnimation(float DeltaSeconds) override;
            	
            protected:
            	...
            
            	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)
            	FVector Velocity;
            
            	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)
            	float GroundSpeed;
            	
            	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)
            	uint8 bShouldMove : 1;
            
            };
            
            ```
            
            ```cpp
            // SXAnimInstance.cpp
            
            ...
            #include "GameFramework/CharacterMovementComponent.h"
            #include "Kismet/KismetMathLibrary.h"
            
            ...
            
            void USXAnimInstance::NativeUpdateAnimation(float DeltaSeconds)
            {
            	if (IsValid(OwnerCharacter) == true && IsValid(OwnerCharacterMovement) == true)
            	{
            		Velocity = OwnerCharacterMovement->Velocity;
            		GroundSpeed = UKismetMathLibrary::VSizeXY(Velocity);
            		
            		float GroundAcceleration = UKismetMathLibrary::VSizeXY(OwnerCharacterMovement->GetCurrentAcceleration());
            		bool bIsAccelerationNearlyZero = FMath::IsNearlyZero(GroundAcceleration);
            		bShouldMove = (KINDA_SMALL_NUMBER < GroundSpeed) && (bIsAccelerationNearlyZero == false);
            	}
            }
            
            ```
            
        
        - [실습] 애니메이션 블루프린트 애셋 생성
            
            Content Browser > AnimationBlueprint > 새 Animation 애셋 > Animation Blueprint
            스켈레톤으로는 SK_Mannequin, SXAnimInstance 부모 클래스 지정
            “ABP_PlayerCharacter” 생성. ABP_PlayerCharacter 더블클릭.
            My Blueprint > 우측 톱니바퀴 클릭 > Show Inherited Variables 클릭.
            
            Animation Blueprint 클릭 후 나오는 창. Path를 잘보고, LyraResource의 SK_Mannequin을 선택.
            
            Show Inherited Variables를 체크 해야 C++ 클래스에서 정의한 속성들을 VARIABLES에서 볼 수 있어짐.
            
        
        - [실습] Blend Poses by bool 노드와 걷기 구현
            
            <img width="461" height="181" alt="스크린샷 2025-08-28 185718" src="https://github.com/user-attachments/assets/6b5ee2a8-9638-46a0-a29c-281cdf0cd68b" />

            ABP_PlayerCharacter > AnimGraph
            
        
        - 애님 프리뷰 에디터 윈도우
        위에서 만든 애님 그래프가 설계대로 잘 동작하는지 테스트 하는 창.
        ABP_PlayerCharacter > Anim Preview Editor > ShouldMove를 조절해보면 알 수 있음.
   
            ABP_PlayerCharacter > Anim Preview Editor
            
        
        - 걷다가 애니메이션이 멈추는 현상
        애님 프리뷰 에디터를 통해 디버깅 해보면 걷기 애니메이션이 중간에 멈춤.
        
            MM_Unarmed_Idle_Ready와 MM_Unarmed_Jog_Fwd 시퀀스 플레이어 노드 클릭 > Details
            Loop Animation에 체크.
            
        
        - 애니메이션 블루프린트 애셋 연결
        BP_PlayerCharacter > Details > Anim Class를 ABP_PlayerCharacter
        
        - [실습] 블랜드 스페이스를 통한 걷기 개선
            
            ABP_PlayerCharacter > Asset Browser > “BS_MM_Unarmed_Jog_Walk” 검색 후 해당 애셋을
            Anim Graph에 드래그 드랍.
           
            <img width="455" height="324" alt="스크린샷 2025-08-28 185922" src="https://github.com/user-attachments/assets/544b9b22-c7de-49a0-b013-a6f87ca37309" />

            기존 로직은 위로 치우고,
            BS_MM_Unarmed_Jog_Walk를 연결. BS_MM_Unarmed_Jog_Walk 애셋을 찾아서 열어보자.
            
        
    - 6.2 State Machine
        - 언리얼 스테이트 머신 
        언리얼의 스테이트 머신은 스테이트와 룰로 구성되어 있음.
        반복 수행해야하는 동작 단위인 스테이트를 정의하고 스테이트 간의 전환(Rule)을 정의함. 
        머신이 돌아가기 시작하면, 전환에 따라 하나의 스테이트가 지정됨. 그 스테이트 반복 수행.
        
        - 룰(Rule)
        스테이트 머신을 설계 할 때, 하나의 스테이트에서 다른 스테이트로 전환되기 위한 조건.
        스테이트 머신에서는 이를 트랜지션(Transition)이라고 보통 부름.
        언리얼 엔진에서는 트랜지션 대신 룰이라는 용어로 표시.
        
        - [실습] 기존 AnimGraph를 스테이트 머신으로 정리하기 - Idle
            
            ABP_PlayerCharacter > AnimGraph 빈공간 우클릭
            “State Machine” 검색 후 “Locomotion”
            
            <img width="463" height="280" alt="스크린샷 2025-08-28 185950" src="https://github.com/user-attachments/assets/70e536a2-2a21-4775-9be4-38dd6e951323" />

            ABP_PlayerCharacter > AnimGraph
          
            ABP_PlayerCharacter > AnimGraph > Locomotion > 빈공간 우클릭 > Add State 클릭 > “Idle”
                  
            ABP_PlayerCharacter > AnimGraph > Locomotion
            Entry 노드에서 Idle 노드로 실행 흐름 연결.
            
            <img width="367" height="100" alt="스크린샷 2025-08-28 191047" src="https://github.com/user-attachments/assets/896dfd9e-c10e-474b-9a46-ad5cf77fd7e3" />

            ABP_PlayerCharacter > AnimGraph > Locomotion > Idle 스테이트
            
        
        - [실습] 기존 AnimGraph를 스테이트 머신으로 정리하기 - Walk/Run
            
           <img width="359" height="567" alt="스크린샷 2025-08-28 191112" src="https://github.com/user-attachments/assets/e297ed3e-9e97-4a5b-b74c-6b92fb201bf6" />

        - [실습] 점프 구현 전 입력 추가
            
            Content Browser > InputAction > 새 Input 애셋 > Input Action > “IA_Jump”
            SXInputConfig 클래스 파일 수정 후 IC_PlayerCharacter에 IA_Jump 추가.
            IMC_PlayerCharacter에도 IA_Jump를 스페이스 바와 매핑.
            
            ```cpp
            // SXInputConfig.h
            
            ...
            class SHOOTERX_API USXInputConfig : public UDataAsset
            {
            	...
            
            public:
            	...
            
            	UPROPERTY(EditAnywhere, BlueprintReadOnly)
            	TObjectPtr<UInputAction> Jump;
            
            };
            
            ```
            
        
        - [실습] ACharacter 클래스의 Jump() 멤버 함수를 이용한 점프 로직 구현
            
            ```cpp
            // SXPlayerCharacter.cpp
            
            ...
            
            void ASXPlayerCharacter::SetupPlayerInputComponent(UInputComponent* PlayerInputComponent)
            {
            	...
            	if (IsValid(EnhancedInputComponent) == true)
            	{
            		...
            		EnhancedInputComponent->BindAction(PlayerCharacterInputConfig->Jump, ETriggerEvent::Started, this, &ACharacter::Jump);
            		EnhancedInputComponent->BindAction(PlayerCharacterInputConfig->Jump, ETriggerEvent::Completed, this, &ACharacter::StopJumping);
            	}
            }
            
            ...
            
            ```
            
        
        - [실습] 점프 구현을 위한 속성 추가
            
            ```cpp
            // SXAnimInstance.h
            
            ...
            class SHOOTERX_API USXAnimInstance : public UAnimInstance
            {
            	...
            
            public:
            	...
            	
            protected:
            	...
            
            	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)
            	uint8 bIsFalling : 1;
            
            };
            
            ```
            
            ```cpp
            // SXAnimInstance.cpp
            
            ...
            
            void USXAnimInstance::NativeUpdateAnimation(float DeltaSeconds)
            {
            	if (IsValid(OwnerCharacter) == true && IsValid(OwnerCharacterMovement) == true)
            	{
            		...
            		bIsFalling = OwnerCharacterMovement->IsFalling();
            	}
            }
            
            ```
            
        
        - [실습] Cached Pose를 활용한 애님 그래프 정리
            
           <img width="366" height="816" alt="스크린샷 2025-08-28 191137" src="https://github.com/user-attachments/assets/02017f6b-26cd-47ee-a3e1-132507e1bc45" />

        - Automatic Rule Based on Sequence Player in Slate 속성
        true이면 애니메이션 종료 시 자동으로 스테이트가 전환되게끔 함.
        
        - [실습] 점프 애니메이션 구현
 
            <img width="379" height="914" alt="스크린샷 2025-08-28 191209" src="https://github.com/user-attachments/assets/928b5546-5f8e-4755-a3b4-1bb1005b8b2b" />
            <img width="366" height="721" alt="스크린샷 2025-08-28 191223" src="https://github.com/user-attachments/assets/fbf32f91-7c8c-4b75-9fc7-137a0ce58616" />
            <img width="361" height="321" alt="스크린샷 2025-08-28 191229" src="https://github.com/user-attachments/assets/e82a27eb-e96e-4dad-93fd-604b132961e4" />

        - [실습] 착지 상태를 추가하여 개선
            
           <img width="368" height="710" alt="스크린샷 2025-08-28 191316" src="https://github.com/user-attachments/assets/0a43d1b9-c21c-454e-8a67-6ace84c239fc" />
            <img width="361" height="727" alt="스크린샷 2025-08-28 191326" src="https://github.com/user-attachments/assets/fca9b3c4-fc88-419b-9835-416af7c731a6" />

        - [실습] Promote To Shared를 통한 룰 재사용
            
            로직이 중복된 룰들이 많음. 하나의 로직을 나눠 써보자.
            
            <img width="364" height="595" alt="스크린샷 2025-08-28 191414" src="https://github.com/user-attachments/assets/c1e039c8-957a-4f1e-b346-cdb318fce95e" />

        - [실습] State Alias를 활용한 정리
            
           <img width="365" height="908" alt="스크린샷 2025-08-28 191441" src="https://github.com/user-attachments/assets/10b4c13b-2ced-43da-9030-3979485656db" />
          <img width="362" height="767" alt="스크린샷 2025-08-28 191454" src="https://github.com/user-attachments/assets/84a51fa3-30ed-42ed-81f0-7f73c2ce5446" />
          <img width="364" height="675" alt="스크린샷 2025-08-28 191536" src="https://github.com/user-attachments/assets/37843f65-4a5d-40de-96b2-4f2e3b19385c" />
          <img width="365" height="208" alt="스크린샷 2025-08-28 191541" src="https://github.com/user-attachments/assets/a2590bb0-a90a-44db-9087-e804a511862e" />

    - 6.3 무기 장착
        - [실습] 무기 스테틱 매시에 Complex Collision 추가하기
            
            스태틱 매시 모양 그대로 Collision에 사용하고 싶다면 이 과정이 필요함.
            
            Content Browser > Content > LyraResource > Weapons > Rifle > Mesh
            SM_Rifle 우클릭 > Duplicate > “SM_Rifle_Complex_Collision”
            SM_Rifle_Complex_Collision > Window > Convex Decomposition 클릭 > Apply 클릭
            
            <img width="365" height="392" alt="스크린샷 2025-08-28 191753" src="https://github.com/user-attachments/assets/311aa6fe-f238-45c3-97ba-29dcca2aca4a" />
       
        - [실습] 습득 컴포넌트 클래스 생성
            
            새 C++ 클래스 > StaticMeshComponent 부모 클래스 > “SXPickupComponent”
            Path: Component
            
            ```cpp
            // SXPickupComponent.h
            
            #pragma once
            
            #include "CoreMinimal.h"
            #include "Components/StaticMeshComponent.h"
            #include "SXPickupComponent.generated.h"
            
            class ASXPlayerCharacter;
            
            DECLARE_DYNAMIC_MULTICAST_DELEGATE_OneParam(FOnPickUp, ASXPlayerCharacter*, InPickUpCharacter);
            
            UCLASS(Blueprintable, BlueprintType, ClassGroup = (Custom), meta = (BlueprintSpawnableComponent))
            class SHOOTERX_API USXPickupComponent : public UStaticMeshComponent
            {
            	GENERATED_BODY()
            
            public:	
            	USXPickupComponent();
            
            protected:
            	virtual void BeginPlay() override;
            
            	UFUNCTION()
            	void HandleOnComponentBeginOverlap(UPrimitiveComponent* OverlappedComponent, AActor* OtherActor, UPrimitiveComponent* OtherComp, int32 OtherBodyIndex, bool bFromSweep, const FHitResult& SweepResult);
            
            public:
            	UPROPERTY(BlueprintAssignable)
            	FOnPickUp OnPickUp;
            
            };
            
            ```
            
            ```cpp
            // SXPickupComponent.cpp
            
            #include "Component/SXPickupComponent.h"
            #include "Character/SXPlayerCharacter.h"
            
            USXPickupComponent::USXPickupComponent()
            {
            	PrimaryComponentTick.bCanEverTick = false;
            }
            
            void USXPickupComponent::BeginPlay()
            {
            	Super::BeginPlay();
            
            	OnComponentBeginOverlap.AddDynamic(this, &ThisClass::HandleOnComponentBeginOverlap);
            }
            
            void USXPickupComponent::HandleOnComponentBeginOverlap(UPrimitiveComponent* OverlappedComponent, AActor* OtherActor, UPrimitiveComponent* OtherComp, int32 OtherBodyIndex, bool bFromSweep, const FHitResult& SweepResult)
            {
            	ASXPlayerCharacter* OverlappedCharacter = Cast<ASXPlayerCharacter>(OtherActor);
            	if (IsValid(OverlappedCharacter) == true)
            	{
            		OnPickUp.Broadcast(OverlappedCharacter);
            
            		OnComponentBeginOverlap.RemoveAll(this);
            	}
            }
            
            ```
            
        
        - [실습] 무기 액터 클래스 생성
            
            새 C++ 클래스 > Actor 부모 클래스 > “SXWeapon”
            Path: Item
            
            ```cpp
            // SXWeapon.h
            
            #pragma once
            
            #include "CoreMinimal.h"
            #include "GameFramework/Actor.h"
            #include "SXWeapon.generated.h"
            
            class ASXPlayerCharacter;
            class USXPickupComponent;
            
            UCLASS()
            class SHOOTERX_API ASXWeapon : public AActor
            {
            	GENERATED_BODY()
            	
            public:	
            	ASXWeapon();
            	
            	USXPickupComponent* GetPickupComponent() const { return PickupComponent; }
            
            protected:
            	virtual void BeginPlay() override;
            
            	UFUNCTION()
            	void HandleOnPickUp(ASXPlayerCharacter* InPickUpCharacter);
            
            protected:
            	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)
            	TObjectPtr<USXPickupComponent> PickupComponent;
            
            };
            
            ```
            
            ```cpp
            // SXWeapon.cpp
            
            #include "Item/SXWeapon.h"
            #include "Component/SXPickupComponent.h"
            #include "Character/SXPlayerCharacter.h"
            
            ASXWeapon::ASXWeapon()
            {
            	PrimaryActorTick.bCanEverTick = false;
            
            	PickupComponent = CreateDefaultSubobject<USXPickupComponent>(TEXT("PickupComponent"));
            	SetRootComponent(PickupComponent);
            }
            
            void ASXWeapon::BeginPlay()
            {
            	Super::BeginPlay();
            	
            	PickupComponent->OnPickUp.AddDynamic(this, &ThisClass::HandleOnPickUp);
            }
            
            void ASXWeapon::HandleOnPickUp(ASXPlayerCharacter* InPickUpCharacter)
            {
            	UE_LOG(LogTemp, Error, TEXT("HandleOnPickUp(%s)"), *InPickUpCharacter->GetName());
            }
            
            ```
            
        
        - [실습] 무기 액터 블루프린트 애셋 생성
            
            Content Browser > ShooterX > Blueprint 우클릭 > 새 폴더 “Item”
            Item > 새 Blueprint 애셋 > SXWeapon 부모 클래스 > “BP_Rifle”
            BP_Rifle > Details > 아래 그림을 참고하여 지정.
                
        <img width="346" height="835" alt="스크린샷 2025-08-28 191823" src="https://github.com/user-attachments/assets/0c37ce81-1dbc-4e56-9732-eeb1ee5d09e2" />

        - [실습] 무기 액터를 붙히기 위한 소켓 생성
            
            Content Browser > LyraResource > Characters > Heroes > Mannequin > Mesh
            SK_Mannequin > Skeleton Tree에 “hand_r” 검색 후 우클릭 > Add Socket 클릭.
            
            <img width="357" height="868" alt="스크린샷 2025-08-28 191843" src="https://github.com/user-attachments/assets/2040a540-d57b-4298-b995-924d1e2af5a0" />
    
        - [실습] 무기 습득시 캐릭터에 부착 구현
            
            ```cpp
            // SXWeapon.cpp
            
            ...
            
            void ASXWeapon::HandleOnPickUp(ASXPlayerCharacter* InPickUpCharacter)
            {
            	//UE_LOG(LogTemp, Error, TEXT("HandleOnPickUp(%s)"), *InPickUpCharacter->GetName());
            
            	if (IsValid(InPickUpCharacter) == false)
            	{
            		return;
            	}
            
            	FAttachmentTransformRules AttachmentRules(EAttachmentRule::SnapToTarget, true);
            	AttachToComponent(InPickUpCharacter->GetMesh(), AttachmentRules, FName(TEXT("hand_rSocket")));
            	PickupComponent->SetCollisionEnabled(ECollisionEnabled::NoCollision);
            	PickupComponent->SetSimulatePhysics(false);
            }
            
            ```
            
        
        - 정오표
            
            ```cpp
            void ASXWeapon::HandleOnPickUp(ASXPlayerCharacter* InPickUpCharacter)
            {
            	...
            
            	FAttachmentTransformRules AttachmentRules(EAttachmentRule::SnapToTarget, true);
            	AttachToComponent(InPickUpCharacter->GetMesh(), AttachmentRules, FName(TEXT("hand_rSocket")));
            	// PickupComponent->SetCollisionEnabled(ECollisionEnabled::NoCollision); PIE 종료시 경고 유발.
            	SetActorEnableCollision(false);
            	PickupComponent->SetSimulatePhysics(false);	
            }
            ```
            
        
    - 6.4 AnimMontage
        - 애니메이션 몽타주
        스테이트 머신의 확장 없이 특정 상황에서 원하는 애니메이션을 재생 시키는 기능.
        본래 몽타주는 이미 촬영된 화면을 잘라내서 새로운 장면을 만드는 기법.
        애니메이션 몽타주도 유사함. 여러 애니메이션 클립들의 일부를 떼고 붙여서
        새로운 애니메이션을 생성하는 기법.
        이때 섹션 단위로 애니메이션들을 자르고 붙이는 작업을 하기도 함.
        
        - [실습] 애니메이션 몽타주 애셋 생성
            
            Content Browser > ShooterX > Animation 우클릭 > 새 폴더 “AnimationMontage”
            AnimationMontage 폴더 > 새 Animation 애셋 > Animation Montage
            SK_Mannequin 지정. “AM_Attack_Melee” 생성.
            
            AM_Attack_Melee > Window > Anim Slot Manager > Add Group > “SXGroup” 추가
            SXGroup 클릭 후 Add Slot > “SXUpperBody” 추가. 아래 그림 참고.
            몽타주 섹션 > DefaultGroup.DefaultSlot 우측 역삼각형 클릭
            Slot Name > SXGroup.SXUpperBody로 지정.
            
            Asset Browser > MM_Rifle_Melee를 SXGroup.SXUpperBody 타임 라인에 드래그 드랍.
            플레이 버튼을 누르면 결과를 볼 수 있음.
            
            타임 테이블 상단에 보면 Default 라는 이름의 섹션이 주어짐.
            Default 섹션 클릭 > Details > Section Name에 “Attack01” 작성.
            
            <img width="359" height="605" alt="스크린샷 2025-08-28 191928" src="https://github.com/user-attachments/assets/975d5f13-51b8-4907-b2c2-9dbcbd0decb8" />
          <img width="362" height="757" alt="스크린샷 2025-08-28 191938" src="https://github.com/user-attachments/assets/a0de7ba3-f268-48f3-a744-089e8ae8ae19" />

        - [실습] 애니메이션 블루프린트에 애님몽타주가 재생될 슬롯 추가
            
            애님 몽타주가 재생되어야 할 로직 순서를 정해줘야함.
            적절한 위치에 Slot을 배치해줌으로써 가능.
            ABP_PlayerCharacter > AnimGraph > 우클릭 “Slot DefaultSlot”이라고 검색 후 엔터.
            해당 슬롯을 아래와 같이 수정 후 배치.
            
         <img width="365" height="742" alt="스크린샷 2025-08-28 192023" src="https://github.com/user-attachments/assets/6740f66b-3ac4-4f5b-9669-a65efad8c98e" />

        - [실습] 밀리 공격용 단축키 추가
            
            Content Browser > InputAction > 새 Input 애셋 > Input Action > “IA_Attack_Melee”
            SXInputConfig 클래스를 아래와 같이 수정. IC_PlayerCharacter에 IA_Attack_Melee 추가.
            IMC_PlayerCharacter에 IA_Attack_Melee 추가. IA_Attack_Melee는 우클릭 키에 매핑.
            
            ```cpp
            // SXInputConfig.h
            
            ...
            class SHOOTERX_API USXInputConfig : public UDataAsset
            {
            	...
            
            public:
            	...
            
            	UPROPERTY(EditAnywhere, BlueprintReadOnly)
            	TObjectPtr<UInputAction> AttackMelee;
            
            };
            
            ```
            
            ```cpp
            // SXPlayerCharacter.h
            
            ...
            class SHOOTERX_API ASXPlayerCharacter : public ASXCharacterBase
            {
            	...
            
            #pragma region Input
            
            private:
            	...
            
            	void InputAttackMelee(const FInputActionValue& InValue);
            
            protected:
            	...
            
            #pragma endregion
            
            };
            
            ```
            
            ```cpp
            // SXPlayerCharacter.cpp
            
            ...
            
            void ASXPlayerCharacter::SetupPlayerInputComponent(UInputComponent* PlayerInputComponent)
            {
            	...
            	if (IsValid(EnhancedInputComponent) == true)
            	{
            		...
            		EnhancedInputComponent->BindAction(PlayerCharacterInputConfig->AttackMelee, ETriggerEvent::Started, this, &ThisClass::InputAttackMelee);
            	}
            }
            
            ...
            
            void ASXPlayerCharacter::InputAttackMelee(const FInputActionValue& InValue)
            {
            	UKismetSystemLibrary::PrintString(this, FString::Printf(TEXT("Attack()")));
            }
            
            ```
            
        
        - [실습] 밀리 공격 로직 작성
            
            ```cpp
            // SXCharacterBase.h
            
            ...
            
            class UAnimMontage;
            
            UCLASS()
            class SHOOTERX_API ASXCharacterBase : public ACharacter
            {
            	...
            
            #pragma region Attack
            
            protected:
            	UPROPERTY(EditAnywhere, BlueprintReadOnly)
            	TObjectPtr<UAnimMontage> AttackMeleeMontage;
            
            #pragma endregion
            
            };
            
            ```
            
            ```cpp
            // SXPlayerCharacter.cpp
            
            ...
            #include "Animation/SXAnimInstance.h"
            
            ...
            
            void ASXPlayerCharacter::InputAttackMelee(const FInputActionValue& InValue)
            {
            	//UKismetSystemLibrary::PrintString(this, FString::Printf(TEXT("Attack()")));
            
            	if (GetCharacterMovement()->IsFalling() == true)
            	{
            		return;
            	}
            
            	USXAnimInstance* AnimInstance = Cast<USXAnimInstance>(GetMesh()->GetAnimInstance());
            	if (IsValid(AnimInstance) == true && IsValid(AttackMeleeMontage) == true && AnimInstance->Montage_IsPlaying(AttackMeleeMontage) == false)
            	{
            		AnimInstance->Montage_Play(AttackMeleeMontage);
            	}
            }
            
            ```
            
            BP_PlayerCharacter > Details > “AttackMeleeMontage” 검색 후 애님몽타주 연결.
            
        
    - 6.5 Animation Notify
        - 애니메이션 노티파이
        애니메이션 재생 동안에 원하는 타이밍에 애님 인스턴스의 멤버 함수를 호출하는 기능
        애니메이션 노티파이는 애니메이션 시퀀스와 애니메이션 몽타주 모두 사용가능.
        
        - 애니메이션 노티파이의 활용
        만약 공격을 구현한다면, 특정 애니메이션 프레임에 충돌 검사 로직을 처리해야함.
        이때 애니메이션 노티파이를 활용하면 구현 가능.
        
        - [실습] 애니메이션 노티파이
            
            AM_Attack_Melee > Notifies 1번 타임 라인 빈공간에 우클릭 
            Add Notify > New Notify 클릭 > “CheckHit”
            그럼 해당 프레임에 USXAnimInstance::AnimNotify_CheckHit() 멤버 함수가 자동으로 호출됨.
            애님 인스턴스 클래스에 “AnimNotify_노티파이명()”의 멤버 함수가 선언되어야함.
            또한 UHT에 의해 언리얼 시스템이 찾을 수 있도록 UFUNCTION() 매크로가 붙어야함.
            
            <img width="365" height="379" alt="스크린샷 2025-08-28 192135" src="https://github.com/user-attachments/assets/1c3efc2a-3079-430f-af28-1b597be13930" />

            ```cpp
            // SXAnimInstance.h
            
            ...
            
            DECLARE_DYNAMIC_MULTICAST_DELEGATE(FOnCheckHit);
            
            /**
             * 
             */
            UCLASS()
            class SHOOTERX_API USXAnimInstance : public UAnimInstance
            {
            	...
            
            public:
            	...
            
            private:
            	UFUNCTION()
            	void AnimNotify_CheckHit();
            
            public:
            	FOnCheckHit OnCheckHit;
            	
            protected:
            	...
            
            };
            
            ```
            
            ```cpp
            // SXAnimInstance.cpp
            
            ...
            
            void USXAnimInstance::AnimNotify_CheckHit()
            {
            	if (OnCheckHit.IsBound() == true)
            	{
            		OnCheckHit.Broadcast();
            	}
            }
            
            ```
            
            ```cpp
            // SXCharacterBase.h
            
            ...
            class SHOOTERX_API ASXCharacterBase : public ACharacter
            {
            	GENERATED_BODY()
            
            //public:
            //	ASXCharacterBase();
            
            #pragma region Override ACharacter
            
            public:
            	ASXCharacterBase();
            
            protected:
            	virtual void BeginPlay() override;
            
            #pragma endregion
            
            #pragma region Attack
            
            public:
            	UFUNCTION()
            	void HandleOnCheckHit();
            
            protected:
            	...
            
            #pragma endregion
            
            };
            
            ```
            
            ```cpp
            // SXCharacterBase.cpp
            
            ...
            #include "Animation/SXAnimInstance.h"
            #include "Kismet/KismetSystemLibrary.h"
            
            ...
            
            void ASXCharacterBase::BeginPlay()
            {
            	Super::BeginPlay();
            	
            	USXAnimInstance* AnimInstance = Cast<USXAnimInstance>(GetMesh()->GetAnimInstance());
            	if (IsValid(AnimInstance) == true)
            	{
            		AnimInstance->OnCheckHit.AddDynamic(this, &ThisClass::HandleOnCheckHit);
            	}
            }
            
            void ASXCharacterBase::HandleOnCheckHit()
            {
            	UKismetSystemLibrary::PrintString(this, TEXT("HandleOnCheckHit()"));
            }
            
            ```
            
        
        - [실습] 애님노티파이 C++ 클래스 생성 및 교체
            
            새 C++ 클래스 > AnimNotify 부모 클래스 > “AN_CheckHit”
            Path: Animation
            
            ```cpp
            // AN_CheckHit.h
            
            #pragma once
            
            #include "CoreMinimal.h"
            #include "Animation/AnimNotifies/AnimNotify.h"
            #include "AN_CheckHit.generated.h"
            
            /**
             * 
             */
            UCLASS()
            class SHOOTERX_API UAN_CheckHit : public UAnimNotify
            {
            	GENERATED_BODY()
            
            private:
            	virtual void Notify(USkeletalMeshComponent* MeshComp, UAnimSequenceBase* Animation, const FAnimNotifyEventReference& EventReference) override;
            
            };
            
            ```
            
            ```cpp
            // AN_CheckHit.cpp
            
            #include "Animation/AN_CheckHit.h"
            
            #include "Character/SXCharacterBase.h"
            
            void UAN_CheckHit::Notify(USkeletalMeshComponent* MeshComp, UAnimSequenceBase* Animation,
            	const FAnimNotifyEventReference& EventReference)
            {
            	Super::Notify(MeshComp, Animation, EventReference);
            
            	if (IsValid(MeshComp) == true)
            	{
            		ASXCharacterBase* AttackingCharacter = Cast<ASXCharacterBase>(MeshComp->GetOwner());
            		if (IsValid(AttackingCharacter) == true)
            		{
            			AttackingCharacter->HandleOnCheckHit();
            		}
            	}
            }
            
            ```
            
            ```cpp
            // SXCharacterBase.cpp
            
            ...
            
            void ASXCharacterBase::BeginPlay()
            {
            	...
            	
            	//USXAnimInstance* AnimInstance = Cast<USXAnimInstance>(GetMesh()->GetAnimInstance());
            	//if (IsValid(AnimInstance) == true)
            	//{
            	//	AnimInstance->OnCheckHit.AddDynamic(this, &ThisClass::HandleOnCheckHit);
            	//}
            }
            
            ...
            
            ```
            
            <img width="367" height="110" alt="스크린샷 2025-08-28 192207" src="https://github.com/user-attachments/assets/1fd23c7a-7179-45ee-9404-246784663c58" />

        - [실습] 연속공격 구현 준비
            
            각 공격 동작을 섹션으로 분리한 후 일정 시간 내에 공격 명령을 내리면
            다음 공격 동작으로 이동하는 연속 공격을 구현해보고자 함.
            
            <img width="365" height="808" alt="스크린샷 2025-08-28 192228" src="https://github.com/user-attachments/assets/b35fdb13-001e-48b6-9d43-2cb34d65d144" />

        - [실습] 공격 입력 확인을 위한 애님 노티파이 C++ 클래스 생성
            
            새 C++ 클래스 > AnimNotify 부모 클래스 > “AN_CheckInput_Attack”
            Path: Animation
            
            ```cpp
            // SXCharacterBase.h
            
            ...
            class SHOOTERX_API ASXCharacterBase : public ACharacter
            {
            	...
            
            #pragma region Attack
            
            public:
            	...
            
            	UFUNCTION()
            	void HandleOnCheckInputAttack();
            
            protected:
            	...
            	
            #pragma endregion
            
            };
            
            ```
            
            ```cpp
            // SXCharacterBase.cpp
            
            ...
            
            void ASXCharacterBase::HandleOnCheckInputAttack()
            {
            	UKismetSystemLibrary::PrintString(this, TEXT("HandleOnCheckInputAttack()"));
            }
            
            ```
            
            ```cpp
            // AN_CheckInput_Attack.h
            
            #pragma once
            
            #include "CoreMinimal.h"
            #include "Animation/AnimNotifies/AnimNotify.h"
            #include "AN_CheckInput_Attack.generated.h"
            
            /**
             * 
             */
            UCLASS()
            class SHOOTERX_API UAN_CheckInput_Attack : public UAnimNotify
            {
            	GENERATED_BODY()
            
            private:
            	virtual void Notify(USkeletalMeshComponent* MeshComp, UAnimSequenceBase* Animation, const FAnimNotifyEventReference& EventReference) override;
            
            };
            
            ```
            
            ```cpp
            // AN_CheckInput_Attack.cpp
            
            #include "Animation/AN_CheckInput_Attack.h"
            
            #include "Character/SXCharacterBase.h"
            
            void UAN_CheckInput_Attack::Notify(USkeletalMeshComponent* MeshComp, UAnimSequenceBase* Animation, const FAnimNotifyEventReference& EventReference)
            {
            	Super::Notify(MeshComp, Animation, EventReference);
            
            	if (IsValid(MeshComp) == true)
            	{
            		ASXCharacterBase* AttackingCharacter = Cast<ASXCharacterBase>(MeshComp->GetOwner());
            		if (IsValid(AttackingCharacter) == true)
            		{
            			AttackingCharacter->HandleOnCheckInputAttack();
            		}
            	}
            }
            
            ```
            
        - [실습] 노티파이 트랙 추가
            
            다음 공격 동작을 재생 할지 말지도 체크해야함.
            Notifies 우측 역삼각형 클릭 > Add Notify Track 클릭 > 2
            Notifies 2번 타임 라인 우클릭 > Add Notify > New Notify > “AN_CheckInput_Attack”
            이때 주의할 점은 CheckAttackInput 노티파이가 한 섹션의 마지막에 가까울수록
            노티파이 실행 후 다음 몽타주 섹션 실행을 하기전에 OnMontageEnded()가 실행될 수 있음.
            또한 마지막 공격에서는 CheckAttackInput 노티파이가 필요없음에 주의.
            
            <img width="361" height="524" alt="스크린샷 2025-08-28 192302" src="https://github.com/user-attachments/assets/0fc1b1dd-7429-45b9-a3c6-c30321d42606" />

        - [실습] Branching Point
            
            애니메이션 노티파이를 설정한 후에는 해당 프레임에 즉각적으로 반응하는 방식인
            Branching Point 값으로 Montage Tick Type 속성을 변경하는 것이 좋음.
            물론 이펙트라던가, 사운드는 한번씩 씹혀도 상관없으니 Queued로 해도됨.
            그런데 지금과 같이 판정을 해야하는 때에는 Branching Point로 지정.
            
      <img width="362" height="363" alt="스크린샷 2025-08-28 192314" src="https://github.com/user-attachments/assets/eba2987f-7a49-4a55-b14f-fbc0472c355d" />
       
        - [실습] 연속 공격 로직 작성
            
            ```cpp
            // SXCharacterBase.h
            
            ...
            class SHOOTERX_API ASXCharacterBase : public ACharacter
            {
            	...
            
            #pragma region Attack
            
            public:
            	...
            
            	virtual void BeginAttack();
            
            	UFUNCTION()
            	virtual void EndAttack(UAnimMontage* InMontage, bool bInterruped);
            
            protected:
            	...
            
            	FString AttackAnimMontageSectionPrefix = FString(TEXT("Attack"));
            
            	int32 MaxComboCount = 3;
            
            	int32 CurrentComboCount = 0;
            
            	bool bIsNowAttacking = false;
            
            	bool bIsAttackKeyPressed = false;
            
            	FOnMontageEnded OnMeleeAttackMontageEndedDelegate;
            
            #pragma endregion
            
            };
            
            ```
            
            ```cpp
            // SXCharacterBase.cpp
            
            ...
            
            void ASXCharacterBase::HandleOnCheckInputAttack()
            {
            	//UKismetSystemLibrary::PrintString(this, TEXT("HandleOnCheckInputAttack()"));
            
            	USXAnimInstance* AnimInstance = Cast<USXAnimInstance>(GetMesh()->GetAnimInstance());
            	checkf(IsValid(AnimInstance) == true, TEXT("Invalid AnimInstance"));
            
            	if (bIsAttackKeyPressed == true)
            	{
            		CurrentComboCount = FMath::Clamp(CurrentComboCount + 1, 1, MaxComboCount);
            
            		FName NextSectionName = *FString::Printf(TEXT("%s%02d"), *AttackAnimMontageSectionPrefix, CurrentComboCount);
            		AnimInstance->Montage_JumpToSection(NextSectionName, AttackMeleeMontage);
            		bIsAttackKeyPressed = false;
            	}
            }
            
            void ASXCharacterBase::BeginAttack()
            {
            	USXAnimInstance* AnimInstance = Cast<USXAnimInstance>(GetMesh()->GetAnimInstance());
            	checkf(IsValid(AnimInstance) == true, TEXT("Invalid AnimInstance"));
            
            	GetCharacterMovement()->SetMovementMode(EMovementMode::MOVE_None);
            	bIsNowAttacking = true;
            	if (IsValid(AnimInstance) == true && IsValid(AttackMeleeMontage) == true && AnimInstance->Montage_IsPlaying(AttackMeleeMontage) == false)
            	{
            		AnimInstance->Montage_Play(AttackMeleeMontage);
            	}
            
            	CurrentComboCount = 1;
            
            	if (OnMeleeAttackMontageEndedDelegate.IsBound() == false)
            	{
            		OnMeleeAttackMontageEndedDelegate.BindUObject(this, &ThisClass::EndAttack);
            		AnimInstance->Montage_SetEndDelegate(OnMeleeAttackMontageEndedDelegate, AttackMeleeMontage);
            	}
            }
            
            void ASXCharacterBase::EndAttack(UAnimMontage* InMontage, bool bInterruped)
            {
            	ensureMsgf(CurrentComboCount != 0, TEXT("CurrentComboCount == 0"));
            
            	CurrentComboCount = 0;
            	bIsAttackKeyPressed = false;
            	bIsNowAttacking = false;
            	GetCharacterMovement()->SetMovementMode(EMovementMode::MOVE_Walking);
            
            	if (OnMeleeAttackMontageEndedDelegate.IsBound() == true)
            	{
            		OnMeleeAttackMontageEndedDelegate.Unbind();
            	}
            }
            
            ```
            
            ```cpp
            // SXPlayerCharacter.cpp
            
            ...
            
            void ASXPlayerCharacter::InputAttackMelee(const FInputActionValue& InValue)
            {
            	if (GetCharacterMovement()->IsFalling() == true)
            	{
            		return;
            	}
            	
            	//USXAnimInstance* AnimInstance = Cast<USXAnimInstance>(GetMesh()->GetAnimInstance());
            	//if (IsValid(AnimInstance) == true && IsValid(AttackMeleeMontage) == true && AnimInstance->Montage_IsPlaying(AttackMeleeMontage) == false)
            	//{
            	//	AnimInstance->Montage_Play(AttackMeleeMontage);
            	//}
            
            	if (0 == CurrentComboCount)
            	{
            		BeginAttack();
            	}
            	else
            	{
            		ensure(FMath::IsWithinInclusive<int32>(CurrentComboCount, 1, MaxComboCount));
            		bIsAttackKeyPressed = true;
            	}
            }
            
            ```
            
        
        - [실습] Layered blend per bone 노드
            
            움직이면서 공격하고 싶다면 아래와 같이 작성 후 Layered blend per bone 노드를 활용해보자.
            
            ```cpp
            // SXCharacterBase.cpp
            
            ...
            
            void ASXCharacterBase::BeginAttack()
            {
            	USXAnimInstance* AnimInstance = Cast<USXAnimInstance>(GetMesh()->GetAnimInstance());
            	checkf(IsValid(AnimInstance) == true, TEXT("Invalid AnimInstance"));
            
            	//GetCharacterMovement()->SetMovementMode(EMovementMode::MOVE_None);
            	...
            }
            
            void ASXCharacterBase::EndAttack(UAnimMontage* InMontage, bool bInterruped)
            {
            	...
            	//GetCharacterMovement()->SetMovementMode(EMovementMode::MOVE_Walking);
            
            	if (OnMeleeAttackMontageEndedDelegate.IsBound() == true)
            	{
            		...
            	}
            }
            
            ```
            
            <img width="368" height="583" alt="스크린샷 2025-08-28 192336" src="https://github.com/user-attachments/assets/4af7e9ed-e5bc-4700-a921-e25e99ad2586" />
