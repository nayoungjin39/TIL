# NetPriority

클라이언트에 보낼 수 있는 대역폭(NetBandwidth)은 한정되어 있습니다.
클라이언트에 보낼 액터 중, NetPriority 값이 높은 액터의 데이터를 우선 전달하도록 설계되어 있습니다.
액터에 설정된 NetPriority 값을 활용해 전송 순서를 결정합니다.

<img width="240" height="142" alt="스크린샷 2025-10-24 181411" src="https://github.com/user-attachments/assets/34304b7b-cb47-45d3-b9c3-22eafb0c8fee" />

## **NetPriority에 대한 고찰**

- `NetPriority` 속성 값은 순서를 정하는 데만 사용됩니다.
- ex) 매 1분마다 오는 비행기가 있다고 가정해보겠습니다.
    좌석 등급(NetPriority)이 높은 승객부터 먼저 탑니다.
    그 결과로 `NetPriority`가 `2.f`인 액터는 `1.f`인 액터보다
    2배의 빈도로 업데이트될 수 있습니다.

> ”될 수 있다”는거지, 무조건 그렇다는게 아님에 주의 바랍니다.
비행기가 널널하면 모두 다 탈 수 있으므로, 똑같은 빈도입니다.

## 액터의 현재 `NetPriority` 속성 값은 `AActor::GetNetPriority()` 함수에서 계산됩니다.

이 계산에는 당연히 `NetCullDistance` 같은 연관성 관련 속성들이 반영됩니다.
그리고 직전 패킷을 보낸 후 경과 시간과 가중치, 이전 `NetPriority` 값 등을 곱해서 계산합니다.
포화 상태가 아니라면 계산된 `NetPriority` 값을 기준으로 정렬된 순서대로 레플리케이션합니다.

## **가중치**

여러 가지 기준에 따라 가중치를 결정합니다.

1️⃣**해당 액터가 `ViewTarget`인 경우 → `4.f`**

2️⃣**해당 액터가 `ViewTarget` 액터에게 영향을 받은 경우 → `4.f`**

3️⃣**`ViewTarget`이 바라보는 방향에 해당 액터가 있고 가까운 경우 → `2.f`**

4️⃣**`ViewTarget`이 바라보는 방향에 해당 액터가 있지만 적당히 먼 경우 → `0.4f`**

5️⃣**`ViewTarget`이 바라보는 방향에 해당 액터가 없고 적당히 먼 경우 → `0.2f`**


- AActor::GetNetPriority() 함수

```
// AActor.cpp

float AActor::GetNetPriority(const FVector& ViewPos, const FVector& ViewDir, AActor* Viewer, AActor* ViewTarget, UActorChannel* InChannel, float Time, bool bLowBandwidth)
{
	if (bNetUseOwnerRelevancy && Owner)
	{
		// If we should use our owner's priority, pass it through
		return Owner->GetNetPriority(ViewPos, ViewDir, Viewer, ViewTarget, InChannel, Time, bLowBandwidth);
	}

	if (ViewTarget && (this == ViewTarget || GetInstigator() == ViewTarget))
	{
		// If we're the view target or owned by the view target, use a high priority
		Time *= 4.f;
	}
	else if (!IsHidden() && GetRootComponent() != NULL)
	{
		// If this actor has a location, adjust priority based on location
		FVector Dir = GetActorLocation() - ViewPos;
		float DistSq = Dir.SizeSquared();

		// Adjust priority based on distance and whether actor is in front of viewer
		if ((ViewDir | Dir) < 0.f)
		{
			if (DistSq > NEARSIGHTTHRESHOLDSQUARED)
			{
				Time *= 0.2f;
			}
			else if (DistSq > CLOSEPROXIMITYSQUARED)
			{
				Time *= 0.4f;
			}
		}
		else if ((DistSq < FARSIGHTTHRESHOLDSQUARED) && (FMath::Square(ViewDir | Dir) > 0.5f * DistSq))
		{
			// Compute the amount of distance along the ViewDir vector. Dir is not normalized
			// Increase priority if we're being looked directly at
			Time *= 2.f;
		}
		else if (DistSq > MEDSIGHTTHRESHOLDSQUARED)
		{
			Time *= 0.4f;
		}
	}

	return NetPriority * Time;
}
```

### 포화 상태

**액터 관련 데이터가 너무 커서 대역폭을 넘어선 경우를 포화 상태라고 합니다.**

현재 `NetPriority` 값이 가장 낮은 액터는 다음 서버 틱에서 레플리케이션 됩니다.

<img width="444" height="86" alt="스크린샷 2025-10-24 181744" src="https://github.com/user-attachments/assets/88eef8d6-bd36-465c-80eb-6e421106ed70" />

DXBox 액터가 전송 해야할 데이터가 너무 커서 대역폭을 넘어서는 상황입니다.
그래서 다른 액터들이 먼저 전송되고 다음 틱에 전송되는 상황입니다.
이 경우에는 다른 액터들보다 NetPriority를 높혀주던가, 전송 해야할 데이터를 줄이던가 해야합니다.














