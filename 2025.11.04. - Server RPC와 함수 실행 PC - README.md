# Server RPC 실습

- 내가 스폰 한 지뢰가 다른 사람에게는 안 보이는 이유

입력이 로컬 클라이언트에서만 발생했기에 입력 함수는 서버에서 실행되지 않습니다.
이때문에 스폰 로직은 서버에서 실행되지 않습니다. 

- 내가 스폰한 지뢰를 다른 사람에게도 보이게하려면 어떻게 해야할까
- 클라이언트가 클라이언트에게 통신해서 보여줄 수 있을까

서버-클라이언트 구조라서 당연히 안됩니다.
액터를 서버에서 스폰 시키고, 해당 액터를 클라이언트들에게 복제해야 합니다.

```
// DXPlayerCharacter.h

...
class DEDICATEDX_API ADXPlayerCharacter : public ADXCharacterBase
{
	...

#pragma region LandMine

private:
	UFUNCTION(Server, Reliable, WithValidation)
	void ServerRPCSpawnLandMine();
	
protected:
	...
	
#pragma endregion

	
};
```

```
// DXPlayerCharacter.cpp


...

void ADXPlayerCharacter::HandleLandMineInput(const FInputActionValue& InValue)
{
	//if (IsValid(LandMineClass) == true)
	//{
	//	FVector SpawnedLocation = (GetActorLocation() + GetActorForwardVector() * 300.f) - FVector(0.f, 0.f, 90.f);
	//	ADXLandMine* SpawnedLandMine = GetWorld()->SpawnActor<ADXLandMine>(LandMineClass, SpawnedLocation, FRotator::ZeroRotator);
	//	SpawnedLandMine->SetOwner(GetController());
	//}

	if (IsLocallyControlled() == true)
	{
		ServerRPCSpawnLandMine();
	}
}

void ADXPlayerCharacter::ServerRPCSpawnLandMine_Implementation()
{
	if (IsValid(LandMineClass) == true)
	{
		FVector SpawnedLocation = (GetActorLocation() + GetActorForwardVector() * 300.f) - FVector(0.f, 0.f, 90.f);
		ADXLandMine* SpawnedLandMine = GetWorld()->SpawnActor<ADXLandMine>(LandMineClass, SpawnedLocation, FRotator::ZeroRotator);
	}
}

bool ADXPlayerCharacter::ServerRPCSpawnLandMine_Validate()
{
	return true;
}
```

### **그래도 서버에서 스폰한 개체가 보이지 않는 이유**

서버에서 스폰 된 액터의 정보가 클라이언트들에게 복제(Replicate) 되지 않았기 때문입니다.
`Content Browser > BP_LandMine > Details > Replicates`에 체크 후 다시 플레이합니다.

<img width="325" height="121" alt="스크린샷 2025-11-04 185020" src="https://github.com/user-attachments/assets/1f794b3c-bfa3-4abe-b646-b8ab27d8753c" />

```
// DXLandMine.cpp


...

ADXLandMine::ADXLandMine()
{
	PrimaryActorTick.bCanEverTick = false;
	bReplicates = true;
		// 생성자에서는 SetReplicates() 함수를 호출하지 않음. 경고 뜰 때가 있음.

	...
}

...
```

### **스폰 흐름 요약**

1️⃣ **특정 키를 누릅니다.**

2️⃣ **해당 키를 누른 클라이언트 로컬 캐릭터 클래스의 키 바인드 함수가 호출됩니다.**

3️⃣ **서버로 보내는 패킷에 Server RPC 함수의 호출 정보도 Serialize 해서 보냅니다.**

4️⃣ **서버에서는 패킷을 `Deserialize` 후 클라이언트의 캐릭터 클래스 Server RPC 함수 실행합니다.**

5️⃣ **서버에서 `BP_LandMine` 스폰 합니다.**

6️⃣ **`BP_LandMine`은 `bReplicates` 속성이 `true`이므로, 클라이언트들에게 스폰 정보 복제합니다.**

### Server/OwningClient/OtherClient 화면에 어떻게 출력될지 예측해 봅니다.

```
// DXLandMine.cpp


...

void ADXLandMine::BeginPlay()
{
	Super::BeginPlay();

	// UKismetSystemLibrary::PrintString(this, FString::Printf(TEXT("ADXLandMine::BeginPlay()")), true, true, FLinearColor::Green, 5.f);	
	
	if (HasAuthority() == true)
	{
		UKismetSystemLibrary::PrintString(this, FString::Printf(TEXT("Run on server.")), true, true, FLinearColor::Green, 5.f);	
	}
	else
	{
		APawn* OwnerPawn = Cast<APawn>(GetOwner());
		if (IsValid(OwnerPawn) == true)
		{
			if (OwnerPawn->IsLocallyControlled() == true)
			{
				UKismetSystemLibrary::PrintString(this, FString::Printf(TEXT("Run on owning client.")), true, true, FLinearColor::Green, 5.f);
			}
			else
			{
				UKismetSystemLibrary::PrintString(this, FString::Printf(TEXT("Run on other client.")), true, true, FLinearColor::Green, 5.f);
			}
		}
	}
}

...
```

### **OwningClient가 호출되지 않는 이유와 해결 방법은?**

Owner 속성이 지정되지 않았기 때문입니다.

```
/// DXPlayerCharacter.cpp


...

void ADXPlayerCharacter::ServerRPCSpawnLandMine_Implementation()
{
	if (IsValid(LandMineClass) == true)
	{
		...
		
		// SpawnedLandMine->SetOwner(GetController());
			// 내 컨트롤러는 서버와 내 PC에만 존재함. 
			// 다른 PC에는 내 컨트롤러가 존재하지 않음에 주의. 현재 로직으로는 Other와 Owning을 구분 불가.
		SpawnedLandMine->SetOwner(this);
			// 그러나 내 캐릭터는 서버와 내 PC 뿐만 아니라 다른 PC에도 존재함.
	}
}

...
```
