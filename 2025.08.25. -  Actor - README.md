- Actor
    - 3.1 언리얼의 기본 요소
        - 월드
        게임이 진행되는 레벨과 모든 액터 그리고 시간, 물리 등의 오브젝트 집합.
        Toolbar > Settings > World Settings를 통해 위와 관련된 설정을 볼 수 있음.
        - **레벨**
          가상 세계를 담당하는 3차원의 공간. 단위는 cm. Viewport를 통해 보이는 공간.
          플레이 버튼을 누르면 Outliner에 기존에 레벨을 구성하던 액터(흰색 이름)와 함께
          동적으로 생성된 액터(노란색 이름)들이 보임.
          레벨에 사용되는 애셋은 .umap 애셋.
        - **액터**
          월드를 구성하는 단위 개체.
          액터 개체는 월드에 존재하므로, 트랜스폼이 항상 부여됨.
          특정 상황에 대응할 구체적인 게임 로직을 소스코드로 개발자가 작성함.
          해당 게임 로직은 C++ 혹은 블루프린트 두 가지 언어로 작성 가능.
        - **시간**
          가상 공간에서 흐르는 시간. 단위는 초.
          가상 공간에서 흐르는 시간이기에 흐르는 속도를 조절할 수도 있음.
        - **물리**
          월드 공간에 배치된 액터들 사이의 작용.
          해당 액터에 Collision 정보가 있다면 물리적인 작용이 일어남.
        
        - 액터의 기능
        시각적 기능: 어떻게 보여질 것인가. ex) 스태틱 메시, 애니메이션, …
        물리적 기능: 액터들 간의 물리 작용 처리는 어떻게 할 것인가. ex) 충돌, …
        이동 기능: 액터가 어떻게 움직일 것인가.
        이러한 액터의 기능을 구현하기 위해 언리얼은 컴포넌트 구조를 채택함.
          ex) 숨겨진 포탈 액터는 시각적 기능과 이동 기능이 없고 물리적 기능만 함.
        
        - 컴포지션
        개체 지향 프로그래밍에서 상속(Is-A)만으로 구조를 짜면 유지보수가 어려워짐.
        상속이 깊은 경우에 최상위 부모 클래스의 변경은 그 하위 모든 자식 클래스에 영향을 줌.
        컴포지션은 Has-A 관계를 구현하는 설계 방법. 즉, 뗐다 붙혔다 하기 쉽다는 장점이 있음.
        기능이라는 개념을 상속으로 구현한다면 많은 기능을 가지는 액터는 유지보수 어려워짐.
        
        - 언리얼 엔진의 컴포지션
        액터를 구현할 때 다양한 기능을 유연하게 탈착할 수 있게끔 각 기능을 규격화함.
        이렇게 규격화된 기능을 컴포넌트라고 함.
          ex) 스태틱 메시 컴포넌트, 박스 컴포넌트, 무브먼트 컴포넌트, …
        
        - 컴포넌트와 액터의 연결
        하나의 언리얼 오브젝트에는 항상 클래스 기본 오브젝트가 있음.
        언리얼 오브젝트 간의 컴포지션은 어떻게 구현할 것인가?
        필수적 포함: CDO에 미리 다른 언리얼 오브젝트를 생성 후 포함.
        선택적 포함: CDO에는 빈 포인터로 두고, 런타임에서 생성 후 포함.
        
        - 컴포넌트와 액터의 관계
        내가 소유한 언리얼 오브젝트를 SubObject라고 함.
        나를 소유한 언리얼 오브젝트를 Owner라고 함.
        
        - 액터는 여러 개의 컴포넌트를 가질 수 있음.
        그 컴포넌트들 중에서도 대표하는 하나의 컴포넌트가 반드시 있음.
        이 컴포넌트를 루트 컴포넌트라고 함.
        
        - 자주 쓰이는 컴포넌트
            
            
            | 스태틱메시 컴포넌트 | 애니메이션 정보가 없는 애셋인 스태틱 메시를 가지고
            시각적 기능과 물리적 기능을 제공할 수 있는 컴포넌트. |
            | --- | --- |
            | 스켈레탈메시 컴포넌트 | 애니메이션 정보가 있는 애셋인 스켈레탈 메시를 가지고
            시각적 기능과 물리적 기능을 제공할 수 있는 컴포넌트. |
            | 콜리전 컴포넌트 | 특정 모양(구/박스/캡슐)의 물리적 기능을 제공하는 컴포넌트. |
            | 무브먼트 컴포넌트 | 물체에 이동 기능을 제공하는 컴포넌트. |
            | 카메라 컴포넌트 | 가상 세계를 플레이어의 모니터에 출력 해주는 
            기능을 제공하는 컴포넌트. |
          
    - 3.2 액터 클래스 생성
        - [실습] 액터 클래스
            
            새 C++ 클래스 > Actor 부모 클래스 > “SXTorch”
            Path: Gimmick
            
            필요 없는 함수들을 구태여 지금 구현할 필요가 없음. 주석까지 모두 지우고 시작.
            A가 생성된 클래스 이름 가장 앞에 붙음. 이는 Actor 클래스를 상속 받는다는 뜻.
            
            ```cpp
            // SXTorch.h
            
            #pragma once
            
            #include "CoreMinimal.h"
            #include "GameFramework/Actor.h"
            #include "SXTorch.generated.h"
            
            class UBoxComponent;
            class UStaticMeshComponent;
            class UPointLightComponent;
            class UParticleSystemComponent;
            
            UCLASS()
            class SHOOTERX_API ASXTorch : public AActor
            {
                GENERATED_BODY()
            
            public:
                ASXTorch();
            
            private:
                UPROPERTY()
                TObjectPtr<UBoxComponent> BoxComponent;
            
                UPROPERTY()
                TObjectPtr<UStaticMeshComponent> BodyStaticMeshComponent;
            
                UPROPERTY()
                TObjectPtr<UPointLightComponent> PointLightComponent;
            
                UPROPERTY()
                TObjectPtr<UParticleSystemComponent> ParticleSystemComponent;
            
            };
            
            ```
            
            ```cpp
            // SXTorch.cpp
            
            ~~#include "Gimmick/SXTorch.h"
            #include "Components/BoxComponent.h"
            #include "Components/PointLightComponent.h"
            #include "Particles/ParticleSystemComponent.h"
            
            // Sets default values
            ASXTorch::ASXTorch()
            {
                PrimaryActorTick.bCanEverTick = false;
            
                BoxComponent = CreateDefaultSubobject<UBoxComponent>(TEXT("BoxComponent"));
                SetRootComponent(BoxComponent);
            
                BodyStaticMeshComponent = CreateDefaultSubobject<UStaticMeshComponent>(TEXT("BodyStaticMeshComponent"));
                BodyStaticMeshComponent->SetupAttachment(GetRootComponent());
            
                PointLightComponent = CreateDefaultSubobject<UPointLightComponent>(TEXT("PointLightComponent"));
                PointLightComponent->SetupAttachment(GetRootComponent());
            
                ParticleSystemComponent = CreateDefaultSubobject<UParticleSystemComponent>(TEXT("ParticleSystemComponent"));
                ParticleSystemComponent->SetupAttachment(GetRootComponent());
            }~~
            
            ```
            
        
        - [실습] 블루프린트 애셋 생성
            
            Content Browser > ShooterX > Blueprint 우클릭 > 새 폴더 “Gimmick”
            Gimmick > 새 Blueprint 애셋 > SXTorch 부모 클래스 > “BP_Torch”
            
        
        - TObjectPtr<>
        언리얼 5 버전 이후부터 헤더에 언리얼 오브젝트 포인터를 선언할 때는
        로우 포인터 대신 TObjectPtr을 사용하는 걸 권장함.
        
        - UPROPERTY()
        언리얼 오브젝트 클래스의 속성(== 멤버, 멤버 변수, …)에 작성하는 매크로.
        해당 매크로가 작성된 속성은 언리얼 런타임 시스템에 의해 관리됨.
        즉, 가비지 컬렉션/리플렉션/레플리케이션 등의 장점을 취할 수 있음.
        다만 성능에 영향을 줄 수 있으므로, 굳이 필요 없는 속성에는 달지 않음.
        
        - bCanEverTick
        이 속성이 true인 경우엔 Tick() 함수가 매 프레임마다 호출됨.
        지금은 Tick() 함수가 굳이 필요 없으므로 false로 지정.
        Tick() 함수의 로직은 매 프레임 마다 수행되므로 성능에 큰 영향을 줌.
        
        - CreateDefaultSubobject<>()
        컴포넌트도 결국 언리얼 오브젝트임.
        Subobject를 필수적으로 포함시키기 위해서 호출하는 함수.
        선택적 포함을 원할 때는 NewObject<>() 함수를 호출함.
        이렇게 생성된 컴포넌트는 반드시 등록 함수인 RegisterComponent() 호출 필요.
        그래야 해당 컴포넌트가 월드의 시간과 물리 처리에 등록됨.
        
        - CreateDefaultSubobject<>() 함수에 전달되는 문자열
        해당 문자열은 액터에 속한 컴포넌트 끼리 구별하기 위한 Hash 값 생성에 사용됨.
        따라서 다른 컴포넌트와 중복되지 않는 유일한 문자열을 지정해야함. 
        저자는 보통 변수명으로 지정.
        
        - [실습] VisibleAnywhere 키워드
            
            UPROPERTY() 매크로에는 지정자(Specifier)를 설정할 수 있음.
            UPROPERTY() 매크로에 VisibleAnywhere 키워드를 전달하면 
            에디터에서 해당 컴포넌트 속성을 수정할 수 있음.
            주소 값을 알면(볼 수 있으면) 메모리에 접근해서 수정도 할 수 있다는 원리와 유사.
            다만 초기화 버튼을 누르거나 에디터를 껏다 키면 값이 모두 초기화됨.
            
            ```cpp
            // SXTorch.h
            
            ...
            class SHOOTERX_API ASXTorch : public AActor
            {
                ...
            
            private:
                UPROPERTY(VisibleAnywhere)
                TObjectPtr<UBoxComponent> BoxComponent;
            
                UPROPERTY(VisibleAnywhere)
                TObjectPtr<UStaticMeshComponent> BodyStaticMeshComponent;
            
                UPROPERTY(VisibleAnywhere)
                TObjectPtr<UPointLightComponent> PointLightComponent;
            
                UPROPERTY(VisibleAnywhere)
                TObjectPtr<UParticleSystemComponent> ParticleSystemComponent;
            
            };
            
            ```
            
        
        - [실습] C++ 코드로 컴포넌트 수정
            
            C++로 컴포넌트 속성을 수정하면 다시 켜도 초기화 되지 않음.
            
            ```cpp
            // SXTorch.cpp
            
            ...
            
            ASXTorch::ASXTorch()
            {
                PrimaryActorTick.bCanEverTick = false;
            
                BoxComponent = CreateDefaultSubobject<UBoxComponent>(TEXT("BoxComponent"));
                SetRootComponent(BoxComponent);
            
                BodyStaticMeshComponent = CreateDefaultSubobject<UStaticMeshComponent>(TEXT("BodyStaticMeshComponent"));
                BodyStaticMeshComponent->SetupAttachment(GetRootComponent());
                BodyStaticMeshComponent->SetRelativeLocation(FVector(0.f, 0.f, -30.f));
            
                PointLightComponent = CreateDefaultSubobject<UPointLightComponent>(TEXT("PointLightComponent"));
                PointLightComponent->SetupAttachment(GetRootComponent());
                PointLightComponent->SetRelativeLocation(FVector(0.f, 0.f, 500.f));
            
                ParticleSystemComponent = CreateDefaultSubobject<UParticleSystemComponent>(TEXT("ParticleSystemComponent"));
                ParticleSystemComponent->SetupAttachment(GetRootComponent());
                ParticleSystemComponent->SetRelativeLocation(FVector(0.f, 0.f, 500.f));
            }
            
            ```
            
        
    - 3.3 언리얼 C++ 자료형
        - 언리얼 C++ 자료형
        오브젝트 타입: 스태틱 메시 컴포넌트의 포인터, …
        프리미티브 타입: uint8, int32, float, …
        
        - 프리미티브 타입 재정의의 필요성
        C++은 C언어를 발전시킨 언어.
        C언어는 임베디드 전용 언어로, 극단적인 환경에서도 동작할 수 있게끔 개발됨.
        극단적인 환경이라 함은 메모리의 크기가 상당히 작은 컴퓨터.
        그래서 자료형의 크기가 아주 중요함. C언어는 자료형의 크기를 컴파일러의 자율로 맡김.
        내가 개발한 임베디드 장비 전용 컴파일러를 만들 때 int를 256비트로 해도된다는 이야기.
        C++ 표준에서도 32bit 플랫폼이냐, 64bit 플랫폼이냐에 따라 자료형 크기가 다름.
        이러한 플랫폼 파편화는 게임 개발에서 유지보수에 심각한 영향을 줌.
        언리얼에서는 프리미티브 타입을 재정의해서 크기가 모호하지 않게끔 함.
        
        - FVector
        위치 혹은 크기를 의미하는 3개의 프리미티브 타입을 묶어서 하나의 구조체로 정의함.
        FVector에는 연산자도 재정의되어 있어서 스칼라곱부터 내적, 외적 등 간편하게 가능함.
        
        - FRotator
        회전에는 FRotator 구조체를 사용함.
        차례대로 Pitch, Yaw, Roll이라는 3개의 회전 값을 가짐.
        Pitch: 좌우를 기준으로 한 회전 값. 언리얼 엔진 기준 Y축 회전 표현.
        Yaw: 상하를 기준으로 한 회전 값. 언리얼 엔진 기준 Z축 회전 표현.
        Roll: 전후를 기준으로 한 회전 값. 언리얼 엔진 기준 X축 회전 표현.
            
        - FTransform
        월드에 존재하는 모든 액터는 트랜스폼 정보를 가짐.
        이때 쓰이는 자료형이 FTransform. 
        크기(FVector), 회전(FRotator), 위치(FVector) 정보가 들어있음.
        
        - [실습] 프리미티브 타입과 UPROPERTY() 매크로
            
            프리미티브 타입의 속성을 선언하고 UPROPERTY() 매크로를 붙히면 자동으로 초기화됨.
            이는 Details에서 확인 가능함. 또한 VisibleAnywhere 키워드 속성이라 수정 불가.
            
            ```cpp
            // SXTorch.h
            
            ...
            class SHOOTERX_API ASXTorch : public AActor
            {
                ...
            
            private:
                ...
            
                UPROPERTY(VisibleAnywhere)
                int32 ID;
            
            };
            
            ```
            
        
        - [실습] 프리미티브 타입과 EditAnywhere 키워드
            
            추가적으로 Category 키워드도 실습.
            Details에서 해당 속성을 특정 카테고리에서 관리할 수 있게끔 함.
            
            ```cpp
            // SXTorch.h
            
            ...
            class SHOOTERX_API ASXTorch : public AActor
            {
                ...
            
            private:
                ...
            
                //UPROPERTY(VisibleAnywhere)
                UPROPERTY(EditAnywhere, Category = "SXTorch")
                int32 ID;
            
            };
            
            ```
            
        
        - UPROPERTY() 매크로의 지정자
        - **Visible / Edit**
          에디터에서 수정 불가능 / 가능
        - **Anywhere / DefaultsOnly / InstanceOnly**
          에디터에서 편집 가능한 영역. 어디서나 / CDO에서만 / 개체에서만
        - **BlueprintReadOnly / BlueprintReadWrite**
          해당 클래스를 블루프린트 클래스에서 상속 받을시에 수정 불가눙 / 가능
        - **Category**
          Details에서의 카테고리 설정 
        - **Meta**
          추가로 필요한 지정자들을 작성하는 용도.
          대표적으로 AllowPrivateAccess, BindWidget, … 키워드 등이 있음.
        
        - [실습] UPROPERTY() 매크로의 지정자
            
            아래와 같이 작성 후 컴파일.
            
            ```cpp
            // SXTorch.h
            
            ...
            class SHOOTERX_API ASXTorch : public AActor
            {
                ...
            
            private:
                //UPROPERTY(VisibleAnywhere)
                UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category = "SXTorch", Meta = (AllowPrivateAccess))
                TObjectPtr<UBoxComponent> BoxComponent;
            
                //UPROPERTY(VisibleAnywhere)
                UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category = "SXTorch", Meta = (AllowPrivateAccess))
                TObjectPtr<UStaticMeshComponent> BodyStaticMeshComponent;
            
                //UPROPERTY(VisibleAnywhere)
                UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category = "SXTorch", Meta = (AllowPrivateAccess))
                TObjectPtr<UPointLightComponent> PointLightComponent;
            
                //UPROPERTY(VisibleAnywhere)
                UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category = "SXTorch", Meta = (AllowPrivateAccess))
                TObjectPtr<UParticleSystemComponent> ParticleSystemComponent;
            
                //UPROPERTY(EditAnywhere, Category = "SXTorch")
                UPROPERTY(EditInstanceOnly, BlueprintReadWrite, Category = "SXTorch", Meta = (AllowPrivateAccess))
                int32 ID;
            
            };
            
            ```
            
            BP_Torch > MyBlueprint > 우측 톱니 바퀴 클릭 > Show Inherited Variables 체크
            하단에 VARIABLES > ID 속성은 Get과 Set 모두 있지만 컴포넌트 속성들은 Get만 있음.
            또한 컴포넌트 속성은 EditDefaultsOnly라서 블루프린트 애셋에서만 수정가능.
            Viewport에 드래그 드랍해서보면, 개체마다 ID 속성을 다르게 가지게끔 가능.
            
        
        - bool 타입의 선언
        C/C++에는 bool 자료형에 대한 표준 자체가 없음. 그래서 그 크기가 명확하지 않음.
        만약 해당 bool 속성이 Serialization이나 Replication과 관련이 있다면
        uint8 자료형 + bit field 연산자를 사용해서 1bit짜리 자료형을 만들어서 대체함. 
        그게 아닌 경우에는 그냥 bool 자료형을 쓰면 됨.
        
    - 3.4 애셋 지정
        - .uasset 확장자
        언리얼의 모든 애셋은 레벨 애셋을 제외하고 모두 .uasset 이라는 확장자를 가짐.
        이 애셋들은 외부적으로는 동일한 체계에서 관리되지만, 애셋에 마우스를 올리거나
        애셋 아이콘 하단의 색상으로 내부 데이터를 파악가능.
        
        - 언리얼 애셋의 고유한 키 값
        언리얼 C++ 코드로 애셋을 불러들이고 특정 컴포넌트에 설정하려면 
        애셋만의 고유한 키 값이 있어야함. 그래야 애셋을 관리하는 시스템에
        해당 키 값으로 질의를 던져서 작업 가능.
        언리얼은 애셋의 고유한 키 값으로 경로 값을 사용함.
        애셋을 클릭하고 Ctrl + C하면 키 값을 얻을 수 있음.
        
        - 오브젝트 패스(Object Path)
        위에서 얻은 키 값을 어딘가에 붙혀넣어 보면 아래와 같은 규칙을 가짐.
        {오브젝트 타입}’{폴더명}/{파일명}.{애셋명}’
        오브젝트 타입 부분과 작은 따옴표를 지워서 간단한 형식으로 사용해도 됨.
        위 키 값을 오브젝트 패스라고 부름. 
        오브젝트 패스는 프로젝트 내에서 유일함이 보장됨. 이를 이용해서 애셋 로딩.
        
        - 실습 전 애셋 추가.
        Content Browser > Add > Add Feature or Content Pack…
            
            Starter Content에는 다양한 이펙트가 들어있음. Add to Project 버튼 클릭.
            
        
        - [실습] 오브젝트 패스를 이용한 애셋 지정
            
            ```cpp
            // SXTorch.cpp
            
            ...
            
            ASXTorch::ASXTorch()
            {
                ...
            
                BodyStaticMeshComponent = CreateDefaultSubobject<UStaticMeshComponent>(TEXT("BodyStaticMeshComponent"));
                BodyStaticMeshComponent->SetupAttachment(GetRootComponent());
                BodyStaticMeshComponent->SetRelativeLocation(FVector(0.f, 0.f, -30.f));
                static ConstructorHelpers::FObjectFinder<UStaticMesh> BodyStaticMesh(TEXT("/Script/Engine.StaticMesh'/Game/StarterContent/Architecture/Pillar_50x500.Pillar_50x500'"));
                    // 경로 정보가 런타임 중에는 변경되지 않으므로, static 키워드 사용.
                if (true == BodyStaticMesh.Succeeded())
                {
                    BodyStaticMeshComponent->SetStaticMesh(BodyStaticMesh.Object);
                }
            
                ...
            
                ParticleSystemComponent = CreateDefaultSubobject<UParticleSystemComponent>(TEXT("ParticleSystemComponent"));
                ParticleSystemComponent->SetupAttachment(GetRootComponent());
                ParticleSystemComponent->SetRelativeLocation(FVector(0.f, 0.f, 500.f));
                static ConstructorHelpers::FObjectFinder<UParticleSystem> ParticleTemplate(TEXT("/Script/Engine.ParticleSystem'/Game/StarterContent/Particles/P_Fire.P_Fire'"));
                if (true == ParticleTemplate.Succeeded())
                {
                    ParticleSystemComponent->SetTemplate(ParticleTemplate.Object);
                }
            }
            
            ```
            
        
        - 하드코딩으로 오브젝트 패스 설정하는 것의 단점
        실제로는 아트팀에서 해당 애셋을 수시로 변경하고 이동시키기 때문에 비추천.
        블루프린트 애셋에서 지정하는 것이 좀 더 안전한 방법. 
        애셋이 이동되더라도 Fix Up Redirectors 메뉴를 통해 자동으로 변경되기 때문.
        근데 사수님이 시키는데로 하는게 가장 올바름.
        
    - 3.5 주요 이벤트 함수
        - 이벤트 함수
        게임이 시작 될 때, 액터는 준비/시작/삭제의 과정을 거침.
        이 과정에서 언리얼 엔진에 의해 자동으로 호출되는 함수를 이벤트 함수라 함.
        
        - 주요 이벤트 함수의 호출 순서
        - **PostInitializeComponents()**
          액터를 구성하는 모든 컴포넌트가 초기화 된 후 호출 되는 함수. 준비 과정에 해당함.
        - **BeginPlay()**
          이 함수가 호출되면서 액터는 자신에게 주어진 로직을 수행하기 시작. 시작 과정.
        - **Tick(float DeltaSeconds)**
          BeginPlay() 함수가 한 번 호출되는 함수라면, Tick() 함수는 매 프레임마다 호출됨.
          프레임이란 게임의 모든 로직(게임 로직, 물리 계산, 랜더링 등)이 한 번 실행되는 단위.
          DeltaSeconds는 이전 프레임과 현재 프레임 사이의 시간 차이.
        - **EndPlay(EEndPlayReason EndPlayReason)**
          게임에서 액터가 소멸될 때 호출되는 함수. 퇴장 과정.
        
        - Tick() 함수를 활용한 액터의 움직임 구현 실습
            
            ```cpp
            // SXTorch.h
            
            ...
            class SHOOTERX_API ASXTorch : public AActor
            {
                ...
            
            public:
                ...
            
                virtual void BeginPlay() override;
            
                virtual void Tick(float DeltaSeconds) override;
            
            private:
                ...
            
                UPROPERTY(EditDefaultsOnly, BlueprintReadWrite, Category = "SXTorch", Meta = (AllowPrivateAccess))
                float RotationSpeed;
            
            };
            
            ```
            
            ```cpp
            // SXTorch.cpp
            
            ...
            
            ASXTorch::ASXTorch()
            {
                PrimaryActorTick.bCanEverTick = true;
            
                ...
            }
            
            void ASXTorch::BeginPlay()
            {
                Super::BeginPlay();
            
                RotationSpeed = 300.f;
            }
            
            void ASXTorch::Tick(float DeltaSeconds)
            {
                Super::Tick(DeltaSeconds);
            
                AddActorWorldRotation(FRotator(0.f, RotationSpeed * DeltaSeconds, 0.f));
            }
            
            ```
            
        
    - 3.6 Movement Component
        - 언리얼에서 제공하는 무브먼트 컴포넌트
        - FloatingPawnMovement
          중력의 영향을 받지 않는 액터의 움직임을 제공. 입력에 따라 자유롭게 움직임 가능.
        - RotatingMovement
          지정한 속도로 액터를 회전시킴.
        - InterpMovement
          지정한 위치로 액터를 이동시킴.
        - ProjectileMovement
          액터에 중력 영향을 주어서 탄도학적인 움직임을 제공. 총알, 미사일 등
        - CharacterMovementComponent
          캐릭터 전용 움직임 컴포넌트. 걷기, 수영, 쪼그리기와 같이 다양한 움직임 제공.
        
        - [실습] RotatingMovement
            
            ```cpp
            // SXTorch.h
            
            ...
            class URotatingMovementComponent;
            
            UCLASS()
            class SHOOTERX_API ASXTorch : public AActor
            {
                ...
            
            private:
                ...
            
                UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "SXTorch", meta = (AllowPrivateAccess))
                TObjectPtr<URotatingMovementComponent> RotatingMovementComponent;
            
            };
            
            ```
            
            ```cpp
            // SXTorch.cpp
            
            ...
            #include "GameFramework/RotatingMovementComponent.h"
            
            ASXTorch::ASXTorch()
            {
                PrimaryActorTick.bCanEverTick = false;
            
                ...
            
                RotatingMovementComponent = CreateDefaultSubobject<URotatingMovementComponent>(TEXT("RotatingMovementComponent"));
            }
            
            void ASXTorch::BeginPlay()
            {
                Super::BeginPlay();
            
                RotationSpeed = 300.f;
                RotatingMovementComponent->RotationRate = FRotator(0.f, RotationSpeed, 0.f);
            }
            
            void ASXTorch::Tick(float DeltaSeconds)
            {
                Super::Tick(DeltaSeconds);
            
                //AddActorWorldRotation(FRotator(0.f, RotationSpeed * DeltaSeconds, 0.f));
            }
            
            ```
            
        
        - 컴포넌트 상속 구조
        씬 컴포넌트는 액터 컴포넌트를 상속 받고 트랜스폼 속성이 추가된 컴포넌트.
        스태틱 메시 컴포넌트도 씬 컴포넌트를 상속 받음.
        반면에 트랜스폼 속성이 필요 없는 무브먼트 컴포넌트들은 액터 컴포넌트를 상속 받음.
        BP_Torch > Components에 보면 RotatingMovementComponent만 따로 있음.
