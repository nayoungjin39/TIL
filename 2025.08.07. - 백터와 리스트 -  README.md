✦ Vector(벡터)와 List(리스트)는 둘 다 순서가 있는 데이터 모음을 저장하는 자료구조이지만, 내부 동작 방식과 성능 특성에서 중요한 차이가 있습니다.

  어떤 프로그래밍 언어인지에 따라 의미가 약간씩 달라질 수 있어, 가장 일반적인 C++ 표준 라이브러리의 std::vector와 std::list를 기준으로 설명하겠습니다.

  핵심 차이점: 메모리 할당 방식

   * Vector (벡터): 연속된 메모리 공간에 데이터를 저장합니다. 마치 일반 배열처럼 데이터들이 메모리상에 연달아 붙어 있습니다.
   * List (리스트): 분산된 메모리 공간에 데이터를 저장합니다. 각 데이터는 다음 데이터의 위치(주소)를 가리키는 포인터를 가지고 있어, 메모리상에 흩어져 있어도 순서를 유지할 수 있습니다. (이를
     '연결 리스트'라고 합니다.)

  비교 표


  
  | 특징 | Vector (Dynamic Array) | List (Linked List) |
  | :--- | :--- | :--- |
  | **메모리 구조** | 연속적인 메모리 (배열 기반) | 비연속적인 메모리 (포인터로 연결) |
  | **임의 접근 속도** | **빠름 (O(1))**<br>인덱스로 바로 접근 가능 | **느림 (O(n))**<br>처음부터  순서대로 찾아가야 함 |
  | **중간 데이터 삽입/삭제** | **느림 (O(n))**<br>삽입/삭제 지점 뒤의 모든 데이터를 이동해야 함 |**빠름 (O(1))**<br>해당 위치의 앞뒤 포인터만 변경하면 됨 |
  | **끝 데이터 삽입/삭제** | **대체로 빠름 (Amortized O(1))**<br>공간이 꽉 차면 재할당이 일어날 수 있음 | **빠름 (O(1))** |
  | **메모리 사용량** | 데이터 외 추가 오버헤드가 적음 | 데이터 외에 이전/다음 노드를 가리키는포인터 때문에 추가 메모리 필요 |
  | **캐시 효율성** | **높음** (데이터가 모여있어 캐시 활용에 유리) | **낮음** (데이터가 흩어져있어 캐시 미스가 잦음) |


  ---

  언어별 참고사항

   * C++: std::vector는 동적 배열, std::list는 연결 리스트(정확히는 이중 연결 리스트)로, 위 설명과 정확히 일치합니다.
   * Python: 파이썬의 기본 list는 C++의 `vector`처럼 동작하는 동적 배열입니다. 이름은 리스트지만, 내부적으로는 벡터의 특징을 가집니다. 파이썬에서 연결 리스트를 사용하려면 collections.deque가
     비슷한 연산을 빠르게 지원하거나, 직접 구현해야 합니다.
   * Java: ArrayList가 C++의 vector와 유사하고, LinkedList가 list와 유사합니다. Java에는 Vector라는 클래스도 있지만, 오래된 버전이며 ArrayList와 거의 같지만 동기화(thread-safe)를 지원한다는
     차이가 있습니다. 특별한 경우가 아니면 ArrayList 사용이 권장됩니다.

  언제 무엇을 써야 할까?

   * Vector를 사용해야 할 때:
       * 데이터를 인덱스로 빠르게 접근해야 할 때 (가장 흔한 경우)
       * 데이터의 순차적인 접근이 많을 때
       * 데이터의 크기를 예측하기 어려워도, 중간에 데이터가 추가/삭제되는 일이 거의 없을 때

   * List를 사용해야 할 때:
       * 데이터의 중간에서 삽입과 삭제가 매우 빈번하게 일어날 때
       * 데이터의 순서는 중요하지만, 특정 위치의 데이터를 직접 접근할 필요가 없을 때

  결론적으로, 대부분의 상황에서는 데이터 접근 속도와 캐시 효율성이 뛰어난 Vector(또는 Python의 list, Java의 ArrayList)를 사용하는 것이 더 유리합니다.
