# **RPC(Remote Procedure Call)**

지금까지는 함수를 호출하는 PC와 실행되는 PC가 같았습니다.
즉, 로컬 PC에서 함수를 호출하고 로컬 PC에서 함수가 실행되었습니다.

RPC는 간단히 말해, 
**“호출하는 PC와 실행하는 PC가 달라도 되게끔 해주는 통신 기법”**이라고 할 수 있습니다.

<img width="815" height="278" alt="스크린샷 2025-10-14 190550" src="https://github.com/user-attachments/assets/d68dfc61-e4fa-47e0-8cd1-cc4942932a2a" />

RPC 공식 문서 : https://dev.epicgames.com/documentation/en-us/unreal-engine/rpcs-in-unreal-engine?application_version=5.1

## **언리얼에서 RPC의 용도**

게임에 큰 영향을 끼치지 않는 일시적인 효과들에 사용하는 편입니다.
주로 코스메틱(사운드, 파티클 등)에 사용합니다.

게임에 큰 영향을 끼치는 것들은 이후에 배울 프로퍼티 레플리케이션을 사용하는 것이 좋습니다.

- 우리의 목표는 아래 두 개의 표를 이해하는 것입니다.
표의 각 칸을 완벽히 이해하기에 앞서, 등장하는 용어들을 먼저 이해해 봅시다.

<img width="816" height="559" alt="스크린샷 2025-10-14 190707" src="https://github.com/user-attachments/assets/58320880-a07a-4b33-9e5e-9205656acbf5" />

# Call과 Invoke

## **Call**

- 정적인 의미를 가집니다.
- 컴파일 타임에 어떤 함수인지, 호출하는 곳과 실행하는 곳이 정해져야 합니다.
- 지금까지 정의했던 일반적인 전역 함수와 멤버 함수가 이에 해당합니다.
즉, **직접적으로** 함수를 **호출하고 실행**해야 합니다. (Direct)

## **Invoke**

- 동적인 의미를 가집니다.
- 런타임에 어떤 함수인지, 호출하는 곳과 실행하는 곳이 어딘지 정해집니다.
- 예를 들어, 함수 포인터, 동적 바인딩, RPC 같은 개념들이 이에 해당합니다.
- 즉, **간접적으로** 함수를 **호출하고 실행**해야 합니다. (Indirect)
RPC도 마찬가지로 "**RPC를 Invoke**"라고 표현합니다.

<img width="792" height="508" alt="스크린샷 2025-10-14 190749" src="https://github.com/user-attachments/assets/e26d39d8-11a7-4cab-a35b-20001d721c69" />

# Actor Ownership

네트워크 멀티플레이가 적용되려면, 액터는 서버에서 스폰되고 `bReplicated`가 `true`여야 합니다.

서버에서 스폰된 후 `SetOwner(PlayerController)` 함수를 호출해야 Client-Owned Actor가 됩니다.


📌 **`PlayerController`가 `Local PlayerController`와 같다면, Client-Owned Actor 입니다.**

- 예: 내 플레이어 캐릭터

📌 **`PlayerController`가 `Local PlayerController`와 다르다면, 
Owned by different client 입니다.**

- 예: 내 화면에 보이는 친구의 플레이어 캐릭터

📌 **서버에서 스폰되었지만 `SetOwner()` 함수 호출이 없으면, Server-Owned Actor 입니다.**

- 예: 보물상자 액터

Unowned Actor와 Server-Owned Actor를 같은 개념으로 생각해도 된다고 말했습니다.
다른 경우있으면 제보 바랍니다.

<img width="824" height="537" alt="스크린샷 2025-10-14 190858" src="https://github.com/user-attachments/assets/73124ab1-1ae0-4b4a-b570-edf55733db9a" />

# NetMulticast, Server, Client 키워드

## `UFUNCTION()` 매크로와 함께 사용되는 키워드들입니다.

키워드는 “해당 원격 PC에서 RPC를 실행시켜 달라는 **요청”**을 뜻합니다.

실제로 실행될지는 표를 통해 판단해야 합니다.

<img width="816" height="540" alt="스크린샷 2025-10-14 190940" src="https://github.com/user-attachments/assets/febd6d07-0270-4c74-beaf-e6ca3e056312" />

### **NetMulticast**

- **“서버를 포함한 모든 클라이언트에서 해당 RPC를 실행시켜주세요”**라는 요청입니다.

<img width="822" height="489" alt="스크린샷 2025-10-14 191041" src="https://github.com/user-attachments/assets/0575cc85-1d7f-440f-97bc-92783f84e237" />

### **erver**

- **“서버에서 해당 RPC를 실행시켜주세요.”**라는 요청입니다.

<img width="821" height="492" alt="스크린샷 2025-10-14 191047" src="https://github.com/user-attachments/assets/64da90ac-b0e7-4fa9-8bc7-8377dd96928e" />

### **Client**

- **“클라이언트에서 해당 RPC를 실행시켜주세요.”**라는 요청입니다.

<img width="826" height="493" alt="스크린샷 2025-10-14 191054" src="https://github.com/user-attachments/assets/d47e7f4c-5f31-4c5a-99fd-2ad870da13c5" />

# 자주 쓰이는 케이스

1. **RPC가 클라이언트에서 호출되고 서버에서 실행되어야 하는 경우에는 `Server` 키워드를 사용해야 합니다.**

- `ClientConnection`이 소유하고 있는 액터(Client-Owned)에서 RPC가 호출되어야 합니다.
- `_Validate()` 함수에서 RPC를 실행할지 말지 결정합니다.

<img width="820" height="551" alt="스크린샷 2025-10-14 191150" src="https://github.com/user-attachments/assets/32291862-756f-4cf1-9d1f-629459c09361" />

2. **RPC가 서버와 모든 클라이언트에서 실행되어야 하는 경우에는 `NetMulticast` 키워드를 사용해야 합니다.**

- 서버에서 호출해야 합니다.
- 부하가 심하므로, 빈번하게 호출되게끔 코드를 작성하는 건 권장되지 않습니다.
ex) Tick() 함수 내에서 NetMulticast RPC 호출.

<img width="807" height="548" alt="스크린샷 2025-10-14 191209" src="https://github.com/user-attachments/assets/a8e18a2e-87ac-464b-998c-971912599619" />

3. **RPC가 서버에서 호출되고 클라이언트에서 실행되어야 하는 경우에는 `Client` 키워드를 사용해야 합니다.**

- 서버에서 호출해야 합니다

<img width="821" height="551" alt="스크린샷 2025-10-14 191227" src="https://github.com/user-attachments/assets/367323e4-0580-441b-b14f-942c6058e009" />

### WithValidation

`UFUNCTION()` 매크로와 함께 사용되는 키워드입니다.
서버에서 실행되는 RPC의 경우에 해당 키워드를 작성하는 것이 권장됩니다.

이 키워드가 붙은 RPC를 구현 할때는 `_Implementation()` 함수와 `_Validate()` 함수로 나뉘어 집니다.

`_Validate()` 함수는 해당 RPC가 서버에서 실제로 실행할지 말지 결정합니다.

서버 실행 로직은 무조건적으로 신뢰되기 때문에, 위변조를 막기 위한 방어막 역할을 합니다.

### Unreliable VS Reliable

`UFUNCTION()` 매크로와 함께 사용되는 키워드입니다.

RPC는 기본적으로 **Unreliable** 키워드를 기준으로 동작합니다. 아무것도 안쓰면 Unreliable입니다.

**Unreliable**은 원격 PC에서 무조건 실행되리라는 보장이 없습니다.

반면, 원격 PC에서 무조건 실행되어야 하는 로직이라면 **Reliable** 키워드를 작성합니다.

#### **Unreliable**의 활용 예시

- 코스메틱(이펙트, 사운드 등)처럼 게임에 큰 영향이 없는 로직.

#### **Reliable**의 활용 예시

- 충돌, 데미지, 스폰처럼 게임에 큰 영향을 끼치는 로직.






