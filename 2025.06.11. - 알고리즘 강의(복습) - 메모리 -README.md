# 알고리즘 강의(복습) - 메모리

## 메모리란

- 메모리는 컴퓨터가 프로그램을 실행하는 동안 데이터를 저장하는 커다란 보관함입니다.
![스크린샷 2025-06-11 162616](https://github.com/user-attachments/assets/0632cc5f-8324-42eb-8406-c825c23aab81)

### 메모리 주소

- 제가 위에 첨부한 이미지를 보시면 긴 줄로 이어진 메모리 공간이 나타나 있습니다.
  이 줄은 실제 컴퓨터 메모리에 있는 아주 많은 연속된 작은 저장 칸들을 단순화해서 표현한 거예요!
  각각의 칸은 아주 작고 기본적인 단위의 정보(예: 1바이트)를 담고 있고, 이 칸들 하나하나가 고유한 주소(e.g. 0x15)를 가지고 있어요.
  이 주소가 바로 **메모리 주소(Memory Address)**랍니다.

### 연속적인 공간 & 주소의 효용성

- 또한, 이미지를 보면 길게 이어진 띠 형태의 구성이 눈에 띄죠?
- 실제 메모리 내부는 끝없이 이어진 많은 칸들이 나열되어 있다고 생각할 수 있습니다!
- 즉, **연속적인 하나의 큰 공간**인거에요! (물론, 이건 프로그래머 관점에서 추상화가 된 모델이라고 생각하시면 됩니다!)
아파트 주소를 예로 들어보자면 101호, 102호, 103호… 이런 식으로 집들이 줄지어 있듯, 메모리 칸들도 0x1000, 0x1001, 0x1002, ... 
이런 식으로 번호가 하나씩 증가하며 이어져 있다고 생각하시면 됩니다. 
정말 엄청나게 긴 줄이지만 주소 덕분에 우리는 수많은 메모리 칸들 중에서 특정 칸을 정확히 찾아갈 수 있게 되는 것입니다!

### 변수를 선언한다는 것

- 아래와 같은 코드가 있다고 생각해볼게요.
  
  ``` c++
  int val = 7;
  ```
  이렇게 변수를 선언한다는 것은 사실 여러 개의 단계로 나눌 수 있습니다.

1. 메모리 어딘가에 val이라는 변수를 위한 공간을 확보하기!
    1-1. 공간을 찾다보니까 0xabc…에 int 데이터를 넣을 수 있는 공간 발견!
2. 해당 공간에 우리가 저장하고 싶은 데이터를 넣기!
3. 우리가 붙인 변수 이름으로 해당 메모리 공간을 간접적으로 참조하기!

즉, 이렇게 선언을 하면 0xabc…에 7이라는 int 데이터를 넣는 것이고 이 메모리 공간에 val이라는 이름표가 붙어서 
우리는 val로 코드에서 사용을 할 수 있는거에요!

## 포인터 

- 앞서 우리는 메모리에 대해 알아보았어요. 메모리에는 데이터를 저장할 수 있는 공간이 있고,
- 각각의 공간은 고유한 **주소**를 가지고 있다는 것을 배웠죠. 

포인터는 이 **메모리의 주소를 저장**하는 특별한 타입의 변수라고 생각하시면 됩니다. 
우선, 어떻게 포인터를 선언할 수 있는지 볼까요?

```c++
int val = 7; // 메모리 어딘가(스택 영역의 어딘가)에 7을 저장 -> 결국, 메모리 주소를 0xabc…에 저장
int* p_val = &val; // 포인터 p_val은 val의 주소 0xabc…을 저장
```
문법이 다소 생경해서 어려워하실 수 있는데요. 해체 분석을 한 번 해보겠습니다.

- `int *` → "정수형 데이터를 가리키는 포인터"를 선언한다는 뜻이에요.
- `&val` → 변수 val의 주소를 가져옵니다.
- 즉, p_val이란 포인터에 val 값이 실제로 저장된 메모리 주소가 들어갑니다.

그리고 포인터 변수는 * 연산자를 통해 간접 참조(dereference)를 할 수 있어요. 

```c++
p_val // 그냥 0xabc…
*p_val // 0xabc…에 저장된 값을 읽어오는 연산
```
즉, p_val은 주소 그 자체이지만 *p_val은 주소가 가리키는 값을 읽는 것이죠! 이해를 위해 그림으로 잠깐 살펴볼까요?
![스크린샷 2025-06-11 163542](https://github.com/user-attachments/assets/8af143bf-e5ff-4734-936e-5721ae951ff7)

다만, 포인터의 경우에는 포인터 자체의 크기가 시스템 아키텍처에 따라 달라지는데요. 
32비트 → 4바이트, 64비트 → 8바이트가 일반적이에요. 왜냐하면, 가리키는 대상이 int든 double이든,
포인터 자체는 ‘메모리 주소’ 하나를 저장할 공간이면 되니까요! 

단, 포인터 자체의 크기와는 별개로 포인터 산술 연산을 할 때 타입을 일치시키지 않으면 예기치 않은 버그가 생길 수 있으니 조심하세요! 
아래처럼, `int*`로 받아야 할 것을 `double*`에 넣는 것과 같이 말이죠.

```c++
// 주의!!! 아래와 같은 코드처럼 이렇게 형변환 하지마세요!
int arr[5] = {1, 2, 3, 4, 5};
double* dp = (double*)arr;  // 위험한 형변환!!!

dp + 1;  // 8바이트 이동
// 원래 int 배열에서는 4바이트씩 이동해야 하는데,
// double 포인터는 8바이트씩 이동하므로 잘못된 메모리 위치를 가리키게 됨
```

포인터 산술 연산은 포인터가 가리키는 타입의 크기에 따라 이동 단위가 달라지기 때문에, 
잘못된 형변환은 메모리 참조 오류를 유발할 수 있어요! 예를 들어, 
int*는 4바이트 단위로 이동하지만 double*는 8바이트 단위로 이동하기 때문에, 메모리를 잘못 참조하게 된답니다. 
이러한 행위는 삼가해주셔야해요!

_포인터를 써야하는 이유!_

아래코드를 봅시다.

```c++
int arr_size;
cin >> arr_size;
int arr[arr_size]; // 어... 이거 왜 안돼요?!
```

얼핏 봐서는 이게 왜 안되는지 궁금한 분들이 많으셨을겁니다! 우선, 일반 배열은 스택 영역에 할당되는데요. 
이 스택 영역은 컴파일 시점에 크기가 결정되어야 하는 메모리 영역이에요. 
근데, 실행 시간에 입력받은 크기로 스택에 메모리를 할당하는 것은 C++ 언어의 설계 철학과 맞지 않아요!

![스크린샷 2025-06-11 163838](https://github.com/user-attachments/assets/bf5ffb84-33fa-48d2-9137-b988458324bc)

게다가, 스택 영역은 크기가 제한적인 메모리 영역인데요. 예를 들어, arr_size를 진짜 미친듯이 매우 큰 수를 입력하면 어떻게 될까요? 

```c++
int arr_size;
cin >> arr_size; // 이 값에 2103420342034204024023402340 이 넣어졌다면?
int arr[arr_size]; // 허허... 그저 웃지요...
```

혹여나 이걸 가능하게 하더라도 실행 시간에 결정되는 크기로 배열을 만들면 스택 오버플로우의 위험이 있어요!
그래서, C++은 컴파일 시점에 모든 변수의 크기와 위치가 결정되어야 한다는 원칙이 있는 것입니다!

그래서, 이런게 가능하려면 벡터를 사용하거나(STL 컨테이너 학습하다 보면 배우세요!) 
아니면 포인터를 이용해서 동적 할당을 해주면 됩니다. 이렇게 말이죠.

```c++
int arr_size;
cin >> arr_size;
int* arr = new int[arr_size]; // 스택이 아니라 힙에 할당!
// ...
// 나중에 어디선가 다 쓰면 delete[] arr; 호출이 필요해요!
```

이렇게 포인터 타입으로 배열을 동적으로 만들 수 있어요. 
이렇게 하면, 동적 할당(new)을 통해 메모리를 관리할 수 있기 때문에 필요할 때만 생성하고 
필요 없을 때는 명시적으로 메모리를 해제(delete)할 수 있답니다! 
즉, 메모리를 효율적으로 사용하는데 포인터는 필수적이란 것이죠!

단, 이렇게 할당 / 해제에 대한 리스크(해제를 알맞게 하지 못하면 메모리 누수가 생깁니다!)를 
우려하여 스마트 포인터라는 개념이 나오기 시작했고 이제는 원시 포인터를 그대로 쓰는 경우는 확실히 좀 줄어들었으나 
(물론, 임베디드와 같은 도메인에선 여전히 쓰입니다) 여기선 편의로 원시 포인터를 쓴다는 것을 이해해주세요!

_포인터와 배열의 관계!_

- 앞서 배운 포인터 개념을 바탕으로, 배열과 포인터의 관계를 살펴보겠습니다. 
사실, 배열은 포인터와 아주 밀접한 관련이 있어요. 배열 이름 자체가 포인터처럼 동작하기 때문이죠!

우선, 배열 이름은 배열의 첫 번째 원소의 주소를 가리키는 상수 포인터로 동작합니다.

```c++
int arr[5] = {1, 2, 3, 4, 5};
int* ptr = arr; // arr은 &arr[0]과 같아요!
```

위 코드에서 배열 이름 `arr`은 `&arr[0]`과 동일합니다. 즉, 배열의 첫 번째 원소를 가리키는 포인터 역할을 하는 것이죠.
따라서, 배열과 포인터는 다음과 같이 같은 결과를 만들어냅니다.

```c++
cout << arr[0] << endl;  // 1
cout << *arr << endl; // 1 (arr은 &arr[0]을 가리키니까 *arr은 arr[0]과 동일)
```

- 예제코드

```c++
int arr[5] = {10, 20, 30, 40, 50};
int* ptr = arr; // arr은 첫 번째 원소(10)의 주소

for (int i = 0; i < 5; i++) {
    cout << *(ptr + i) << " "; // 포인터로 배열 접근하는 방법 중 하나. * 연산자 재등장!
}
cout << endl;

// 이렇게 접근하는건 익숙하실거에요!
for (int i = 0; i < 5; i++) {
    cout << arr[i] << " ";
}
cout << endl;
```

위와 같이 포인터 변수에 * 연산자를 통해 인덱스로 산술 연산을 하여 각 원소에 자유롭게 접근할 수 있어요! 
다만, 가독성이 좋지 않아서 이렇게 잘 쓰진 않아요.

배열을 함수의 매개변수로 전달할 때 은근히 헷갈리신 분들이 많았을 것 같은데요. 
이럴 때는, 배열 이름 자체를 전달하며 이는 포인터로 전달되는 것과 같아요.
함수 내부에서 배열은 포인터로 다뤄지므로 원래 배열에 똑같이 영향을 줄 수 있어요!

```c++
// 배열을 함수의 매개변수로 받을 때, 배열 이름은 포인터로 전달되므로
// 보통 이렇게 포인터 변수 타입으로 매개변수 형식을 정의합니다!
void modify_array(int* arr, int size) {
    for (int i = 0; i < size; i++) {
        arr[i] += 10; // 아 보기 편하다! 배열 표기법! 
                      // arr[i]는 내부적으로 *(arr+ i)로 치환이 되니 이렇게 쓰세요!
    }
}

int main() {
    int arr[3] = {1, 2, 3};
    modify_array(arr, 3); // 배열 이름을 포인터로 전달

    for (int i = 0; i < 3; i++) {
        cout << arr[i] << " "; // 11 12 13 출력
    }
}
```

근데, 이렇게 **원시 배열을 포인터로 전달할 때는 배열 크기를 별도로 전달해주는게 무조건 중요**합니다! 왜냐하면, 
배열 자체가 포인터로 처리되기 때문에 크기 정보를 알 수 없기 때문이에요! 

이러한 점들이 불편하니 나중에는 STL 배우신 분들은 `std::array` 또는 `std::vector`를 많이 사용한다고 알고 계시면 될 것 같아요! 
이 친구들은 배열의 크기 정보를 내부적으로 관리하거든요.

동적 배열 예시도 한 번만 볼게요.

```c++
int main() {
    int size = 5;
    int* arr = new int[size]; // 동적 메모리 할당

    for (int i = 0; i < size; i++) {
        arr[i] = i + 1; // 이제 익숙
    }

    for (int i = 0; i < size; i++) {
        cout << arr[i] << " ";
    }
    cout << endl;

    delete[] arr; // 동적 배열은 다 썼으면 반드시 해제는 필수!
}
```

동적 배열을 함수의 매개변수로 넘겨줄 때도 포인터 타입으로 매개변수를 선언하면 됩니다!
```c++
void print_dynamic_array(int* ptr, int size) {
    for (int i = 0; i < size; i++) {
        cout << ptr[i] << " ";
    }
    cout << endl;
}

int main() {
    int* ptr = new int[5]; // 동적 배열
    for (int i = 0; i < 5; i++) {
        ptr[i] = (i + 1) * 10;
    }

    print_dynamic_array(ptr, 5); // 정적 배열과 다를게 없이 똑같이 변수 이름을 넘겨줘요.

    delete[] ptr; // 한번 더! 동적 배열은 다 썼으면 반드시 해제는 필수!
}
```
_정적 배열이랑 동적 배열 차이!_

- 정적 배열: 스택 메모리, 컴파일 시점 크기 결정, 자동 해제
- 동적 배열: 힙 메모리, 런타임 크기 결정, 수동 해제

배열 크기를 런타임에 동적으로 결정해야 할 때 
(위에서 cin으로 배열 사이즈 받는 것 기억나시죠?)나 큰 배열을 생성해야 될 때는 동적 배열을 사용하시면 됩니다. 
그 외엔 그냥 편하게 정적 배열(**기왕이면** `std::array`) 쓰세요! → C++11 이후 표준이라고 보시면 되고 일반 정적 배열에 
비해 STL 알고리즘 사용도 편하고 장점이 많아요!

마지막으로, 요점 정리 살짝 해봤어요!

- 배열 이름은 포인터처럼 동작하지만 진짜 포인터는 아닙니다.
    - 배열 이름은 상수 포인터로 첫 번째 원소를 가리킵니다!
- 포인터 산술 연산은 포인터 타입에 따라 이동 단위가 달라지므로 주의하세요.
- 배열을 함수로 전달하면 배열이 아니라 **포인터**로 전달됩니다.
    - 배열 크기가 필요하면 별도로 전달하세요.
- 동적 메모리 할당 시 `delete[]`를 반드시 사용해 메모리를 해제하세요.
    - 하지만, 이러면 실수를 할 수 있으니 **기왕이면** `std::vector`를 사용하세요!

## 참조자(Reference)

- c++에서 참조자(Reference) 는 기존 변수(혹은 객체)에 대한 별칭을 제공하는 개념이에요.

```c++
int val = 7;
int& ref = val;   // ref는 val의 별칭!
```

이렇게 코드를 작성하면, ref라는 이름은 val이 저장된 메모리 공간을 참조하게 됩니다. 
여기가 중요한데 ref를 통해 값을 바꾸면 실제 val의 값도 같이 바뀌어요! 
왜냐하면, ref와 val은 완전히 같은 메모리 공간을 공유하니깐요!

```c++
ref = 20; // ref에 20을 대입 -> 20이란 값을 재참조를 하는 것이 아니고 val(ref)에 복사하는 것!
cout << val << endl; // 20이 출력됨!
```
엄밀히 말하자면, 참조자 ref는 포인터처럼 메모리 주소를 직접 들고 있진 않지만, 
내부적으로는 컴파일러가 val을 가리키도록 만들어주는 것이라고 생각하면 됩니다!

_참조자와 포인터의 차이점_

- 참조자는 선언과 동시에 무조건 누군가를 참조해야 합니다. 초기화는 무조건 필수!
비유를 하자면 숙주가 반드시 있어야 하죠! 

```c++
int& r1; // 에러! 참조자는 반드시 초기화가 필요해요!
```

- 즉, 어떤 변수(객체)를 참조하기로 한 번 정해지면 나중에 그 참조 대상을 바꿀 수가 없어요.
- 포인터는 ‘NULL 포인터(nullptr)’가 될 수 있지만, 참조자는 그런 개념이 없어요.
    - 즉, 참조자는 항상 유효한 대상(예: int 변수라면 int 타입 변수를) 가리키고 있어야 해요.
    - 다시 말해서 NULLABLE하지 않기 때문에 코드의 안정성이 더 올라갈 수 있죠!
- 문법적으로 좀 더 단순하고, 별도의 간접 참조 연산을 하지 않아도 됩니다!
    - 포인터가 `*p_val`처럼 값에 접근하는 것과 달리 참조자는 별도의 표기 없이 `ref` 자체가 바로 그 값을 의미하거든요!

그렇다면, 굳이 참조자를 왜 쓸까요?! 가장 흔한 예시로 함수 매개변수로의 활용을 들어볼 수 있어요.

```c++
void increase(int& ref) {
    ref += 10;
}

void increase2(int val) {
    val += 10; // val이라는 매개변수에 값 복사가 되어서 사용이 가능!
}

void increase3(int* p_val) {
		*p_val += 10; // 간접 참조 문법이 은근 귀찮아요.
}

int main() {
    int num = 5;
    increase(num);
    cout << num << endl; // 15 출력
    increase2(num);
    cout << num << endl; // 15 출력. num은 변하지 않음
}
```

위 코드에서 increase 함수는 ref라는 int 참조자를 매개변수로 받아요. 이 경우, ref는 num 변수 자체에 대한 별칭이 되므로
**함수 내부에서 ref를 통해 변경된 값이 num에 직접 적용**됩니다. 이걸 **Call by reference**라고 얘기를 해요!

만약, 참조자가 아니라 위 코드에서 increase2 함수와 같이 값 복사를 하면 지역 변수만 10이 증가하고, 
함수가 끝난 뒤에는 원본 num 변수는 변화가 없겠죠? 이걸 **Call by value**라고 얘기를 해요! 
이게 바로 기본적인 매개변수 전달 방식이라고 생각하시면 되며 원본 값을 변경하고 싶다면 참조자나 
포인터를 명시적으로 사용하라는 의미입니다!

물론, 포인터로도 똑같이 할 수 있지만 (increase3 함수) 매번 `*p_val += 10;`처럼 간접 참조 문법을 써줘야 하니 귀찮고 
코드 가독성도 떨어질 수 있어요. 그래서, 함수 매개변수로 참조자의 형태가 참 많이 활용이 된답니다!

또한, **상수(const) 참조자**를 활용해서 **읽기 전용으로 접근**하는 것도 가능해요!

```c++
void print(const int& ref) {
    cout << ref << endl;
    // 이후에 ref 값을 여기서 변경하려고 하면 변경이 안됨!
}
```

이렇게 하면 함수 내부에서 `ref`를 변경할 수 없으니, 원본 값이 함부로 손상되지 않아 안전합니다.

주의할 점은, **참조자가 가리키는 대상이 사라진 뒤**(스코프를 벗어났거나 동적 할당 메모리를 해제했거나 등)에도 
**참조자를 계속 사용하면 문제**가 생길 수 있어요.

```c++
int& get_ref() {
    int x = 42; // 지역 변수
    return x; // 지역 변수의 참조 반환
}

int main() {
    int& ref = get_ref(); // ref는 소멸한 x를 참조
    std::cout << ref; // 동작을 이상하게 할 수 있음!
}
```

실제로 위의 코드를 실행해보면 42가 출력되는 경우가 대부분일겁니다. 얼핏 봐서는 정상처럼 보이지만 이게 실제론 함수가 반환된 직후에 해당 스택 메모리 위치가 아직 다른 용도로 덮어써지지 않았을 수 있기 때문에 정상처럼 보이는거고 이러한 경우가 보장이 안되면 이상 행동을 할 수 있어요!

그러면, 이제 참조자에 대해 한 번 정리해볼까요?

- 참조자는 별칭을 제공하는 문법으로, 가독성과 편의성을 향상시키는 데 유용해요.
- 선언 시 반드시 유효한 대상을 초기화해야 하며, 한 번 참조 대상을 정하면 변경할 수 없어요. **낙장불입!**
- 함수 매개변수에 참조자를 사용하는 패턴이 아주 많고, 큰 객체를 매개변수로 넘길 때 복사가 일어나지 않아 **성능 면에서도 유리**할 수 있다는 점!

마지막으로, 언제 포인터를 쓰고 언제 참조자를 쓰는게 좋을까요?

<aside>
💡

**동적 메모리 관리를 해야되거나 재할당을 해야되는게 아니면 참조자부터 고려하자!**

</aside>

우선, 참조자가 편리한 건 사실이지만 참조자는 무조건 유효한 대상만을 참조해야 하고 대상도 변경 못한다는 제약이 있어요!

반면, 포인터는 유효/무효(Null) 상태를 구분할 수도 있고 가리키는 대상을 바꾸는 연산도 자유롭죠. 
이러한 특성 때문에 동적 메모리를 할당할 때엔 포인터 타입의 변수를 많이 씁니다. 

- **동적 메모리 할당을 해야만 하는 경우**
    
    동적 메모리를 할당해야 하는 상황은 앞서 언급한 것처럼 **1) 컴파일 시점에 크기를 알 수 없는 경우**, 또는 **2)
  객체의 생존 주기를 명시적으로 관리해야 하는 경우**입니다.
    
    참조자는 본인이 가리키는 객체의 생존 주기에 종속되지만,
  포인터는 **언제 메모리를 할당하거나 해제할지를 개발자가 직접 결정**할 수 있어요! 그래서,
  데이터가 함수가 끝난 뒤에도 계속 유지가 되어야 하는 경우라면 주로 포인터를 많이 써요!
    

 상황에 따라 올바른 도구를 사용해야 한다는 점, 잊지 마세요!

## 문제 풀어보기!

다음 배열의 값이 어떻게 출력될 지 손으로 풀고 알려주세요! (5분)

```c++
int arr[] = {10, 20, 30, 40, 50};
int* ptr = arr;
ptr += 2;
ptr[1] = *ptr + 10;
// 과연 arr의 값은 어떻게 채워져 있을까요?
```

정답!_10, 20, 30, 40, 50_ 

int* ptr = arr;
에서
ptr
은 배열
arr
의 첫 번째 요소를 가리킵니다.

ptr += 2;
를 통해
ptr
은 이제
arr[2]
즉, 값이
30
인 요소를 가리킵니다.

ptr[1] = *ptr + 10;
에서
*ptr
은 현재
ptr
이 가리키고 있는 값, 즉
30
입니다. 따라서
ptr[1]
은
ptr
이 가리키는 다음 요소, 즉
arr[3]
에 해당합니다.

이제
ptr[1]
에
30 + 10
즉
40
을 대입하게 됩니다.

결과적으로, 배열
arr
의 값은 다음과 같이 변경됩니다:

arr[] = {10, 20, 30, 40, 50}; // arr[3]의 값이 40으로 변경됨
따라서 최종적으로
arr
의 값은
{10, 20, 30, 40, 50}
로 남아있습니다.

arr[3]
의 값이 원래
40
이었기 때문에 변화가 없습니다.

다음 코드에서 a, b의 최종 값을 유추해주세요! (5분)

```c++
void foo(int& x, int* y) {
    x = *y;
    *y = 30;
    y = &x;
    *y = 40;
}

int main() {
    int a = 10;
    int b = 20;
		foo(a, &b);
    // a와 b의 최종값은 무엇일까요?
}
```

정답!_a=40,b=30_

main
함수에서
int a = 10;
과
int b = 20;
으로 두 개의 정수 변수를 선언하고 초기화합니다.

foo(a, &b);
를 호출하면서
a
의 주소를 참조로 전달하고,
b
의 주소를 포인터로 전달합니다.
이제
foo
함수 내부에서 어떤 일이 일어나는지 살펴보겠습니다:

x = *y;
에서
y
는
b
의 주소를 가리키고 있으므로,
*y
는
b
의 값인 20입니다. 따라서
x
(즉,
a
)는 20으로 변경됩니다. 이제
a
의 값은 20입니다.

다음으로
*y = 30;
에서
*y
는 여전히
b
를 가리키고 있으므로,
b
의 값이 30으로 변경됩니다. 이제
b
의 값은 30입니다.

그 다음 줄에서
y = &x;
가 실행됩니다. 이 줄은
y
가 이제
a
의 주소를 가리키게 만듭니다. 하지만 이 변경은
foo
함수 내에서만 유효하며,
main
함수의
b
에는 영향을 주지 않습니다.

마지막으로
*y = 40;
에서
*y
는 이제
a
를 가리키고 있으므로,
a
의 값이 40으로 변경됩니다. 이제
a
의 값은 40입니다.

결과적으로,
foo
함수가 실행된 후
a
와
b
의 최종값은 다음과 같습니다:

a
의 최종값: 40
b
의 최종값: 30
따라서,
a
와
b
의 최종값은 각각 40과 30입니다.


    
