# 휴면(NetDormancy)

휴면 상태인 액터는 더 이상 프로퍼티 레플리케이션이나 RPC가 동작하지 않습니다.

액터가 레플리케이트 되었지만, 자주 수정되진 않을 때 활용합니다.
플레이어 캐릭터처럼 자주 수정되는 액터를 휴면 상태로 두면 오히려 오버헤드가 발생합니다.

<img width="607" height="222" alt="스크린샷 2025-10-27 184328" src="https://github.com/user-attachments/assets/a494228c-6622-4ef5-bfef-b2ada5dcc30e" />

## DORM_Initial 실습

아래와 같이 작성 후 BP_Box > Details > PointLight 
BP_Box에서 PointLight 컴포넌트를 적절히 추가해 주고 아래와 같이 작성합니다.

```
// DXBox.h

...
class UPointLightComponent;

UCLASS()
class DEDICATEDX_API ADXBox: public AActor
{
	...
	
public:	
	...
	
private:
	...

	UFUNCTION()
	void OnRep_ServerLightColor();
	
protected:
	...

	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)
	TObjectPtr<UPointLightComponent> PointLight;

	UPROPERTY(ReplicatedUsing = OnRep_ServerLightColor)
	FLinearColor ServerLightColor;

};
```

```
// DXBox.cpp


...
#include "Components/PointLightComponent.h"

ADXBox::ADXBox()
	...
{
	...

	PointLight = CreateDefaultSubobject<UPointLightComponent>(TEXT("PointLight"));
	PointLight->SetupAttachment(SceneRoot);
	
	SetNetDormancy(DORM_Initial);
}

void ADXCube::GetLifetimeReplicatedProps(TArray<class FLifetimeProperty>& OutLifetimeProps) const
{
	...
	DOREPLIFETIME(ThisClass, ServerLightColor);
}

...

void ADXBox::BeginPlay()
{
	Super::BeginPlay();

	DX_LOG_ROLE(LogDXNet, Log, TEXT(""));

	if (HasAuthority() == true)
	{
		FTimerHandle TimerHandle01;
		GetWorld()->GetTimerManager().SetTimer(TimerHandle01, FTimerDelegate::CreateLambda(
			[&]() -> void
			{
				float RandomR = FMath::RandRange(0.f, 1.f);
				float RandomG = FMath::RandRange(0.f, 1.f);
				float RandomB = FMath::RandRange(0.f, 1.f);
				ServerLightColor = FLinearColor(RandomR, RandomG, RandomB, 1.f);
				OnRep_ServerLightColor();
					// 서버에서도 로직이 수행될 수 있게끔 OnRep_() 함수를 명시적으로 호출.
			}), 1.f, true
		);
	}
}

...

void ADXBox::OnRep_ServerLightColor()
{
	if (HasAuthority() == false)
	{
		DX_LOG_NET(LogDXNet, Log, TEXT("OnRep_ServerLightColor(): %s"), *ServerLightColor.ToString());
	}

	PointLight->SetLightColor(ServerLightColor);
}
```

## FlushNetDormancy() 함수 실습

```
// DXBox.cpp


...

void ADXBox::BeginPlay()
{
	...

	if (HasAuthority() == true)
	{
		...
		
		FTimerHandle TimerHandle02;
		GetWorld()->GetTimerManager().SetTimer(TimerHandle02, FTimerDelegate::CreateLambda(
			[&]()->void
			{
				FlushNetDormancy();
			}), 5.f, false
		);
	}
}

...
```

### **Conditional Property Replication**

프로퍼티는 한 번 레플리케이션에 등록되면 다시 해제시킬 수가 없습니다.
그래서 `DOREPLIFETIME`라고 합니다.

`Conditional Property Replication`은 레플리케이션에 등록된 프로퍼티를 
조건식을 통해 좀 더 세밀하게 조정할 수 있게끔 해줍니다. 
단점은 조건식 값이 너무 자주 바뀐다면 오버헤드 발생할 수 있습니다.

## DOREPLIFETIME_CONDITION() 실습

```
// DXBox.cpp


...

ADXBox::ADXBox()
	...
{
	...

	//SetNetDormancy(DORM_Initial);
}

...

void ADXBox::GetLifetimeReplicatedProps(TArray<class FLifetimeProperty>& OutLifetimeProps) const
{
	...
	// DOREPLIFETIME(ThisClass, ServerLightColor);
	DOREPLIFETIME_CONDITION(ThisClass, ServerLightColor, COND_InitialOnly);
}

...

void ADXBox::BeginPlay()
{
	Super::BeginPlay();

	if (HasAuthority() == true)
	{
		...

		//FTimerHandle TimerHandle02;
		//GetWorld()->GetTimerManager().SetTimer(TimerHandle02, FTimerDelegate::CreateLambda(
		//	[&]()->void
		//	{
		//		FlushNetDormancy();
		//	}), 5.f, false
		//);
	}
}

...
```
