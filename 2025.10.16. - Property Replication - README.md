# NetLocalOnClient 속성

레벨 디자인을 통해 레벨에 고정적으로 배치되는 액터는 `NetLoadOnClient` 속성을 `true`로 지정해서 모든 클라이언트가 스스로 스폰하도록 합니다.

플레이어의 로그인에 따라 스폰 되는 액터(PlayerController, Pawn 등)와 혼동하지 않으셔야 합니다.

<img width="685" height="388" alt="스크린샷 2025-10-16 183015" src="https://github.com/user-attachments/assets/10c6a04a-42d3-4a1f-aa4b-bef698f54f8d" />

## NetLoadOnClient 속성 실습

- `새 C++ 클래스 > Actor 부모 클래스 > “DXBox”`
- Path: `Gimmick`
- `Content Browser > Content > DedicatedX > Blueprint > 새 폴더 “Gimmick”`
- `Gimmick > 새 Blueprint 에셋 > DXBox 부모 클래스 > “BP_Box”` 생성합니다.
- `BP_Box > Details > Static Mesh`에 `SM_Cube` 지정합니다.
- 레벨에 `BP_Cube`를 적절히 배치합니다.
- `BP_Box > Details > NetLoadOnClient` 속성을 `false/true`로 바꿔가며 PIE 진행 비교합니다.

```
// DXBox.h
#pragma once

#include "GameFramework/Actor.h"
#include "DXBox.generated.h"

class USceneComponent;
class UStaticMeshComponent;

UCLASS()
class DEDICATEDX_API ADXBox : public AActor
{
	GENERATED_BODY()
	
public:	
	ADXBox();

	virtual void BeginPlay() override;

protected:
	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)
	TObjectPtr<USceneComponent> SceneRoot;
	
	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)
	TObjectPtr<UStaticMeshComponent> Mesh;

};
```

```
// DXBox.cpp
#include "DXBox.h"

#include "DedicatedX.h"


ADXBox::ADXBox()
{
	PrimaryActorTick.bCanEverTick = false;

	SceneRoot = CreateDefaultSubobject<USceneComponent>(TEXT("SceneRoot"));
	SetRootComponent(SceneRoot);

	Mesh = CreateDefaultSubobject<UStaticMeshComponent>(TEXT("Mesh"));
	Mesh->SetupAttachment(SceneRoot);
	Mesh->SetRelativeLocation(FVector(-50.f, -50.f, 50.f));
}

void ADXBox::BeginPlay()
{
	Super::BeginPlay();

	DX_LOG_ROLE(LogDXNet, Log, TEXT(""));
}
```

## Property Replication 복습

```
// DXBox.h

...
class DEDICATEDX_API ADXBox : public AActor
{
	GENERATED_BODY()
	
public:	
	...
	
	virtual void GetLifetimeReplicatedProps(TArray<class FLifetimeProperty>& OutLifetimeProps) const override;

	virtual void Tick(float DeltaSeconds) override;

protected:
	...

	UPROPERTY(Replicated)
	float ServerRotationYaw;

	float RotationSpeed;

};
```

```
// DXBox.cpp


...
#include "Net/UnrealNetwork.h"


ADXBox::ADXBox()
	: ServerRotationYaw(0.0f)
	, RotationSpeed(30.0f)
{
	PrimaryActorTick.bCanEverTick = true;
	bReplicates = true;

	...
}

...

void ADXBox::GetLifetimeReplicatedProps(TArray<class FLifetimeProperty>& OutLifetimeProps) const
{
	Super::GetLifetimeReplicatedProps(OutLifetimeProps);

	DOREPLIFETIME(ThisClass, ServerRotationYaw);
}

void ADXBox::Tick(float DeltaSeconds)
{
	Super::Tick(DeltaSeconds);

	if (HasAuthority() == true)
	{
		AddActorLocalRotation(FRotator(0.f, RotationSpeed * DeltaSeconds, 0.f));
		ServerRotationYaw = RootComponent->GetComponentRotation().Yaw;
	}
	else
	{
		SetActorRotation(FRotator(0.f, ServerRotationYaw, 0.f));
	}
}
```

# **Replication Notify**

위 예제에서는 매 틱마다 클라이언트에 복제된 값을 읽어왔습니다.

이는 비효율적입니다.
클라이언트에 속성 값이 레플리케이션 될 때마다 호출되는 함수가 있습니다.

`ReplicatedUsing` 키워드를 통해 정의 가능합니다.
해당 콜백함수는 `OnRep_` 접두사가 꼭 붙어야 하고, `UFUNCTION()` 키워드가 붙어야 합니다.

## Replication Notify 실습

```
// DXBox.h

...
class DEDICATEDX_API ADXBox : public AActor
{
	...
	
public:	
	...

private:
	UFUNCTION()
	void OnRep_ServerRotationYaw();

protected:
	...

	//UPROPERTY(Replicated)
	UPROPERTY(ReplicatedUsing = OnRep_ServerRotationYaw)
	float ServerRotationYaw;

	...

};
```

```
// DXBox.cpp


...

ADXBox::ADXBox()
	...
{
	...
}

...

void ADXBox::Tick(float DeltaSeconds)
{
	...

	if (HasAuthority() == true)
	{
		...
	}
	else
	{
		//SetActorRotation(FRotator(0.f, ServerRotationYaw, 0.f));
	}
}

void ADXBox::OnRep_ServerRotationYaw()
{
	DX_LOG_NET(LogDXNet, Log, TEXT("OnRep_ServerRotationYaw(): %f"), ServerRotationYaw);

	SetActorRotation(FRotator(0.f, ServerRotationYaw, 0.f));
		// 지금은 큰 차이 없긴함. Tick() 함수에서 매 틱 마다 ServerRotationYaw 값이 수정되기 때문.
		// 하지만 추후에 매 틱마다 수정하지 않거나, 레플리케이션 주기를 낮춘다면 상당한 효과를 거둘 수 있음.
}
```

### **Replication Notify의 특징**

- 서버에서는 실행되지 않고, 클라이언트에서만 실행됩니다.
- 서버에서도 해당 로직이 실행되어야 한다면, 명시적으로 호출해 줘야 합니다.

### **C++ `OnRep_` 함수 VS `Blueprint RepNotify` 함수**

- 속성 값이 변경되어서 변경된 값이 클라이언트에 복제되는 시점에 콜백함수를 바인드할 수 있습니다.
- 이것을 `Replication Notify` 함수라고 합니다. C++에서는 `OnRep_`, 블루프린트에선 `RepNotify`

<img width="416" height="178" alt="스크린샷 2025-10-16 183351" src="https://github.com/user-attachments/assets/42de6b8b-7121-4d6f-ba75-eafd7bbd650e" />
