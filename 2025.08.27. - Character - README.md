- Character
    - 5.1 Enhanced Input System
        - 기존 입력 시스템 Vs. Enhanced Input System
        기존 입력 시스템은 입력 관련 설정을 해둔 뒤에 게임 로직에서 입력 값을 처리함.
        이런 방식은 게임 플레이 중에 플레이어의 키세팅 변경에 대응할 수 없음.
        그래서 Enhanced Input System은 입력 관련 설정을 플레이 중에도 변경 할 수 있게끔 함.
        이외에도 다양한 기능들이 추가적으로 지원됨.
        
        - Enhanced Input System의 동작
        사용자의 입력 데이터를 최종 함수에 매핑하는 과정을 체계적으로 구성.
        입력을 처리하는 주체는 액션. 입력과 액션이 연결됨.
        액션에서는 들어온 입력을 어떻게 재가공(Modifier)할지 
        어떻게 입력 이벤트를 활성화(Trigger)할지의 설정 진행.
        마지막으로 게임 로직에서 작성한 특정 함수와 바인드 진행.
          플랫폼에 따른 다양한 입력 장치의 설정
          ex) 게임 패드용 입력 매핑 컨텍스트, 키보드용 입력 매핑 컨텍스트
          입력 값의 변환(Modifier)
          ex) AD/WS 입력 값을 Y축과 X축으로 변경. 값의 반전 처리..
          이벤트 발생 조건의 상세 설정(Trigger)
          ex) 일반 버튼인가, 축 이동인가, 일정 이상 눌려야하는가, …
            
        - [실습] Enhanced Input System 관련 플러그인과 모듈 추가
            
            플러그인을 추가하고자 한다면 .uproject 파일을 수정해야함. 프로젝트 폴더에 있음.
            A 모듈에 B 모듈을 추가하고자 한다면 A 모듈의 .Build.cs 파일을 수정해야함
            프로젝트 폴더 > Source > A 모듈 이름의 폴더에서 찾으면 됨.
            지금은 StudyProject 모듈에 EnhancedInput 모듈을 추가하고자 하므로,
            프로젝트 폴더 > Source > StudyProject 폴더에서 StudyProject.Build.cs 파일을 수정.
            
            ```cpp
            // ShooterX.uproject
            
            {
            	"FileVersion": 3,
            	"EngineAssociation": "5.5",
            	"Category": "",
            	"Description": "",
            	"Modules": [
            		{
            			"Name": "ShooterX",
            			"Type": "Runtime",
            			"LoadingPhase": "Default",
            			"AdditionalDependencies": [
            				"Engine",
            				"CoreUObject"
            			]
            		}
            	],
            	"Plugins": [
            		{
            			"Name": "ModelingToolsEditorMode",
            			"Enabled": true,
            			"TargetAllowList": [
            				"Editor"
            			]
            		},
            		{
            			"Name": "EnhancedInput",
            			"Enabled": true
            		}
            	]
            }
            ```
            
            ```cpp
            // ShooterX.Build.cs
            
            ...
            
            public class ShooterX : ModuleRules
            {
            	public ShooterX(ReadOnlyTargetRules Target) : base(Target)
            	{
            		...
            
            		PublicDependencyModuleNames.AddRange(new string[]
            			{ 
            				...
            
            				// Input
            				"EnhancedInput",
            			}
            		);
            
            		...
            	}
            }
            
            ```
            
        
        - [실습] InputAction 애셋 생성
            
            Content Browser > ShooterX 우클릭 > 새 폴더 “Input”
            Input 폴더 우클릭 > 새 폴더 “InputAction”
            InputAction > 새 Input 애셋 > Input Action > “IA_Move” 생성
            IA_Move는 Axis Mapping임. 이전 단원에서 다룬 UpDown LeftRight를 합친 2D 값.
            따라서 Value Type에 Vector2D 지정.
            
        
        - [실습] InputConfig 애셋 생성
            
            새 C++ 클래스 > DataAsset 부모 클래스 > “SXInputConfig”
            Path: Input
            InputConfigData는 레벨과 폰에 따라 별도로 만들기도 함.
            이번 강의에서는 SXInputConfig 하나로 모두 해결하고자 함. 
            
            ```cpp
            // SXInputConfig.h
            
            #pragma once
            
            #include "CoreMinimal.h"
            #include "Engine/DataAsset.h"
            #include "SXInputConfig.generated.h"
            
            class UInputAction;
            
            /**
             *
             */
            UCLASS()
            class SHOOTERX_API USXInputConfig : public UDataAsset
            {
            	GENERATED_BODY()
            
            public:
            	UPROPERTY(EditAnywhere, BlueprintReadOnly)
            	TObjectPtr<UInputAction> Move;
            
            };
            
            ```
            
            ```cpp
            // SXInputConfig.cpp
            
            #include "Input/SXInputConfig.h"
            
            ```
            
        
        - [실습] InputConfig 블루프린트 애셋 생성
            
            Content Browser > ShooterX > Input 우클릭 > 새 폴더 “InputConfig”
            InputConfig > 새 Miscellaneous 애셋 > Data Asset > SXInputConfig 부모 클래스
            “IC_PlayerCharacter” 생성.
            IC_PlayerCharacter > Details > InputActions에 IA_Move 추가.
            
        
        - [실습] InputMappingContext 생성
            
            Content Browser > StudyProject > Input 우클릭 > 새 폴더 “InputMappingContext”
            InputMappingContext 우클릭 > 새 Input 애셋 > Input Mapping Context 
            “IMC_PlayerCharacter” 생성.
            아래 그림을 참고하여 설정. 
            
           <img width="435" height="589" alt="스크린샷 2025-08-27 194420" src="https://github.com/user-attachments/assets/c52b843a-4836-4f4b-aa63-d341d6c6e13e" />
        
    - 5.2 캐릭터 Vs. 폰
        - ACharacter Vs. APawn
        인간형 폰을 좀 더 효과적으로 구현하기 위한 클래스가 캐릭터 클래스.
        ACharacter 클래스도 APawn 클래스를 상속 받고 있음.
        이전에 만든 예제와 비교해서, Capsule Component와 Skeletal Mesh Component는 같지만
        CharacterMovementComponent를 사용한다는 점이 다름.
        
        - CharacterMovementComponent Vs. FloatingPawnMovementComponent
        CharacterMovementComponent는 점프와 같은 중력의 영향을 받는 움직임 제공.
        걷기 이외에도 기어가기, 날아가기, 수영하기 등의 다양한 움직임 모드를 제공.
        멀티 플레이 환경에서 캐릭터들 간의 움직임을 자동으로 동기화함.
        
        - 캐릭터 무브먼트 컴포넌트의 움직임 모드(EMovementMode)
        Movement Mode: None, Walking, Falling, …
        None: 이동 기능을 끄고 싶을 때
        MaxWalkSpeed: 이동 모드에서의 이동속도
        JumpZVelocity: Falling 모드에서의 점프속도
        
        - [실습] 캐릭터 베이스 클래스 생성
            
            캐릭터도 이후에 PlayerCharacter와 NonPlayerCharacter로 나뉨.
            이 둘의 부모 클래스를 만들어두고자 함.
            
            새 C++ 클래스 > Character 부모 클래스 > “SXCharacterBase”
            Path: Character
            
            ```cpp
            // SXCharacterBase.h
            
            #pragma once
            
            #include "CoreMinimal.h"
            #include "GameFramework/Character.h"
            #include "SXCharacterBase.generated.h"
            
            UCLASS()
            class SHOOTERX_API ASXCharacterBase : public ACharacter
            {
            	GENERATED_BODY()
            
            public:
            	ASXCharacterBase();
            
            };
            
            ```
            
            ```cpp
            // SXCharacterBase.cpp
            
            #include "Character/SXCharacterBase.h"
            #include "Components/CapsuleComponent.h"
            #include "GameFramework/CharacterMovementComponent.h"
            
            ASXCharacterBase::ASXCharacterBase()
            {
            	PrimaryActorTick.bCanEverTick = true;
            
            	float CharacterHalfHeight = 90.f;
            	float CharacterRadius = 40.f;
            
            	GetCapsuleComponent()->InitCapsuleSize(CharacterRadius, CharacterHalfHeight);
            
            	FVector PivotPosition(0.f, 0.f, -CharacterHalfHeight);
            	FRotator PivotRotation(0.f, -90.f, 0.f);
            	GetMesh()->SetRelativeLocationAndRotation(PivotPosition, PivotRotation);
            
            	GetCharacterMovement()->MaxWalkSpeed = 350.f;
            	GetCharacterMovement()->MinAnalogWalkSpeed = 20.f;
            	GetCharacterMovement()->JumpZVelocity = 700.f;
            	GetCharacterMovement()->AirControl = 0.35f;
            	GetCharacterMovement()->BrakingDecelerationWalking = 2000.f;
            }
            
            ```
            
        
    - 5.3 Control Rotation
        - 시점 구현 선행 지식
        Backview 혹은 Quarterview 같은 시점 구현을 위해 컨트롤 로테이션에 대한 개념이 필요.
        시점 구현 중에 나오는 함수들은 차량 탑승 혹은 관전자 등의 기능을 구현할때도 필요함.
        
        - 플레이어 컨트롤러의 컨트롤 로테이션
        게임 세계의 물리적인 요소가 아직 고려되지 않은
        플레이어의 의지 관련 데이터를 관리함.
        플레이어의 의지의 대표적인 속성이 컨트롤 로테이션.
            
            <img width="431" height="246" alt="스크린샷 2025-08-27 194501" src="https://github.com/user-attachments/assets/84a8c3d2-88c9-412b-92f6-2273597f7abc" />

        - 폰의 속도
        게임 세계에서는 물리적인 제약이 있기 때문에
        현재 캐릭터(폰)가 처한 물리적인 상황이 고려됨.
        대표적인 속성으로는 속도.
        
        - [실습] 캐릭터 클래스 생성
            
            새 C++ 클래스 > SXCharacterBase 부모 클래스 > “SXPlayerCharacter”
            Path: Character
            
            ```cpp
            // SXPlayerCharacter.h
            
            #pragma once
            
            #include "CoreMinimal.h"
            #include "Character/SXCharacterBase.h"
            #include "InputActionValue.h"
            #include "SXPlayerCharacter.generated.h"
            
            class USpringArmComponent;
            class UCameraComponent;
            class USXInputConfig;
            class UInputMappingContext;
            
            /**
             * 
             */
            UCLASS()
            class SHOOTERX_API ASXPlayerCharacter : public ASXCharacterBase
            {
            	GENERATED_BODY()
            
            #pragma region Override ACharacter
            
            public:
            	ASXPlayerCharacter();
            
            	virtual void BeginPlay() override;
            
            protected:
            	virtual void SetupPlayerInputComponent(UInputComponent* PlayerInputComponent) override;
            
            protected:
            	UPROPERTY(EditAnywhere, BlueprintReadOnly, meta = (AllowPrivateAccess))
            	TObjectPtr<USpringArmComponent> SpringArmComponent;
            
            	UPROPERTY(EditAnywhere, BlueprintReadOnly, meta = (AllowPrivateAccess))
            	TObjectPtr<UCameraComponent> CameraComponent;
            
            #pragma endregion
            
            #pragma region Input
            
            private:
            	void InputMove(const FInputActionValue& InValue);
            
            protected:
            	UPROPERTY(EditAnywhere, BlueprintReadOnly, meta = (AllowPrivateAccess))
            	TObjectPtr<USXInputConfig> PlayerCharacterInputConfig;
            
            	UPROPERTY(EditAnywhere, BlueprintReadOnly, meta = (AllowPrivateAccess))
            	TObjectPtr<UInputMappingContext> PlayerCharacterInputMappingContext;
            
            #pragma endregion	
            
            };
            
            ```
            
            ```cpp
            // SXPlayerCharacter.cpp
            
            #include "Character/SXPlayerCharacter.h"
            #include "GameFramework/SpringArmComponent.h"
            #include "Camera/CameraComponent.h"
            #include "EnhancedInputComponent.h"
            #include "EnhancedInputSubsystems.h"
            #include "Input/SXInputConfig.h"
            #include "Kismet/KismetSystemLibrary.h"
            
            ASXPlayerCharacter::ASXPlayerCharacter()
            {
            	PrimaryActorTick.bCanEverTick = false;
            
            	SpringArmComponent = CreateDefaultSubobject<USpringArmComponent>(TEXT("SpringArmComponent"));
            	SpringArmComponent->SetupAttachment(RootComponent);
            	SpringArmComponent->TargetArmLength = 400.f;
            
            	CameraComponent = CreateDefaultSubobject<UCameraComponent>(TEXT("CameraComponent"));
            	CameraComponent->SetupAttachment(SpringArmComponent);
            }
            
            void ASXPlayerCharacter::BeginPlay()
            {
            	Super::BeginPlay();
            
            	APlayerController* PlayerController = Cast<APlayerController>(GetController());
            	if (IsValid(PlayerController) == true)
            	{
            		UEnhancedInputLocalPlayerSubsystem* Subsystem = ULocalPlayer::GetSubsystem<UEnhancedInputLocalPlayerSubsystem>(PlayerController->GetLocalPlayer());
            		if (IsValid(Subsystem) == true)
            		{
            			Subsystem->AddMappingContext(PlayerCharacterInputMappingContext, 0);
            		}
            	}
            }
            
            void ASXPlayerCharacter::SetupPlayerInputComponent(UInputComponent* PlayerInputComponent)
            {
            	Super::SetupPlayerInputComponent(PlayerInputComponent);
            
            	UEnhancedInputComponent* EnhancedInputComponent = Cast<UEnhancedInputComponent>(PlayerInputComponent);
            	if (IsValid(EnhancedInputComponent) == true)
            	{
            		EnhancedInputComponent->BindAction(PlayerCharacterInputConfig->Move, ETriggerEvent::Triggered, this, &ThisClass::InputMove);
            	}
            }
            
            void ASXPlayerCharacter::InputMove(const FInputActionValue& InValue)
            {
            	FVector2D MovementVector = InValue.Get<FVector2D>();
            
            	AddMovementInput(GetActorForwardVector(), MovementVector.X);
            	AddMovementInput(GetActorRightVector(), MovementVector.Y);
            
            	//UKismetSystemLibrary::PrintString(this, FString::Printf(TEXT("X: %.3f, Y: %.3f"), MovementVector.X, MovementVector.Y));
            }
            
            ```
            
        
        - [실습] 캐릭터 블루프린트 애셋 생성
            
            Content Browser > ShooterX > Blueprint > Character > 새 Blueprint 애셋
            SXPlayerCharacter 부모 클래스 > “BP_PlayerCharacter” 생성
            BP_PlayerCharacter > Details > InputConfigData와 InputMappingContext에
            IC_PlayerCharacter와 IMC_PlayerCharacter 지정.
            Override Input Component Class에는 EnhancedInputComponent 지정.
            Skeletal Mesh Asset에는 SKM_Manny 지정.
            
            <img width="423" height="223" alt="스크린샷 2025-08-27 194535" src="https://github.com/user-attachments/assets/3eb9629b-5f81-4563-b238-b5673228f71b" />

        
        - [실습] 새로 만든 BP_PlayerCharacter 애셋 등록
            
            BP_GameMode > Details > Default Pawn Class에 BP_PlayerCharacter 지정.
            
        
        - [실습] GameMode 클래스에 C++로 하드 코딩해서 애셋 지정하기
            
            ```cpp
            // SXGameMode.cpp
            
            ...
            
            ASXGameMode::ASXGameMode()
            {
                ...
            
                /*
                static ConstructorHelpers::FClassFinder<ASXPlayerCharacter> DefaultPlayerCharacterClassRef(TEXT("오브젝트패스"));
                if (DefaultPlayerCharacterClassRef.Class)
                {
                    DefaultPawnClass = DefaultPlayerCharacterClassRef.Class;
                }
                */
            }
            ```
            
        
        - [실습] 컨트롤 로테이션 실습 준비
            
            Content Browser > InputAction > 새 Input 애셋 > Input Action > “IA_Look”
            Value Type에 Vector2D
            InputConfig > IC_PlayerCharacter에도 IA_Look을 추가 하기 위해서 
            USXInputConfig 클래스를 아래와 같이 수정하고 빌드. 빌드 성공하면 IA_Look 연결.
            
            ```cpp
            // SXInputConfig.h
            
            ...
            class SHOOTERX_API USXInputConfig : public UDataAsset
            {
            	...
            
            public:
            	...
            
            	UPROPERTY(EditAnywhere, BlueprintReadOnly)
            	TObjectPtr<UInputAction> Look;
            
            };
            
            ```
            
            <img width="436" height="769" alt="스크린샷 2025-08-27 194600" src="https://github.com/user-attachments/assets/a965913b-0b5b-4e3f-9724-b3c287631e09" />

        
        - [실습] 컨트롤 로테이션
        마우스 움직임에 따라 폰이 회전해야 할 최종 목표 회전 값을 구하고자 함.
        이를 향해 폰이 일정한 속도로 회전하는 기능을 구현함.
            
            ```cpp
            // SXPlayerCharacter.h
            
            ...
            class SHOOTERX_API ASXPlayerCharacter : public ASXCharacterBase
            {
            	...
            
            #pragma region Input
            
            private:
            	...
            
            	void InputLook(const FInputActionValue& InValue);
            
            protected:
            	...
            
            #pragma endregion	
            
            };
            
            ```
            
            ```cpp
            // SXPlayerCharacter.cpp
            
            ...
            
            void ASXPlayerCharacter::SetupPlayerInputComponent(UInputComponent* PlayerInputComponent)
            {
            	...
            	if (IsValid(EnhancedInputComponent) == true)
            	{
            		EnhancedInputComponent->BindAction(PlayerCharacterInputConfig->Move, ETriggerEvent::Triggered, this, &ThisClass::InputMove);
            		EnhancedInputComponent->BindAction(PlayerCharacterInputConfig->Look, ETriggerEvent::Triggered, this, &ThisClass::InputLook);
            	}
            }
            
            ...
            
            void ASXPlayerCharacter::InputLook(const FInputActionValue& InValue)
            {
            	if (IsValid(GetController()) == true)
            	{
            		FVector2D LookVector = InValue.Get<FVector2D>();
            
            		AddControllerYawInput(LookVector.X);
            		AddControllerPitchInput(LookVector.Y);
            	}
            }
            
            ```
            
        
        - Viewport에서 컨트롤 로테이션 값을 확인하는 방법
        뷰포트를 클릭 후 틸드(~) 키를 눌러서 콘솔창 진입
        아래와 같은 명령어를 작성하고 엔터
        DisplayAll PlayerController ControlRotation
        
        - Controller와 Pawn, 그리고 컴포넌트 간의 관계
        - **Controller <-> Pawn**
          bUseControllerRotation- 속성에 의해 결정됨.
          ex) Character 클래스는 기본적으로 bUseControllerRotationYaw 속성이 true임.
            그래서 이전 예제에서 좌우 회전이 가능했음. 
            컨트롤 로테이션의 Yaw 값이 캐릭터의 Rotation Yaw 값에 즉시 동기화.
        - **Pawn <-> SpringArmComponent**
          bUsePawnControlRotation 속성에 의해서 폰의 회전 값이 연동될지 안될지 결정됨.
          ex) bUsePawnControlRotation이 false라면 폰이 회전해도 스프링암은 회전하지 않음.
            컷씬을 촬영할 때, 플레이어의 회전에도 스프링암이 움직이지 않았으면 할때 활용 가능.
          bDoCollisionTest 속성은 폰과 카메라 사이에 벽이 있을 경우 카메라를 폰쪽으로 당김.
          bInherit- 속성에 의해서 Root Component의 회전 값이 연동될지 안될지 결정.
        - **Pawn <-> CameraComponent**
          bUsePawnControlRotation 속성에 의해서 폰의 회전 값이 연동 될지 안될지 결정되지만
          지금은 SpringArmComponent의 자식 Component라서, SpringArmComponent에 따름.
            
  
        - CharacterMovement Component의 회전 옵션
        - **RotationRate**
          회전 속도
        - **UseControllerDesiredRotation**
          컨트롤 로테이션에 폰의 로테이션을 RotationRate 속도로 동기화
        - **OrientRotationToMovement**
          이동 방향에 폰의 로테이션을 RotationRate 속도로 동기화
          폰의 회전 옵션과 충돌나면 움직임이 이상해지므로 주의해야함.
        
        - 마우스 감도 조절
        UE4 이하 버전에서는 InputPitchScale, InputYawScale, InputRollScale으로 조절했음.
        UE5 이상 버전에서는 Deprecated.
        따라서 Look() 함수의 인자로 전달되는 값에 계수를 곱하면서 마우스 감도 조절.
        
    - 5.4 BackView
        - [실습] BackView 구현
            
            ```cpp
            // SXPlayerCharacter.h
            
            ...
            
            UENUM(BlueprintType)
            enum class EViewMode : uint8
            {
            	None,
            	BackView,
            	End
            };
            
            /**
             * 
             */
            UCLASS()
            class SHOOTERX_API ASXPlayerCharacter : public ASXCharacterBase
            {
            	...
            
            #pragma region Override ACharacter
            
            public:
            	...
            
            	virtual void PossessedBy(AController* NewController) override;
            
            protected:
            	...
            
            #pragma endregion
            
            ...
            
            #pragma region ViewMode
            
            public:
            	void SetViewMode(EViewMode InViewMode);
            
            protected:
            	EViewMode CurrentViewMode = EViewMode::None;
            
            #pragma endregion
            
            };
            
            ```
            
            ```cpp
            // SXPlayerCharacter.cpp
            
            ...
            
            void ASXPlayerCharacter::PossessedBy(AController* NewController)
            {
            	Super::PossessedBy(NewController);
            
            	SetViewMode(EViewMode::BackView);
            }
            
            ...
            
            void ASXPlayerCharacter::SetViewMode(EViewMode InViewMode)
            {
            	if (CurrentViewMode == InViewMode)
            	{
            		return;
            	}
            
            	CurrentViewMode = InViewMode;
            
            	switch (CurrentViewMode)
            	{
            	case EViewMode::BackView:
            		bUseControllerRotationPitch = false;
            		bUseControllerRotationYaw = true;
            		bUseControllerRotationRoll = false;
            
            		SpringArmComponent->TargetArmLength = 400.f;
            		SpringArmComponent->SetRelativeRotation(FRotator::ZeroRotator);
            		// ControlRotation이 Pawn의 회전과 동기화 -> Pawn의 회전이 SpringArm의 회전 동기화. 이로 인해 SetRotation()이 무의미.
            
            		SpringArmComponent->bUsePawnControlRotation = true;
            
            		SpringArmComponent->bInheritPitch = true;
            		SpringArmComponent->bInheritYaw = true;
            		SpringArmComponent->bInheritRoll = false;
            
            		SpringArmComponent->bDoCollisionTest = true;
            
            		break;
            	case EViewMode::None:
            	case EViewMode::End:
            	default:
            		break;
            	}
            }
            
            ```
            
        
        - BackView에 적합한 이동 구현
        위 코드를 컴파일 하고 PIE 해보면 이동이 부적절함.
        대표적인 백뷰 게임으로 GTA가 있음. GTA의 폰 이동을 생각해보면,
        컨트롤 로테이션에 의해 카메라가 회전 한 뒤 이동하면 해당 방향으로 이동함.
        
        - 카메라 방향 얻어오기
        bUsePawnControlRotation 속성이 현재 true.
        이 때문에 컨트롤 로테이션은 “결국” 스프링암 컴포넌트의 로테이션에 동기화 되고 있음.
        컨트롤 로테이션이 결국 카메라의 방향이 되고 있음.
        
        - 카메라 회전 행렬 만들기
        컨트롤 로테이션의 Pitch 값은 의미가 없음.
        왜냐하면 Pitch가 변화한다고 해서 그쪽으로 이동되면 안됨. 회전되도 안됨. 
        전투기 게임이 아니기 때문. 그래서 컨트롤 로테이션의 Yaw 값만으로 회전 행렬을 만들고자 함.
        
        - [실습] 이동 방향 구하기
            
            컨트롤 로테이션으로부터 회전 행렬을 생성 후 해당 행렬의 축 성분을 추출.
            이때 X축 성분이 Forward Vector. Y축 성분이 Right Vector.
            
            ```cpp
            // ASXPlayerCharacter.cpp
            
            ...
            
            void ASXPlayerCharacter::InputMove(const FInputActionValue& InValue)
            {
            	FVector2D MovementVector = InValue.Get<FVector2D>();
            
            	//AddMovementInput(GetActorForwardVector(), MovementVector.X);
            	//AddMovementInput(GetActorRightVector(), MovementVector.Y);
            
            	switch (CurrentViewMode)
            	{
            	case EViewMode::BackView:
            	{ // Switch-Case 구문 내에서 Scope를 지정하면 해당 Scope 내에서 변수 선언이 가능해짐.
            		const FRotator ControlRotation = GetController()->GetControlRotation();
            		const FRotator ControlRotationYaw(0.f, ControlRotation.Yaw, 0.f);
            
            		const FVector ForwardVector = FRotationMatrix(ControlRotationYaw).GetUnitAxis(EAxis::X);
            		const FVector RightVector = FRotationMatrix(ControlRotationYaw).GetUnitAxis(EAxis::Y);
            
            		AddMovementInput(ForwardVector, MovementVector.X);
            		AddMovementInput(RightVector, MovementVector.Y);
            
            		break;
            	}
            	case EViewMode::None:
            	case EViewMode::End:
            	default:
            		AddMovementInput(GetActorForwardVector(), MovementVector.X);
            		AddMovementInput(GetActorRightVector(), MovementVector.Y);
            		break;
            	}
            }
            
            void ASXPlayerCharacter::InputLook(const FInputActionValue& InValue)
            {
            	/*
            	if (IsValid(GetController()) == true)
            	{
            		FVector2D LookVector = InValue.Get<FVector2D>();
            
            		AddControllerYawInput(LookVector.X);
            		AddControllerPitchInput(LookVector.Y);
            	}
            	*/
            
            	FVector2D LookVector = InValue.Get<FVector2D>();
            
            	switch (CurrentViewMode)
            	{
            	case EViewMode::BackView:
            		AddControllerYawInput(LookVector.X);
            		AddControllerPitchInput(LookVector.Y);
            		break;
            	case EViewMode::None:
            	case EViewMode::End:
            	default:
            		break;
            	}
            }
            ```
            
        
        - [실습] bOrientRotationToMovement 속성
            
            캐릭터가 시선 방향으로 이동은 되지만 회전은 안되서 어색함.
            월드의 X축을 향해 바라보면서 이동.
            캐릭터가 움직이는 방향으로 회전 시켜주는 bOrientRotationToMovement 속성을 활용.
            회전 속도를 함께 지정해서 이동 방향으로 캐릭터가 부드럽게 회전하도록 기능 추가.
            
            ```cpp
            // SXPlayerCharacter.cpp
            
            ...
            #include "GameFramework/CharacterMovementComponent.h"
            
            ...
            
            void ASXPlayerCharacter::SetViewMode(EViewMode InViewMode)
            {
            	...
            
            	switch (CurrentViewMode)
            	{
            	case EViewMode::BackView:
            		bUseControllerRotationPitch = false;
            		//bUseControllerRotationYaw = true;
            		bUseControllerRotationYaw = false;
            		...
            
            		GetCharacterMovement()->RotationRate = FRotator(0.f, 360.f, 0.f);
            		GetCharacterMovement()->bOrientRotationToMovement = true;
            		GetCharacterMovement()->bUseControllerDesiredRotation = false;
            
            		break;
            	case EViewMode::None:
            	case EViewMode::End:
            	default:
            		break;
            	}
            }
            
            ...
            ```
            
        
        - bUseControllerRotationYaw와 bOrientRotationToMovement의 차이
        bUseControllerRotationYaw를 true로 두면, 동시에 시선방향과 캐릭터 회전이 동기화
        bOrientRotationToMovement를 true로 두면, 이동 키를 눌러야 동기화됨.
        
    - 5.5 QuarterView
        - [실습] QuarterView
            
            ```cpp
            // SXPlayerCharacter.h
            
            ...
            
            UENUM(BlueprintType)
            enum class EViewMode : uint8
            {
            	None,
            	BackView,
            	QuarterView,
            	End
            };
            
            ...
            class SHOOTERX_API ASXPlayerCharacter : public ASXCharacterBase
            {
            	...
            
            #pragma region Override ACharacter
            
            public:
            	...
            
            	virtual void Tick(float DeltaSeconds) override;
            
            protected:
            	...
            
            #pragma endregion
            ...
            
            #pragma region ViewMode
            
            public:
            	...
            
            protected:
            	...
            
            	FVector DirectionToMove = FVector::ZeroVector;
            
            #pragma endregion
            
            };
            
            ```
            
            ```cpp
            // SXPlayerCharacter.cpp
            
            ...
            
            ASXPlayerCharacter::ASXPlayerCharacter()
            {
            	PrimaryActorTick.bCanEverTick = true;
            
            	...
            }
            
            ...
            
            void ASXPlayerCharacter::PossessedBy(AController* NewController)
            {
            	...
            
            	//SetViewMode(EViewMode::BackView);
            	SetViewMode(EViewMode::QuarterView);
            }
            
            void ASXPlayerCharacter::Tick(float DeltaSeconds)
            {
            	Super::Tick(DeltaSeconds);
            
            	switch (CurrentViewMode)
            	{
            	case EViewMode::BackView:
            		break;
            	case EViewMode::QuarterView:
            	{
            		if (KINDA_SMALL_NUMBER < DirectionToMove.SizeSquared())
            		{
            			GetController()->SetControlRotation(FRotationMatrix::MakeFromX(DirectionToMove).Rotator());
            			AddMovementInput(DirectionToMove);
            			DirectionToMove = FVector::ZeroVector;
            		}
            		break;
            	}
            	case EViewMode::None:
            	case EViewMode::End:
            	default:
            		break;
            	}
            }
            
            ...
            
            void ASXPlayerCharacter::InputMove(const FInputActionValue& InValue)
            {
            	...
            
            	switch (CurrentViewMode)
            	{
            	case EViewMode::BackView:
            	{ 
            		...
            	}
            	case EViewMode::QuarterView:
            		DirectionToMove.X = MovementVector.X;
            		DirectionToMove.Y = MovementVector.Y;
            
            		break;
            	case EViewMode::None:
            	case EViewMode::End:
            	default:
            		...
            		break;
            	}
            }
            
            void ASXPlayerCharacter::InputLook(const FInputActionValue& InValue)
            {
            	...
            
            	switch (CurrentViewMode)
            	{
            	case EViewMode::BackView:
            		...
            		break;
            	case EViewMode::QuarterView:
            	case EViewMode::None:
            	case EViewMode::End:
            	default:
            		break;
            	}
            }
            
            void ASXPlayerCharacter::SetViewMode(EViewMode InViewMode)
            {
            	...
            
            	switch (CurrentViewMode)
            	{
            	case EViewMode::BackView:
            		...
            
            		break;
            	case EViewMode::QuarterView:
            		bUseControllerRotationPitch = false;
            		bUseControllerRotationYaw = true;
            		bUseControllerRotationRoll = false;
            
            		SpringArmComponent->TargetArmLength = 800.f;
            		SpringArmComponent->SetRelativeRotation(FRotator(-45.f, 0.f, 0.f));
            
            		SpringArmComponent->bUsePawnControlRotation = false;
            
            		SpringArmComponent->bInheritPitch = false;
            		SpringArmComponent->bInheritYaw = false;
            		SpringArmComponent->bInheritRoll = false;
            
            		SpringArmComponent->bDoCollisionTest = false;
            
            		break;
            	case EViewMode::None:
            	case EViewMode::End:
            	default:
            		break;
            	}
            }
            
            ```
            
        
        - 입력 관련 이벤트 함수와 Tick() 함수
        플레이어의 입력 값에 따라 액터의 행동이 결정되어야 하므로,
        입력 관련 이벤트 함수를 먼저 호출해서 플레이어의 의지를 확인함.
        그다음 Tick() 함수를 호출해서 플레이어의 입력에 대응할 액터의 최종 행동 결정.
        그 후에 게임 로직의 결과를 보고 Animation 관련 이벤트 함수가 동작됨.
        주의해야 할 점은, 입력 관련 이벤트 함수가 해당 입력이 있을 때만 매 프레임 호출되는 것.
        입력 관련 이벤트 함수 > 액터의 Tick() 함수 > 애니메이션 관련 이벤트 함수
        
        - FRotationMatrix
        회전 좌표계 정보를 저장하는 행렬.
        
        - BackView 방식과 QuarterView 방식의 이동 방향 도출
        - **BackView**
          컨트롤 로테이션으로 회전 행렬 생성. 컨트롤 로테이션이 결국 카메라의 로테이션이었기 때문.
          회전 행렬의 X축과 Y축을 통해 이동 방향 도출.
        - **QuarterView**
          입력 이벤트 함수에서 X축 값과 Y축 값으로 방향 벡터 값을 생성.
          이 방향 벡터를 캐릭터의 컨트롤 로테이션(플레이어의 회전 의지)으로 간주하고자 함.
          해당 방향 벡터를 MakeFromX() 함수의 인자로 전달. 
          직교하는 나머지 두 축을 구해서 회전 행렬 생성.
          이 회전 행렬에 대응되는 FRotator를 컨트롤 로테이션에 대입.
        
        - 하나의 벡터로부터 회전 행렬을 구하는 함수
        MakeFromX(), MakeFromY(), MakeFromZ()
          ex) QuarterView 방식에서는 두 축의 입력을 합산한 벡터와
            캐릭터의 시선 방향(X축)이 같아야 하므로 MakeFromX()가 사용됨.
        
        - [실습] bUseControllerDesiredRotation 속성
        현재 캐릭터는 키보드 조합에 따라 45도 단위로 끊어져서 회전함.
        캐릭터 무브먼트의 bUseControllerDesiredRotation 속성을 활용하면
        컨트롤 회전이 가리키는 방향으로 캐릭터가 부드럽게 회전함.
            
            ```cpp
            // ASXPlayerCharacter.cpp
            
            ...
            
            void ASXPlayerCharacter::SetViewMode(EViewMode InViewMode)
            {
            	...
            
            	switch (CurrentViewMode)
            	{
            	case EViewMode::BackView:
            		...
            
            		break;
            	case EViewMode::QuarterView:
            		bUseControllerRotationPitch = false;
            		//bUseControllerRotationYaw = true;
            		bUseControllerRotationYaw = false; // 더이상 컨트롤 로테이션과 폰의 로테이션을 동기화 할 필요가 없음.
            		bUseControllerRotationRoll = false;
            
            		...
            
            		GetCharacterMovement()->RotationRate = FRotator(0.f, 360.f, 0.f);
            		GetCharacterMovement()->bOrientRotationToMovement = false;
            		GetCharacterMovement()->bUseControllerDesiredRotation = true;
            
            		break;
            	case EViewMode::None:
            	case EViewMode::End:
            	default:
            		break;
            	}
            }
            ```
            
        
    - 5.6 시점 전환 구현
        - [실습] 시점 전환용 단축키 추가
            
            Content Browser > InputAction > 새 Input 애셋 > Input Action > “IA_ChangeView”
            SXInputConfig 클래스를 아래와 같이 수정. IC_PlayerCharacter에 IA_ChangeView 추가.
            IMC_PlayerCharacter에 IA_ChangeView 추가. IA_ChangeView는 V 키에 매핑.
            
            ```cpp
            // SXInputConfig.h
            
            ...
            class SHOOTERX_API USXInputConfig : public UDataAsset
            {
            	...
            
            public:
            	...
            
            	UPROPERTY(EditAnywhere, BlueprintReadOnly)
            	TObjectPtr<UInputAction> ChangeView;
            
            };
            
            ```
            
        
        - [실습] 시점 전환 구현
            
            ```cpp
            // SXPlayerCharacter.h
            
            ...
            class SHOOTERX_API ASXPlayerCharacter : public ASXCharacterBase
            {
            	...
            
            #pragma region Input
            
            private:
            	...
            
            	void InputChangeView(const FInputActionValue& InValue);
            
            protected:
            	...
            
            #pragma endregion
            
            ...
            
            };
            
            ```
            
            ```cpp
            // SXPlayerCharacter.cpp
            
            ...
            
            void ASXPlayerCharacter::SetupPlayerInputComponent(UInputComponent* PlayerInputComponent)
            {
            	...
            	if (IsValid(EnhancedInputComponent) == true)
            	{
            		...
            		EnhancedInputComponent->BindAction(PlayerCharacterInputConfig->ChangeView, ETriggerEvent::Started, this, &ThisClass::InputChangeView);
            	}
            }
            
            ...
            
            void ASXPlayerCharacter::InputChangeView(const FInputActionValue& InValue)
            {
            	switch (CurrentViewMode)
            	{
            	case EViewMode::BackView:
            		SetViewMode(EViewMode::QuarterView);
            		break;
            	case EViewMode::QuarterView:
            		SetViewMode(EViewMode::BackView);
            		break;
            	case EViewMode::None:
            	case EViewMode::End:
            	default:
            		break;
            	}
            }
            
            ...
            
            ```
            
        
        - [실습] 시점 전환시 스프링암의 길이가 부드럽게 전환되게끔 구현
            
            SpringArm의 길이와 회전 값이 목표 값까지 각각 서서히 변하게끔 
            FMath 클래스에서 제공하는 InterpTo() 함수를 활용함.
            InterpTo() 함수는 등속으로 목표 지점까지 진행하되, 
            목표 지점에 도달하면 그 값에서 멈추는 함수.
            float 자료형은 FInterpTo(), FVector 자료형은 VInterpTo(), FRotator 자료형은 RInterpTo().
            
            ```cpp
            // SXPlayerCharacter.h
            
            ...
            class SHOOTERX_API ASXPlayerCharacter : public ASXCharacterBase
            {
            	...
            
            #pragma region ViewMode
            
            public:
            	...
            
            protected:
            	...
            
            	float DestArmLength = 0.f;
            
            	float ArmLengthChangeSpeed = 3.f;
            
            	FRotator DestArmRotation = FRotator::ZeroRotator;
            
            	float ArmRotationChangeSpeed = 10.f;
            
            #pragma endregion
            
            };
            
            ```
            
            ```cpp
            // SXPlayerCharacter.cpp
            
            ...
            
            void ASXPlayerCharacter::PossessedBy(AController* NewController)
            {
            	...
            
            	//SetViewMode(EViewMode::QuarterView);
            	SetViewMode(EViewMode::BackView);
            	DestArmLength = 400.f;
            	DestArmRotation = FRotator::ZeroRotator;
            }
            
            void ASXPlayerCharacter::Tick(float DeltaSeconds)
            {
            	...
            
            	if (KINDA_SMALL_NUMBER < abs(DestArmLength - SpringArmComponent->TargetArmLength))
            	{
            		SpringArmComponent->TargetArmLength = FMath::FInterpTo(SpringArmComponent->TargetArmLength, DestArmLength, DeltaSeconds, ArmLengthChangeSpeed);
            		SpringArmComponent->SetRelativeRotation(FMath::RInterpTo(SpringArmComponent->GetRelativeRotation(), DestArmRotation, DeltaSeconds, ArmRotationChangeSpeed));
            	}
            }
            
            ...
            
            void ASXPlayerCharacter::InputChangeView(const FInputActionValue& InValue)
            {
            	switch (CurrentViewMode)
            	{
            	case EViewMode::BackView:
            		// SetViewMode(EViewMode::QuarterView);
            		/* Case 1. BackView에서 QuarterView로
            
            		BackView는 컨트롤 로테이션이 스프링암 로테이션에 동기화 되고 있음.
            		QuarterView는 컨트롤 로테이션에 폰 로테이션이 동기화 됨.
            		따라서 시점 변경 전에 컨트롤 로테이션에 폰의 로테이션을 세팅해둬야 함.
            
            		*/
            		GetController()->SetControlRotation(GetActorRotation());
            		DestArmLength = 800.f;
            		DestArmRotation = FRotator(-45.f, 0.f, 0.f);
            		SetViewMode(EViewMode::QuarterView);
            
            		break;
            	case EViewMode::QuarterView:
            		// SetViewMode(EViewMode::BackView);
            		/* Case 2. QuarterView에서 BackView로
            
            		QuarterView는 컨트롤 로테이션에 폰 로테이션이 동기화 됨.
            		BackView는 컨트롤 로테이션이 스프링암 로테이션에 동기화 되고 있음.
            		따라서 시점 변경 전에 컨트롤 로테이션에 스프링암 로테이션을 세팅해둬야 함.
            
            		*/
            		GetController()->SetControlRotation(SpringArmComponent->GetRelativeRotation());
            		DestArmLength = 400.f;
            		DestArmRotation = FRotator::ZeroRotator;
            		SetViewMode(EViewMode::BackView);
            
            		break;
            	case EViewMode::End:
            		break;
            	default:
            		break;
            	}
            }
            
            void ASXPlayerCharacter::SetViewMode(EViewMode InViewMode)
            {
            	...
            
            	switch (CurrentViewMode)
            	{
            	case EViewMode::BackView:
            		...
            
            		//SpringArmComponent->TargetArmLength = 400.f;
            		//SpringArmComponent->SetRelativeRotation(FRotator::ZeroRotator);
            
            		...
            
            		break;
            	case EViewMode::QuarterView:
            		...
            
            		//SpringArmComponent->TargetArmLength = 800.f;
            		//SpringArmComponent->SetRelativeRotation(FRotator(-45.f, 0.f, 0.f));
            
            		...
            
            		break;
            	case EViewMode::None:
            	case EViewMode::End:
            	default:
            		break;
            	}
            }
            
            ```
            
        
        - TPS 전용 시점 설정
            
            우리가 만들게 될 프로젝트는 TPS 게임. 이에 맞게 아래와 같이 코드 작성.
            
            ```cpp
            // SXPlayerCharacter.h
            
            #pragma once
            
            #include "CoreMinimal.h"
            #include "Character/SXCharacterBase.h"
            #include "InputActionValue.h"
            #include "SXPlayerCharacter.generated.h"
            
            class USpringArmComponent;
            class UCameraComponent;
            class USXInputConfig;
            class UInputMappingContext;
            
            /**
             * 
             */
            UCLASS()
            class SHOOTERX_API ASXPlayerCharacter : public ASXCharacterBase
            {
            	GENERATED_BODY()
            
            #pragma region Override ACharacter
            
            public:
            	ASXPlayerCharacter();
            
            	virtual void BeginPlay() override;
            
            protected:
            	virtual void SetupPlayerInputComponent(UInputComponent* PlayerInputComponent) override;
            
            protected:
            	UPROPERTY(EditAnywhere, BlueprintReadOnly, meta = (AllowPrivateAccess))
            	TObjectPtr<USpringArmComponent> SpringArmComponent;
            
            	UPROPERTY(EditAnywhere, BlueprintReadOnly, meta = (AllowPrivateAccess))
            	TObjectPtr<UCameraComponent> CameraComponent;
            
            #pragma endregion
            
            #pragma region Input
            
            private:
            	void InputMove(const FInputActionValue& InValue);
            
            	void InputLook(const FInputActionValue& InValue);
            
            protected:
            	UPROPERTY(EditAnywhere, BlueprintReadOnly, meta = (AllowPrivateAccess))
            	TObjectPtr<USXInputConfig> PlayerCharacterInputConfig;
            
            	UPROPERTY(EditAnywhere, BlueprintReadOnly, meta = (AllowPrivateAccess))
            	TObjectPtr<UInputMappingContext> PlayerCharacterInputMappingContext;
            
            #pragma endregion
            
            };
            
            ```
            
            ```cpp
            // SXPlayerCharacter.cpp
            
            #include "Character/SXPlayerCharacter.h"
            #include "GameFramework/SpringArmComponent.h"
            #include "Camera/CameraComponent.h"
            #include "EnhancedInputComponent.h"
            #include "EnhancedInputSubsystems.h"
            #include "Input/SXInputConfig.h"
            #include "Kismet/KismetSystemLibrary.h"
            #include "GameFramework/CharacterMovementComponent.h"
            
            ASXPlayerCharacter::ASXPlayerCharacter()
            {
            	PrimaryActorTick.bCanEverTick = false;
            
            	bUseControllerRotationPitch = false;
            	bUseControllerRotationYaw = true;
            	bUseControllerRotationRoll = false;
            
            	GetCharacterMovement()->RotationRate = FRotator(0.f, 500.f, 0.f);
            	GetCharacterMovement()->bOrientRotationToMovement = true;
            	GetCharacterMovement()->bUseControllerDesiredRotation = false;
            	GetCharacterMovement()->MaxWalkSpeed = 600.f;
            
            	SpringArmComponent = CreateDefaultSubobject<USpringArmComponent>(TEXT("SpringArmComponent"));
            	SpringArmComponent->SetupAttachment(RootComponent);
            	SpringArmComponent->TargetArmLength = 400.f;
            	SpringArmComponent->bUsePawnControlRotation = true;
            	SpringArmComponent->bInheritPitch = true;
            	SpringArmComponent->bInheritYaw = true;
            	SpringArmComponent->bInheritRoll = false;
            	SpringArmComponent->bDoCollisionTest = true;
            	SpringArmComponent->SetRelativeLocation(FVector(0.f, 25.f, 25.f));
            
            	CameraComponent = CreateDefaultSubobject<UCameraComponent>(TEXT("CameraComponent"));
            	CameraComponent->SetupAttachment(SpringArmComponent);
            	CameraComponent->bUsePawnControlRotation = false;
            }
            
            void ASXPlayerCharacter::BeginPlay()
            {
            	Super::BeginPlay();
            
            	APlayerController* PlayerController = Cast<APlayerController>(GetController());
            	if (IsValid(PlayerController) == true)
            	{
            		UEnhancedInputLocalPlayerSubsystem* Subsystem = ULocalPlayer::GetSubsystem<UEnhancedInputLocalPlayerSubsystem>(PlayerController->GetLocalPlayer());
            		if (IsValid(Subsystem) == true)
            		{
            			Subsystem->AddMappingContext(PlayerCharacterInputMappingContext, 0);
            		}
            	}
            }
            
            void ASXPlayerCharacter::SetupPlayerInputComponent(UInputComponent* PlayerInputComponent)
            {
            	Super::SetupPlayerInputComponent(PlayerInputComponent);
            
            	UEnhancedInputComponent* EnhancedInputComponent = Cast<UEnhancedInputComponent>(PlayerInputComponent);
            	if (IsValid(EnhancedInputComponent) == true)
            	{
            		EnhancedInputComponent->BindAction(PlayerCharacterInputConfig->Move, ETriggerEvent::Triggered, this, &ThisClass::InputMove);
            		EnhancedInputComponent->BindAction(PlayerCharacterInputConfig->Look, ETriggerEvent::Triggered, this, &ThisClass::InputLook);
            	}
            }
            
            void ASXPlayerCharacter::InputMove(const FInputActionValue& InValue)
            {
            	FVector2D MovementVector = InValue.Get<FVector2D>();
            
            	const FRotator ControlRotation = GetController()->GetControlRotation();
            	const FRotator ControlRotationYaw(0.f, ControlRotation.Yaw, 0.f);
            
            	const FVector ForwardVector = FRotationMatrix(ControlRotationYaw).GetUnitAxis(EAxis::X);
            	const FVector RightVector = FRotationMatrix(ControlRotationYaw).GetUnitAxis(EAxis::Y);
            
            	AddMovementInput(ForwardVector, MovementVector.X);
            	AddMovementInput(RightVector, MovementVector.Y);
            }
            
            void ASXPlayerCharacter::InputLook(const FInputActionValue& InValue)
            {
            	FVector2D LookVector = InValue.Get<FVector2D>();
            
            	AddControllerYawInput(LookVector.X);
            	AddControllerPitchInput(LookVector.Y);
            }
            
            ```
