- Shooting
    - 10.1 사격 애니메이션 구현
        - [실습] 사격 입력 추가
            
            Content Browser > ShooterX > Input > InputAction
            새 Input 애셋 > InputAction > “IA_Attack_Ranged”
            아래와 같이 작성 후 IC_PlayerCharacter에 IA_Attack_Ranged 연결.
            IMC_PlayerCharacter에도 IA_Attack_Ranged를 좌클릭에 매핑.
            
            ```cpp
            // SXInputConfig.h
            
            ...
            class SHOOTERX_API USXInputConfig : public UDataAsset
            {
            	...
            
            public:
            	...
            
            	UPROPERTY(EditAnywhere, BlueprintReadOnly)
            	TObjectPtr<UInputAction> AttackRanged;
            
            };
            
            ```
            
        
        - [실습] 사격 애님몽타주 제작
            
            Content Browser > MM_Rifle_Fire 검색 후 우클릭 > Create > Create AnimMontage
            “AM_Attack_Ranged” 생성. 이렇게하면 스켈레톤 지정 안하고 실수도 없음.
            Content Browser > MM_Attack_Ranged 검색 후 AnimationMontage로 이동.
            
            <img width="349" height="736" alt="스크린샷 2025-09-03 190927" src="https://github.com/user-attachments/assets/8ae5d41d-1ca8-4765-8122-95bdf0bcf828" />

        
        - [실습] 사격 애님몽타주 연결
            
            ```cpp
            // SXWeapon.h
            
            ...
            class UAnimMontage;
            
            UCLASS()
            class SHOOTERX_API ASXWeapon : public AActor
            {
            	...
            	
            public:	
            	...
            
            	UAnimMontage* GetAttackMontage() const { return AttackMontage; }
            
            protected:
            	...
            	
            protected:
            	...
            
            	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)
            	TObjectPtr<UAnimMontage> AttackMontage;
            
            };
            
            ```
            
            ```cpp
            // SXCharacterBase.h
            
            ...
            class ASXWeapon;
            
            UCLASS()
            class SHOOTERX_API ASXCharacterBase : public ACharacter
            {
            	...
            
            #pragma region Attack
            
            public:
            	...
            
            	UAnimMontage* GetCurrentWeaponAttackAnimMontage() const;
            
            protected:
            	...
            	
            public:
            	...
            
            	UPROPERTY()
            	TObjectPtr<ASXWeapon> CurrentWeapon;
            
            protected:
            	...
            
            #pragma endregion
            
            ...
            
            };
            
            ```
            
            ```cpp
            // SXCharacterBase.cpp
            
            ...
            #include "Item/SXWeapon.h"
            
            ...
            
            UAnimMontage* ASXCharacterBase::GetCurrentWeaponAttackAnimMontage() const
            {
            	if (IsValid(CurrentWeapon) == true)
            	{
            		return CurrentWeapon->GetAttackMontage();
            	}
            	return nullptr;
            }
            
            ...
            
            ```
            
            ```cpp
            // SXWeapon.cpp
            
            ...
            
            void ASXWeapon::HandleOnPickUp(ASXPlayerCharacter* InPickUpCharacter)
            {
            	...
            	InPickUpCharacter->CurrentWeapon = this;
            }
            
            ```
            
            ```cpp
            // SXPlayerCharacter.h
            
            ...
            class SHOOTERX_API ASXPlayerCharacter : public ASXCharacterBase
            {
            	...
            
            #pragma region Input
            
            private:
            	...
            
            	void InputAttackRanged(const FInputActionValue& InValue);
            
            protected:
            	...
            
            #pragma endregion
            
            ...
            
            };
            
            ```
            
            ```cpp
            // SXPlayerCharacter.cpp
            
            ...
            #include "Item/SXWeapon.h"
            
            ...
            
            void ASXPlayerCharacter::SetupPlayerInputComponent(UInputComponent* PlayerInputComponent)
            {
            	...
            	if (IsValid(EnhancedInputComponent) == true)
            	{
            		...
            		EnhancedInputComponent->BindAction(PlayerCharacterInputConfig->AttackRanged, ETriggerEvent::Started, this, &ThisClass::InputAttackRanged);
            	}
            }
            
            ...
            
            void ASXPlayerCharacter::InputAttackRanged(const FInputActionValue& InValue)
            {
            	if (0.f < GetCharacterMovement()->Velocity.Size())
            	{
            		return;
            	}
            	
            	if (IsValid(CurrentWeapon) == false)
            	{
            		return;
            	}
            	
            	if (IsValid(GetCurrentWeaponAttackAnimMontage()) == false)
            	{
            		return;
            	}
            
            	UAnimInstance* AnimInstance = GetMesh()->GetAnimInstance();
            	if (IsValid(AnimInstance) == true)
            	{
            		if (AnimInstance->Montage_IsPlaying(GetCurrentWeaponAttackAnimMontage()) == false)
            		{
            			AnimInstance->Montage_Play(GetCurrentWeaponAttackAnimMontage());
            		}
            	}
            }
            
            ```
            
          <img width="346" height="102" alt="스크린샷 2025-09-03 191046" src="https://github.com/user-attachments/assets/e5107325-5103-4612-82e0-4f90f4186b5c" />
   
            BP_Rifle > Details
            
        
        - [실습] 사격 애니메이션이 재생되지 않는 문제
            
            지금은 우클릭해도 사격 애니메이션이 재생되지 않음. 이는 블랜드 시간 문제.
            AM_Fire_Rifle의 플레이 시간이 0.23초로 매우 짧음.
            그런데 Asset Details > Blend In > Blend Time은 0.25초라서 블랜드 하느라 재생되지 않음.
            Blend In과 Blend Out의 Blend Time을 0.1로 수정하여 해결.
            
        
        - [실습] 견착 애니메이션
            
            ```cpp
            // SXAnimInstance.h
            
            ...
            class SHOOTERX_API USXAnimInstance : public UAnimInstance
            {
            	...
            	
            protected:
            	...
            
            	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)
            	uint8 bIsUnarmed : 1;
            
            };
            
            ```
            
            ```cpp
            // SXAnimInstance.cpp
            
            ...
            
            void USXAnimInstance::NativeInitializeAnimation()
            {
            	...
            
            	bIsUnarmed = true;
            }
            
            void USXAnimInstance::NativeUpdateAnimation(float DeltaSeconds)
            {
            	if (IsValid(OwnerCharacter) == true && IsValid(OwnerCharacterMovement) == true)
            	{
            		...
            
            		bIsUnarmed = OwnerCharacter->GetCurrentWeaponAttackAnimMontage() == nullptr ? true : false;
            	}
            }
            
            ...
            
            ```
            
           <img width="353" height="780" alt="스크린샷 2025-09-03 191116" src="https://github.com/user-attachments/assets/556b1412-fd18-4d07-aaed-abc3671870d2" />

        - [실습] 애임오프셋 애셋 준비
            
            견착은 하고 있으나 마우스를 위로 올려도 총구가 안올라감.
            이를 위해서 애임오프셋 애셋을 준비하고자 함.
            
            ContentBrowser > Content > ShooterX > Animation 우클릭 > 새 폴더 “AimOffset”
            새 Animation 애셋 > AimOffset > “AO_Rifle_Idle”
            
            <img width="348" height="275" alt="스크린샷 2025-09-03 191137" src="https://github.com/user-attachments/assets/bfdb0aee-320f-49d5-bbdd-195d4abd111a" />

            
            <img width="350" height="795" alt="스크린샷 2025-09-03 191150" src="https://github.com/user-attachments/assets/ca0a2df0-7013-4350-b2df-4a201bb37e25" />

            
            <img width="356" height="285" alt="스크린샷 2025-09-03 191155" src="https://github.com/user-attachments/assets/a9a1ce1d-8de1-4a89-9563-164c16999cfb" />

            
        
        - [실습] 에임 오프셋
            
            ```cpp
            // SXAnimInstance.h
            
            ...
            class SHOOTERX_API USXAnimInstance : public UAnimInstance
            {
            	...
            	
            protected:
            	...
            
            	UPROPERTY(BlueprintReadOnly)
            	float NormalizedCurrentPitch;
            
            };
            
            ```
            
            ```cpp
            // SXAnimInstance.cpp
            
            ...
            #include "Character/SXPlayerCharacter.h"
            
            ...
            
            void USXAnimInstance::NativeUpdateAnimation(float DeltaSeconds)
            {
            	if (IsValid(OwnerCharacter) == true && IsValid(OwnerCharacterMovement) == true)
            	{
            		...
            		
            		if (APlayerController* OwnerPlayerController = Cast<APlayerController>(OwnerCharacter->GetController()))
            		{
            			NormalizedCurrentPitch = UKismetMathLibrary::NormalizeAxis(OwnerPlayerController->GetControlRotation().Pitch);
            		}
            	}
            }
            
            ...
            
            ```
            
            <img width="351" height="154" alt="스크린샷 2025-09-03 191226" src="https://github.com/user-attachments/assets/2123d787-5360-40c8-bdef-3b9e9b41458c" />

        
        - [실습] 애님 노티파이를 활용한 머즐 이펙트와 사운드
            
            AM_Attack_Ranged > Notifies 테이블 우클릭 > Add Notify > Play Sound 검색 후 엔터
            PlaySound 노티파이 클릭 > Details > Sound에 원하는 사운드 지정.
            Notifies 옆 역삼각형 클릭 > Add Notify Track > 2번 트랙 추가
            2번 트랙 우클릭 > Add Notify > Play Particle Effect 검색 후 엔터
            PlayParticleEffect 노티파이 클릭 > Details > 아래와 같이 지정.
            
            <img width="344" height="176" alt="스크린샷 2025-09-03 191238" src="https://github.com/user-attachments/assets/10e5dcbc-a47c-4fde-8cf7-454fc2a26879" />

        
        - [실습] 발사 시 카메라 쉐이크 애셋 제작
            
            Content Browser > ShooterX 우클릭 > 새 폴더 “Effect”
            Effect > 새 Blueprint 애셋 > DefaultCameraShakeBase 부모 클래스
            “BP_Attack_Ranged_Camera_Shake” 생성 > Details 내의 값들을 아래 그림과 같이 설정.
            
            <img width="346" height="442" alt="스크린샷 2025-09-03 191250" src="https://github.com/user-attachments/assets/347311b0-df10-47ac-b8c6-0948de70118c" />

            
        
        - [실습] 카메라 쉐이크 로직 구현
            
            ```cpp
            // SXPlayerCharacter.h
            
            ...
            class UCameraShakeBase;
            
            /**
             * 
             */
            UCLASS()
            class SHOOTERX_API ASXPlayerCharacter : public ASXCharacterBase
            {
            	...
            
            #pragma region Effect
            
            public:
            	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)
            	TSubclassOf<UCameraShakeBase> AttackRangedCameraShake;
            
            #pragma endregion 
            
            };
            
            ```
            
            ```cpp
            // SXPlayerCharacter.cpp
            
            ...
            
            void ASXPlayerCharacter::InputAttackRanged(const FInputActionValue& InValue)
            {
            	...
            	
            	APlayerController* OwnerPlayerController = Cast<APlayerController>(GetController());
            	if (IsValid(AttackRangedCameraShake) == true && IsValid(OwnerPlayerController) == true)
            	{
            		OwnerPlayerController->ClientStartCameraShake(AttackRangedCameraShake);
            	}
            }
            
            ```
            
            <img width="349" height="91" alt="스크린샷 2025-09-03 191307" src="https://github.com/user-attachments/assets/74eb3780-c366-4495-978c-89d562666930" />

            
        
    - 10.2 사격 로직
        - [실습] 크로스헤어 위젯 제작
            
            Content Browser > ShooterX 우클릭 > 새 폴더 “Texture”
            Texture 우클릭 > Import to Game/ShooterX/Texture… 클릭 후 파일 임포트.
            Content Browser > ShooterX > UI > 새 User Interface 애셋 > Widget Blueprint > UserWidget > “WBP_Crosshair”
            아래 그림을 참고하여 Hierarchy 작성.
            
            <img width="100" height="100" alt="Crosshair" src="https://github.com/user-attachments/assets/9f09b661-dc1c-4558-b5c3-7bd94a990962" />

            
           <img width="347" height="882" alt="스크린샷 2025-09-03 191502" src="https://github.com/user-attachments/assets/91dbe820-acf7-4131-aeb6-6d669ead8197" />

        <img width="344" height="391" alt="스크린샷 2025-09-03 191523" src="https://github.com/user-attachments/assets/c00d9d5c-ba7d-447f-b987-28f173403a92" />

        - [실습] 크로스헤어 위젯 연결
            
            ```cpp
            // SXPlayerController.h
            
            ...
            class UUserWidget;
            
            ...
            class SHOOTERX_API ASXPlayerController : public APlayerController
            {
            	...
            
            private:
            	...
            
            	UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = ASPlayerController, Meta = (AllowPrivateAccess))
            	TSubclassOf<UUserWidget> CrosshairWidgetClass;
            
            };
            
            ```
            
            ```cpp
            // SXPlayerController.cpp
            
            ...
            
            void ASXPlayerController::BeginPlay()
            {
            	...
            
            	if (IsValid(CrosshairWidgetClass) == true)
            	{
            		UUserWidget* CrosshairWidgetInstance = CreateWidget<UUserWidget>(this, CrosshairWidgetClass);
            		if (IsValid(CrosshairWidgetInstance) == true)
            		{
            			CrosshairWidgetInstance->AddToViewport(1);
            
            			CrosshairWidgetInstance->SetVisibility(ESlateVisibility::Visible);
            		}
            	}
            }
            
            ```
            
            <img width="347" height="110" alt="스크린샷 2025-09-03 191537" src="https://github.com/user-attachments/assets/6ca37412-3569-4f2e-93a3-04034c5d5cf6" />

        
        - [실습] 사격 구현
            
            ```cpp
            // SXWeapon.h
            
            ...
            class SHOOTERX_API ASXWeapon : public AActor
            {
            	...
            	
            public:	
            	...
            
            	float GetMaxAttackRange() const { return MaxAttackRange; }
            
            protected:
            	...
            
            protected:
            	...
            
            	UPROPERTY(EditAnywhere, BlueprintReadOnly, Meta = (Units = cm))
            	float MaxAttackRange = 25000.f;
            
            };
            
            ```
            
            ```cpp
            // SXCharacterBase.h
            
            ...
            class SHOOTERX_API ASXCharacterBase : public ACharacter
            {
            	...
            
            #pragma region Attack
            
            public:
            	...
            
            protected:
            	...
            	
            public:
            	...
            
            	static int32 ShowAttackRangedDebug;
            
            protected:
            	...
            
            #pragma endregion
            
            ...
            
            };
            
            ```
            
            ```cpp
            // SXCharacterBase.cpp
            
            ...
            
            int32 ASXCharacterBase::ShowAttackRangedDebug = 0;
            
            FAutoConsoleVariableRef CVarShowAttackRangedDebug(
            	TEXT("SX.ShowAttackRangedDebug"),
            	ASXCharacterBase::ShowAttackRangedDebug,
            	TEXT(""),
            	ECVF_Cheat
            );
            
            ASXCharacterBase::ASXCharacterBase()
            {
            	...
            }
            
            ...
            ```
            
            ```cpp
            // SXPlayerCharacter.h
            
            ...
            class SHOOTERX_API ASXPlayerCharacter : public ASXCharacterBase
            {
            	...
            
            #pragma region Input
            
            private:
            	...
            
            	void TryFire();
            
            protected:
            	...
            
            #pragma endregion
            
            ...
            
            };
            
            ```
            
            ```cpp
            // SXPlayerCharacter.cpp
            
            ...
            #include "Kismet/KismetMathLibrary.h"
            #include "Component/SXPickupComponent.h"
            #include "Engine/EngineTypes.h"
            #include "Engine/DamageEvents.h"
            #include "ShooterX.h"
            
            ...
            
            void ASXPlayerCharacter::InputAttackRanged(const FInputActionValue& InValue)
            {
            	...
            
            	/*
            	UAnimInstance* AnimInstance = GetMesh()->GetAnimInstance();
            	if (IsValid(AnimInstance) == true)
            	{
            		if (AnimInstance->Montage_IsPlaying(GetCurrentWeaponAttackAnimMontage()) == false)
            		{
            			AnimInstance->Montage_Play(GetCurrentWeaponAttackAnimMontage());
            		}
            	}
            	
            	APlayerController* OwnerPlayerController = Cast<APlayerController>(GetController());
            	if (IsValid(AttackRangedCameraShake) == true && IsValid(OwnerPlayerController) == true)
            	{
            		OwnerPlayerController->ClientStartCameraShake(AttackRangedCameraShake);
            	}
            	*/
            
            	TryFire();
            }
            
            void ASXPlayerCharacter::TryFire()
            {
            	APlayerController* PlayerController = GetController<APlayerController>();
            	if (IsValid(PlayerController) == true)
            	{
            #pragma region CaculateTargetTransform
            		float FocalDistance = 400.f;
            		FVector FocalLocation;
            		FVector CameraLocation;
            		FRotator CameraRotation;
            
            		PlayerController->GetPlayerViewPoint(CameraLocation, CameraRotation);
            
            		FVector AimDirectionFromCamera = CameraRotation.Vector().GetSafeNormal();
            		FocalLocation = CameraLocation + (AimDirectionFromCamera * FocalDistance);
            
            		FVector WeaponMuzzleLocation = CurrentWeapon->GetPickupComponent()->GetSocketLocation(TEXT("MuzzleFlash"));
            		FVector FinalFocalLocation = FocalLocation + (((WeaponMuzzleLocation - FocalLocation) | AimDirectionFromCamera) * AimDirectionFromCamera);
            
            		FTransform TargetTransform = FTransform(CameraRotation, FinalFocalLocation);
            
            		if (1 == ShowAttackRangedDebug)
            		{
            			DrawDebugSphere(GetWorld(), WeaponMuzzleLocation, 2.f, 16, FColor::Red, false, 60.f);
            
            			DrawDebugSphere(GetWorld(), CameraLocation, 2.f, 16, FColor::Yellow, false, 60.f);
            
            			DrawDebugSphere(GetWorld(), FinalFocalLocation, 2.f, 16, FColor::Magenta, false, 60.f);
            
            			// (WeaponLoc - FocalLoc)
            			DrawDebugLine(GetWorld(), FocalLocation, WeaponMuzzleLocation, FColor::Yellow, false, 60.f, 0, 2.f);
            
            			// AimDir
            			DrawDebugLine(GetWorld(), CameraLocation, FinalFocalLocation, FColor::Blue, false, 60.f, 0, 2.f);
            
            			// Project Direction Line
            			DrawDebugLine(GetWorld(), WeaponMuzzleLocation, FinalFocalLocation, FColor::Red, false, 60.f, 0, 2.f);
            		}
            
            #pragma endregion
            
            #pragma region PerformLineTracing
            
            		FVector BulletDirection = TargetTransform.GetUnitAxis(EAxis::X);
            		FVector StartLocation = WeaponMuzzleLocation;
            		FVector EndLocation = TargetTransform.GetLocation() + BulletDirection * CurrentWeapon->GetMaxAttackRange();
            
            		FHitResult HitResult;
            		FCollisionQueryParams TraceParams(NAME_None, false, this);
            		TraceParams.AddIgnoredActor(CurrentWeapon);
            
            		bool IsCollided = GetWorld()->LineTraceSingleByChannel(HitResult, StartLocation, EndLocation, ECC_Attack, TraceParams);
            		if (IsCollided == false)
            		{
            			HitResult.TraceStart = StartLocation;
            			HitResult.TraceEnd = EndLocation;
            		}
            
            		if (2 == ShowAttackRangedDebug)
            		{
            			if (IsCollided == true)
            			{
            				DrawDebugSphere(GetWorld(), StartLocation, 2.f, 16, FColor::Red, false, 60.f);
            
            				DrawDebugSphere(GetWorld(), HitResult.ImpactPoint, 2.f, 16, FColor::Green, false, 60.f);
            
            				DrawDebugLine(GetWorld(), StartLocation, HitResult.ImpactPoint, FColor::Blue, false, 60.f, 0, 2.f);
            			}
            			else
            			{
            				DrawDebugSphere(GetWorld(), StartLocation, 2.f, 16, FColor::Red, false, 60.f);
            
            				DrawDebugSphere(GetWorld(), EndLocation, 2.f, 16, FColor::Green, false, 60.f);
            
            				DrawDebugLine(GetWorld(), StartLocation, EndLocation, FColor::Blue, false, 60.f, 0, 2.f);
            			}
            		}
            
            #pragma endregion
            
            		if (IsCollided == true)
            		{
            			ASXCharacterBase* HittedCharacter = Cast<ASXCharacterBase>(HitResult.GetActor());
            			if (IsValid(HittedCharacter) == true)
            			{
            				FDamageEvent DamageEvent;
            				HittedCharacter->TakeDamage(10.f, DamageEvent, GetController(), this);
            			}
            		}
            		
            		UAnimInstance* AnimInstance = GetMesh()->GetAnimInstance();
            		if (IsValid(AnimInstance) == true)
            		{
            			if (AnimInstance->Montage_IsPlaying(GetCurrentWeaponAttackAnimMontage()) == false)
            			{
            				AnimInstance->Montage_Play(GetCurrentWeaponAttackAnimMontage());
            			}
            		}
            		
            		if (IsValid(AttackRangedCameraShake) == true)
            		{
            			PlayerController->ClientStartCameraShake(AttackRangedCameraShake);
            		}
            	}
            }
            
            ```
            
        
        - 연발 기획
        마우스 휠 버튼을 클릭하면 단발/연발이 토글링 되게끔 구현해보고자 함.
        단발일때는 지금처럼 클릭시에만 Attack() 함수가 호출됨.
        연발일때는 분당 탄환 개수를 조절하여 Attack() 함수가 호출되게끔 하려함.
        
        - [실습] 연발 입력 추가
            
            Content Browser > InputAction > Input > 새 InputAction 애셋 > “IA_Toggle_Selector”
            InputConfigs > IC_PlayerCharacter에도 IA_Toggle_Selector를 추가하기위해 클래스 수정
            IMC_PlayerCharacter > Mapping 추가 > IA_Trigger 지정하고 단축키는 마우스 휠버튼 클릭
            
            ```cpp
            // SXInputConfig.h
            
            ...
            class SHOOTERX_API USXInputConfig : public UDataAsset
            {
            	...
            
            public:
            	...
            
            	UPROPERTY(EditAnywhere, BlueprintReadOnly)
            	TObjectPtr<UInputAction> ToggleSelector;
            
            };
            
            ```
            
        
        - [실습] 단발/연발 토글링 구현
            
            ```cpp
            // SXPlayerCharacter.h
            
            ...
            class SHOOTERX_API ASXPlayerCharacter : public ASXCharacterBase
            {
            	...
            
            #pragma region Input
            
            private:
            	...
            
            	void InputToggleSelector(const FInputActionValue& InValue);
            
            	void InputStartFullAutoFire(const FInputActionValue& InValue);
            
            	void InputStopFullAutoFire(const FInputActionValue& InValue);
            
            protected:
            	...
            
            #pragma endregion
            
            ...
            
            #pragma region Selector
            
            public:
            	UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)
            	float FirePerMinute = 600;
            
            	bool bIsFullAutoFire = false;
            
            	FTimerHandle FullAutoTimerHandle;
            
            	float TimeBetweenFire;
            
            #pragma endregion
            
            };
            
            ```
            
            ```cpp
            // SXPlayerCharacter.cpp
            
            ...
            
            ASXPlayerCharacter::ASXPlayerCharacter()
            {
            	...
            
            	TimeBetweenFire = 60.f / FirePerMinute;
            }
            
            ...
            
            void ASXPlayerCharacter::SetupPlayerInputComponent(UInputComponent* PlayerInputComponent)
            {
            	...
            	if (IsValid(EnhancedInputComponent) == true)
            	{
            		...
            		EnhancedInputComponent->BindAction(PlayerCharacterInputConfig->ToggleSelector, ETriggerEvent::Started, this, &ThisClass::InputToggleSelector);
            		EnhancedInputComponent->BindAction(PlayerCharacterInputConfig->AttackRanged, ETriggerEvent::Started, this, &ThisClass::InputStartFullAutoFire);
            		EnhancedInputComponent->BindAction(PlayerCharacterInputConfig->AttackRanged, ETriggerEvent::Completed, this, &ThisClass::InputStopFullAutoFire);
            	}
            }
            
            ...
            
            void ASXPlayerCharacter::InputAttackRanged(const FInputActionValue& InValue)
            {
            	...
            
            	//TryFire();
            	if (false == bIsFullAutoFire)
            	{
            		TryFire();
            	}
            }
            
            ...
            
            void ASXPlayerCharacter::InputToggleSelector(const FInputActionValue& InValue)
            {
            	bIsFullAutoFire = !bIsFullAutoFire;
            }
            
            void ASXPlayerCharacter::InputStartFullAutoFire(const FInputActionValue& InValue)
            {
            	if (true == bIsFullAutoFire)
            	{
            		GetWorldTimerManager().SetTimer(FullAutoTimerHandle, this, &ThisClass::TryFire, TimeBetweenFire, true);
            	}
            }
            
            void ASXPlayerCharacter::InputStopFullAutoFire(const FInputActionValue& InValue)
            {
            	GetWorldTimerManager().ClearTimer(FullAutoTimerHandle);
            }
            
            ```
            
        
        - [실습] 줌 구현 준비
            
            Content Browser > InputActions > Input > 새 InputAction 애셋 > “IA_IronSight”
            InputConfigs > IC_PlayerCharacter에도 IA_IronSight를 추가하기위해 클래스 파일 수정
            IMC_PlayerCharacter > Mapping 추가 > IA_IronSight 지정하고 단축키는 Q
            
            ```cpp
            // SXInputConfig.h
            
            ...
            class SHOOTERX_API USXInputConfig : public UDataAsset
            {
            	...
            
            public:
            	...
            
            	UPROPERTY(EditAnywhere, BlueprintReadOnly)
            	TObjectPtr<UInputAction> IronSight;
            
            };
            
            ```
            
        
        - [실습] 줌 구현
            
            ```cpp
            // SXPlayerCharacter.h
            
            ...
            class SHOOTERX_API ASXPlayerCharacter : public ASXCharacterBase
            {
            	...
            	
            #pragma region Override ACharacter
            
            public:
            	...
            
            	virtual void Tick(float DeltaSeconds) override;
            
            protected:
            	...
            
            #pragma endregion
            
            #pragma region Input
            
            private:
            	...
            
            	void InputStartIronSight(const FInputActionValue& InValue);
            
            	void InputEndIronSight(const FInputActionValue& InValue);
            
            protected:
            	...
            
            #pragma endregion
            
            ...
            
            #pragma region IronSight
            
            protected:
            	float TargetFOV = 70.f;
            
            	float CurrentFOV = 70.f;
            
            #pragma endregion
            
            };
            
            ```
            
            ```cpp
            // SXPlayerCharacter.cpp
            
            ...
            
            ASXPlayerCharacter::ASXPlayerCharacter()
            {
            	PrimaryActorTick.bCanEverTick = true;
            
            	...
            }
            
            ...
            
            void ASXPlayerCharacter::Tick(float DeltaSeconds)
            {
            	Super::Tick(DeltaSeconds);
            
            	CurrentFOV = FMath::FInterpTo(CurrentFOV, TargetFOV, DeltaSeconds, 35.f);
            	CameraComponent->SetFieldOfView(CurrentFOV);
            }
            
            void ASXPlayerCharacter::SetupPlayerInputComponent(UInputComponent* PlayerInputComponent)
            {
            	...
            	if (IsValid(EnhancedInputComponent) == true)
            	{
            		...
            		EnhancedInputComponent->BindAction(PlayerCharacterInputConfig->IronSight, ETriggerEvent::Started, this, &ThisClass::InputStartIronSight);
            		EnhancedInputComponent->BindAction(PlayerCharacterInputConfig->IronSight, ETriggerEvent::Completed, this, &ThisClass::InputEndIronSight);
            	}
            }
            
            ...
            
            void ASXPlayerCharacter::InputStartIronSight(const FInputActionValue& InValue)
            {
            	TargetFOV = 45.f;
            }
            
            void ASXPlayerCharacter::InputEndIronSight(const FInputActionValue& InValue)
            {
            	TargetFOV = 70.f;
            }
            
            ```
            
        
    - 10.3 피격
        - ~~[실습] 부분 랙돌을 이용한 피격 모션 대체~~
            
            ```cpp
            // SXCharacterBase.h
            
            ...
            class SHOOTERX_API ASXCharacterBase : public ACharacter
            {
            	...
            
            #pragma region Ragdoll
            
            private:
            	UFUNCTION()
            	void OnHittedRagdollRestoreTimerElapsed();
            
            protected:
            	FTimerHandle HittedRagdollRestoreTimer;
            
            	FTimerDelegate HittedRagdollRestoreTimerDelegate;
            
            #pragma endregion
            
            };
            
            ```
            
            ```cpp
            // SXCharacterBase.cpp
            
            ...
            
            float ASXCharacterBase::TakeDamage(float DamageAmount, FDamageEvent const& DamageEvent, AController* EventInstigator, AActor* DamageCauser)
            {
            	...
            
            	if (StatusComponent->IsDead() == true)
            	{
            		...
            	}
            	else
            	{
            		FName PivotBoneName = FName(TEXT("spine_01"));
            		GetMesh()->SetAllBodiesBelowSimulatePhysics(PivotBoneName, true);
            		float BlendWeight = 1.f;
            		GetMesh()->SetAllBodiesBelowPhysicsBlendWeight(PivotBoneName, BlendWeight);
            			// 단, 위 로직은 GetMesh()->SetSimulatePhysics(false) 상태가 선행되어야함.
            
            		HittedRagdollRestoreTimerDelegate.BindUObject(this, &ThisClass::OnHittedRagdollRestoreTimerElapsed);
            		GetWorld()->GetTimerManager().SetTimer(HittedRagdollRestoreTimer, HittedRagdollRestoreTimerDelegate, 1.f, false);
            	}
            
            	...
            }
            
            ...
            
            void ASXCharacterBase::OnHittedRagdollRestoreTimerElapsed()
            {
            	FName PivotBoneName = FName(TEXT("spine_01"));
            	GetMesh()->SetAllBodiesBelowSimulatePhysics(PivotBoneName, false);
            	float BlendWeight = 0.f;
            	GetMesh()->SetAllBodiesBelowPhysicsBlendWeight(PivotBoneName, BlendWeight);
            }
            
            ```
            
        
        - [실습] 좀 더 자연스럽게 회복되게끔 구현
            
            ```cpp
            // SXCharacterBase.h
            
            ...
            class SHOOTERX_API ASXCharacterBase : public ACharacter
            {
            	...
            
            #pragma region Override ACharacter
            
            public:
            	...
            
            protected:
            	...
            
            	virtual void Tick(float DeltaSeconds) override;
            
            #pragma endregion
            
            ...
            
            #pragma region Ragdoll
            
            private:
            	...
            
            protected:
            	...
            
            	float TargetRagDollBlendWeight = 0.f;
            
            	float CurrentRagDollBlendWeight = 0.f;
            
            	bool bIsNowRagdollBlending = false;
            
            #pragma endregion
            
            };
            
            ```
            
            ```cpp
            // SXCharacterBase.cpp
            
            ...
            
            void ASXCharacterBase::Tick(float DeltaSeconds)
            {
            	Super::Tick(DeltaSeconds);
            
            	if (true == bIsNowRagdollBlending)
            	{
            		CurrentRagDollBlendWeight = FMath::FInterpTo(CurrentRagDollBlendWeight, TargetRagDollBlendWeight, DeltaSeconds, 10.f);
            
            		FName PivotBoneName = FName(TEXT("spine_01"));
            		GetMesh()->SetAllBodiesBelowPhysicsBlendWeight(PivotBoneName, CurrentRagDollBlendWeight);
            
            		if (CurrentRagDollBlendWeight - TargetRagDollBlendWeight < KINDA_SMALL_NUMBER)
            		{
            			GetMesh()->SetAllBodiesBelowSimulatePhysics(PivotBoneName, false);
            			bIsNowRagdollBlending = false;
            		}
            	}
            }
            
            ...
            
            float ASXCharacterBase::TakeDamage(float DamageAmount, FDamageEvent const& DamageEvent, AController* EventInstigator, AActor* DamageCauser)
            {
            	...
            
            	if (StatusComponent->IsDead() == true)
            	{
            		...
            	}
            	else
            	{
            		FName PivotBoneName = FName(TEXT("spine_01"));
            		GetMesh()->SetAllBodiesBelowSimulatePhysics(PivotBoneName, true);
            		//float BlendWeight = 1.f;
            		//GetMesh()->SetAllBodiesBelowPhysicsBlendWeight(PivotBoneName, BlendWeight);
            		TargetRagDollBlendWeight = 1.f;
            
            		HittedRagdollRestoreTimerDelegate.BindUObject(this, &ThisClass::OnHittedRagdollRestoreTimerElapsed);
            		GetWorld()->GetTimerManager().SetTimer(HittedRagdollRestoreTimer, HittedRagdollRestoreTimerDelegate, 1.f, false);
            	}
            
            	...
            }
            
            ...
            
            void ASXCharacterBase::OnHittedRagdollRestoreTimerElapsed()
            {
            	FName PivotBoneName = FName(TEXT("spine_01"));
            	//GetMesh()->SetAllBodiesBelowSimulatePhysics(PivotBoneName, false);
            	//float BlendWeight = 0.f;
            	//GetMesh()->SetAllBodiesBelowPhysicsBlendWeight(PivotBoneName, BlendWeight);
            	TargetRagDollBlendWeight = 0.f;
            	CurrentRagDollBlendWeight = 1.f;
            	bIsNowRagdollBlending = true;
            }
            
            ```
            
        
        - [실습] 부위 판정 구현
            
            ```cpp
            // SXPlayerCharacter.cpp
            
            ...
            
            void ASXPlayerCharacter::TryFire()
            {
            	...
            	if (IsValid(PlayerController) == true)
            	{
            		...
            
            		if (IsCollided == true)
            		{
            			...
            			
            			if (IsValid(HittedCharacter) == true)
            			{
            				...
            				
            				FString BoneNameString = HitResult.BoneName.ToString();
            				UKismetSystemLibrary::PrintString(this, BoneNameString);
            				DrawDebugSphere(GetWorld(), HitResult.Location, 3.f, 16, FColor(255, 0, 0, 255), true, 20.f, 0U, 5.f);
            			}
            		}
            
            		...
            	}
            }
            
            ...
            
            ```
            
        
        - [실습] None이라고 나온다면 그건 무엇이 문제일까?
            
            본의 이름을 가져오고자 했으나 None이라고 나온다면, 그건 캡슐에 부딪혔기 때문.
            이 문제를 해결하기 위해서라도 공격 트레이스 채널을 따로 만들어야 함.
            공격 트레이스 채널은 Capsule에는 반응하지 않고, SkeletalMesh에 반응하게끔 설정 필요.
            
        
        - [실습] 헤드샷 로직 추가
            
            ```cpp
            // SXPlayerCharacter.cpp
            
            ...
            
            void ASXPlayerCharacter::TryFire()
            {
            	...
            	if (IsValid(PlayerController) == true)
            	{
            		...
            
            		if (IsCollided == true)
            		{
            			ASXCharacterBase* HittedCharacter = Cast<ASXCharacterBase>(HitResult.GetActor());
            			if (IsValid(HittedCharacter) == true)
            			{
            				FDamageEvent DamageEvent;
            				//HittedCharacter->TakeDamage(10.f, DamageEvent, GetController(), this);
            
            				FString BoneNameString = HitResult.BoneName.ToString();
            				//UKismetSystemLibrary::PrintString(this, BoneNameString);
            				//DrawDebugSphere(GetWorld(), HitResult.Location, 3.f, 16, FColor(255, 0, 0, 255), true, 20.f, 0U, 5.f);
            
            				if (true == BoneNameString.Equals(FString(TEXT("HEAD")), ESearchCase::IgnoreCase))
            				{
            					HittedCharacter->TakeDamage(100.f, DamageEvent, GetController(), this);
            				}
            				else
            				{
            					HittedCharacter->TakeDamage(10.f, DamageEvent, GetController(), this);
            				}
            			}
            		}
            
            		...
            	}
            }
            
            ...
            
            ```
